---
title: "Effective Depth in Joint Source-Channel Coding: An Implicit Equilibrium Analysis"
title_zh: 联合源信道编码中的有效深度：一种隐式均衡分析
authors: "Kaiwen Yu, Gang Wu, Xiaodong Xu, Yi Ma, Rahim Tafazolli"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29737v2"
tags: ["query:channel-nn"]
score: 8.0
evidence: 联合源信道编码的隐式均衡框架
tldr: 针对深度联合信源信道编码中固定深度架构在信道条件变化时计算冗余或不充分的问题，提出Implicit-JSCC隐式均衡框架。将编解码建模为不动点均衡过程，由残差求解器决定有效深度，参数共享实现深度无关复杂度。开发高斯过程核演化分析信道噪声下接收端均衡迭代的扰动传播，推导理论指导的深度-信噪比关系。实验表明该方法实现竞争性重建性能、自适应推理和可控计算-质量权衡，并提供信噪比依赖的细化深度表征。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29737v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1778, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1741, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1740, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 919, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 868, \"height\": 665, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 868, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29737v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1484, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29737v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 887, \"height\": 1037, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29737v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 524, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29737v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1836, \"height\": 1365, \"label\": \"Table\"}]"
motivation: 现有Deep JSCC采用固定深度架构，在信道条件好时计算冗余，噪声严重时细化不足，缺乏深度-信噪比关系的理论指导。
method: 提出Implicit-JSCC隐式均衡框架，将编解码视为不动点均衡过程，通过残差求解器自动确定有效深度；并建立高斯过程核演化模型分析信道噪声扰动传播，推导深度-信噪比关系。
result: Implicit-JSCC在不同信噪比下达到竞争性重建性能，实现自适应推理和可控计算-质量权衡，深度-信噪比模型准确预测达到给定扰动容忍所需的接收端细化深度。
conclusion: 隐式均衡框架为Deep JSCC提供了自适应深度机制和理论分析工具，有效匹配信道条件与计算资源，提升系统鲁棒性与效率。
---

## 摘要
深度联合源信道编码（Deep JSCC）中的一个基本设计问题仍未被充分探索：给定信道信噪比（SNR），语义重建需要多大的有效计算深度？现有的Deep JSCC系统通常采用通过经验超参数调优选择的固定深度神经架构，这可能在良好信道条件下导致不必要的计算，而在严重信道噪声下导致精化不足。本文提出了一种隐式均衡框架Implicit-JSCC，将语义编码和解码形式化为不动点均衡过程。编码器和解码器的有效深度由基于残差的求解器收敛决定，而非人工预定义的层数，同时在均衡迭代间共享参数以实现与深度无关的参数复杂度。为了分析由此产生的有效深度行为，我们开发了一个受高斯过程启发的核演化框架，将均衡迭代建模为有效深度传播过程。由于信道噪声在编码器和解码器之间注入，分析追踪了接收端均衡迭代中信道引起的表示扰动，并推导出理论指导的深度-SNR关系。在离线校准系统特定参数后，所得模型刻画了不同SNR下所需的接收端精化深度。大量实验表明，Implicit-JSCC在实现竞争性重建性能的同时，支持基于残差的自适应推理和可控的计算-质量权衡。深度-SNR模型进一步刻画了达到规定扰动容限所需的SNR依赖的精化深度。

## Abstract
A fundamental design question in deep joint source-channel coding (Deep JSCC) remains insufficiently explored: given a channel signal-to-noise ratio (SNR), what effective computation depth is required for semantic reconstruction? Existing Deep JSCC systems typically employ fixed-depth neural architectures selected through empirical hyperparameter tuning, which may lead to unnecessary computation under favorable channel conditions and insufficient refinement under severe channel noise. This paper proposes \emph{Implicit-JSCC}, an implicit equilibrium framework in which semantic encoding and decoding are formulated as fixed-point equilibrium processes. The effective encoder and decoder depths are determined by residual-based solver convergence rather than manually predefined layer numbers, while parameter sharing across equilibrium iterations enables depth-independent parameter complexity. To analyze the resulting effective-depth behavior, we develop a Gaussian-process-inspired kernel evolution framework that models equilibrium iterations as an effective-depth propagation process. Since channel noise is injected between the encoder and decoder, the analysis tracks channel-induced representation perturbations across receiver-side equilibrium iterations and derives a theory-guided depth--SNR relationship. After offline calibration of the system-specific parameters, the resulting model characterizes the required receiver-side refinement depth under different SNRs. Extensive experiments show that Implicit-JSCC achieves competitive reconstruction performance while enabling residual-based adaptive inference and controllable computation--quality tradeoffs. The depth--SNR model further provides a characterization of the SNR-dependent refinement depth required to reach a prescribed perturbation tolerance.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：在深度联合源信道编码（Deep JSCC）中，给定一个信道信噪比（SNR），用于语义重建的有效计算深度应该是多少？
- **研究动机**：现有Deep JSCC系统通常采用固定深度的神经网络架构（如固定层数的编解码器），其深度通过经验性超参数调优确定。这导致两个问题：
    - 在高SNR（信道条件好）时，固定深度可能引入冗余计算，徒增推理开销。
    - 在低SNR（信道噪声严重）时，固定深度可能不足以抑制信道扰动，导致精化不足。
- **背景**：随着6G发展，通信范式从比特级向语义级转变，Deep JSCC成为重要框架。然而，缺乏理论指导如何根据信道条件自适应地调整计算深度。

### 2. 论文提出的方法论
- **核心思想**：提出一个名为“Implicit-JSCC”的隐式均衡框架。该框架将语义编码和解码过程建模为**不动点（Fixed Point）均衡过程**，而非传统的显式多层神经网络。
- **关键技术细节**：
    - **架构设计**：
        - **隐式编码器**：给定输入特征 $h_{enc}$ 和SNR嵌入 $e_{snr}$，求解不动点 $z_{enc}^* = f_{enc}(z_{enc}^*, h_{enc}, e_{snr}; \psi_{enc})$。
        - **隐式解码器**：给定接收特征 $h_{dec}$ 和 $e_{snr}$，求解不动点 $z_{dec}^* = f_{dec}(z_{dec}^*, h_{dec}, e_{snr}; \psi_{dec})$。
        - 生成信道符号和重建图像：$x = Head_{enc}(z^*_{enc})$， $\hat{s} = Head_{dec}(z^*_{dec})$。
    - **隐式更新与自适应求解器**：
        - **更新函数**：$f_q(z_q) = (1-\alpha_q)z_q + \alpha_q[u_q + \Delta_q(z_q, u_q, e_{snr})]$，这是一个带阻尼的插值迭代。
        - **收缩条件**：当内部更新$\Delta_q$的Lipschitz常数$L_g<1$时，$f_q$是收缩映射，保证不动点存在且唯一（**命题1**）。
        - **自适应深度**：使用**残差求解器**（Algorithm 1: EqSolve）进行不动点迭代。算法监控混合绝对-相对残差$r^{(k)}$，当残差低于预设阈值$\tau_q$时停止，所执行的迭代次数$K_q$即为“有效深度”。这实现了深度由求解器收敛性自动决定，而非人工预设。
    - **参数共享**：所有均衡迭代共享相同的网络参数$\psi_q$，因此参数复杂度与有效深度无关。
    - **训练**：对编码器和解码器进行联合端到端训练。除了MSE重建损失，还加入了**雅可比正则化项**$L_{Jac}$来抑制不稳定放大，促进固定点收敛。梯度通过隐式微分（Implicit Differentiation）计算，避免了存储所有前向迭代的中间结果，节省了内存。
- **理论分析**：
    - **核演化分析**：基于神经网络高斯过程（NNGP）理论，将均衡迭代建模为核（Kernel）的传播过程。
    - **扰动与收敛**（**定理1**）：在局部线性化假设下，解码器端由信道噪声引起的核偏差$||K^{(k)}-K^*||$随迭代次数$k$呈几何级衰减：$||K^{(k)}-K^*|| \le \beta^k \Delta_0$，其中$\Delta_0$是初始扰动，$\beta<1$是有效传播因子。
    - **深度-SNR关系**（**推论1**）：为了达到目标容差$\epsilon_{tol}$，所需的理论深度为 $K_{pred}^{dec} = \frac{\log(\Delta_0/\epsilon_{tol})}{\log(1/\beta)}$。结合噪声扰动模型 $\Delta_0 = c_0 + c_1 10^{-SNR_{dB}/10}$，得出连续深度预测公式。
    - **离线校准**：利用验证集上的测量深度，校准系统的特定参数（$\beta$，$\hat{\eta}=c_1/c_0$），以获得可用于指导的理论深度-SNR曲线。

### 3. 实验设计
- **数据集**：
    - **训练集**：DIV2K（随机裁剪为256x256图像块）。
    - **测试/评估集**：Kodak24。
- **信道场景**：AWGN， Rayleigh衰落， Rician衰落（Rician因子$\kappa=3$）。
    - **注意**：理论分析主要针对AWGN，但实验验证了在更复杂衰落信道下的通用性。
- **对比方法**：包括两类，所有方法在相同带宽比 $R=1/12$ 下比较。
    - **学习型JSCC基线**：
        - DeepJSCC（代表性CNN基线）
        - SwinJSCC（Swin Transformer基线）
        - ADJSCC（注意力机制自适应基线）
    - **传统分离编码基线**（通过穷举比特级仿真得到的最佳解码性能包络）：
        - BPG + LDPC + AMC
        - JPEG + LDPC + AMC

### 4. 资源与算力
- **文中信息**：
    - 训练使用**单个NVIDIA GeForce RTX 5080 GPU**。
    - 训练了**180个epochs**，学习率从$3\times10^{-5}$降至$5\times10^{-7}$，批大小为8。
    - 采用25轮的SNR课程学习策略。
- **缺失信息**：未明确说明训练单次实验的总时长。也未提及对比方法（如SwinJSCC、ADJSCC）的训练所需资源。

### 5. 实验数量与充分性
- **实验组数**：较为充分。
    - **主对比实验**：在AWGN、Rayleigh、Rician三种信道下进行了PSNR和SSIM性能比较（图3, 4）。
    - **自适应深度控制实验**：使用不同停止阈值$\tau_{dec}$探究计算与质量权衡（图5）。
    - **理论模型验证实验**：验证学院推导的深度-SNR预测曲线与测量值吻合（图6-左），并用固定深度推理验证预测深度的有效性（图6-右）。
    - **固定深度消融实验**：使用不同的固定解码器深度进行比较，证明自适应深度的优势（图7）。
    - **收敛性诊断实验**：展示了不同SNR下的残差轨迹和残差比率（图8）。
    - **复杂度与资源分析**：对比了不同方法的可训练参数量、峰值内存和执行深度（表III）。
    - **可视化比较**：在10dB SNR下展示了多张图像的视觉效果（图9）。
- **公平性**：
    - 所有对比方法都在相同的图像尺寸和带宽比下进行。
    - 对比了参数数量、峰值内存等资源消耗指标。
    - 分离式基线通过穷举法找到最优解码点，而非简单的固定策略，这使得比较对神经网络方法更为公平。
    - **风险**：对传统方法采用最优包络进行比较，在实际在线运行中可能无法达到此性能。对Implicit-JSCC的自适应推理，文中使用了默认停止阈值，不同阈值会显著影响结果（正如图5所示）。

### 6. 论文的主要结论与发现
- Implicit-JSCC在所有信道模型（AWGN、Rayleigh、Rician）下都能实现稳定的重建性能，并将适应性扩展到了非AWGN场景。
- 与传统分离式方案（BPG+LDPC+AMC等）相比，所有JSCC方法都展现出“优雅降级”，Implicit-JSCC也不例外。
- 与显式神经网络基线（SwinJSCC、ADJSCC等）相比，Implicit-JSCC能用**更少的参数**和**适中的计算量**达到竞争性或更优的PSNR/SSIM。
- **自适应深度**是有效的：
    - 固定深度过浅（如$K=6$）会导致精化不足，在较好信道条件下性能下降明显。
    - 固定深度过深（如$K=20$）会产生冗余计算，性能不升反降。
    - 由残差终止条件确定的有效深度能更好地适应不同信道条件。
- **深度-SNR模型**具有指导意义：
    - 校准后的理论模型预测的深度与测量到的实际自适应深度高度吻合。
    - 预测深度落在固定深度性能曲线的近饱和区域，证明其能有效指导深度选择。

### 7. 优点
- **方法论新颖**：将Deep JSCC与深度均衡模型（DEQ）结合，为自适应计算深度（推理时动态确定网络有效层数）提供了新思路。
- **理论与实践的紧密结合**：不仅提出了工程架构，还基于NNGP理论推导了深度-SNR关系的数学模型，解释了为什么低SNR需要更多迭代。
- **高效的资源利用**：通过参数共享，模型参数复杂度不随有效深度增加而增加。通过隐式微分，反向传播不需要存储所有中间状态，节省了训练内存。
- **良好的可控性**：通过调整停止阈值，可以实现对推理质量与计算复杂度的灵活权衡。

### 8. 不足与局限
- **理论假设的局限性**：
    - 理论分析（核演化与定理1）依赖于“无限宽”、“局部线性化”等强假设，这与实际的有限宽、非线性网络存在偏差。
    - MSE损失与核空间收敛之间的严格关系未被证明。
- **实验覆盖不足**：
    - 仅测试了图像传输任务（DIV2K, Kodak24），未涵盖视频、文本或更复杂的任务导向通信。
    - 虽然验证了Rayleigh和Rician信道，但理论分析仅针对AWGN，缺乏对更复杂衰落信道下的理论扩展。
- **校准依赖**：理论模型中的参数（$\beta, \eta$）需要在上线前通过验证集进行离线校准。模型的预测能力对校准数据集的代表性可能有较强依赖。
- **缺乏足够严格的鲁棒性实验**：实验主要关注不同SNR下的“优雅降级”，但没有针对模型对不常见的极端信道（如突发脉冲噪声、非线性失真）的鲁棒性进行测试。
- **缺少实时性比较**：文中虽然给出了参数量和峰值内存，但没有对端到端延迟进行衡量或比较。隐式模型的固定点迭代可能在延迟上不如前馈式显式网络，这是一个重要的实际部署考量。
- **优化开销**：训练时需要加入雅可比正则化，对不动点求解的收敛性有隐性要求，这可能对超参数（如$\lambda_{Jac}$）的选择较为敏感。

（完）
