---
title: Fast Wireless Foundation Models with Early-Exits
title_zh: 具有早期退出功能的快速无线基础模型
authors: "Omar Mashaal, Hatem Abou-Zeid"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29640v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 早期退出高效无线基础模型
tldr: "无线基础模型计算成本高，全深度推理效率低且泛化差。提出早期退出框架，在冻结编码器不同层附加轻量级头，实现可变深度推理。中间层特征加速93%FLOPs，且在未见任务上准确率超全编码器。固定退出策略优于动态路由，表明每任务有最优表征深度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29640v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1158, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29640v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29640v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 769, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29640v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1686, \"height\": 304, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29640v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 913, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29640v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29640v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 723, \"height\": 230, \"label\": \"Table\"}]"
motivation: 无线基础模型全深度推理计算成本高，且对未见分布外任务性能下降，亟需高效推理方法。
method: 在冻结的无线FM编码器上，为每个任务选择最优退出层并附加轻量级头，实现可变深度推理。
result: "该方法减少最多93% FLOPs，且在未见OOD任务上准确率超过全编码器。"
conclusion: 固定退出策略比动态样本路由更有效，表明任务级表征深度选择是关键。
---

## 摘要
虽然无线基础模型（FMs）在实现AI原生6G网络方面展现出强大潜力，但其高计算成本仍是部署的关键障碍。高计算成本源于每个任务都需要刚性、全深度执行FM骨干网络，我们证明这一过程不仅低效，还可能在未见过的分布外（OOD）任务上降低性能。在本文中，我们提出了一种新颖的早期退出FM框架，该框架在冻结的无线FM编码器最合适的退出阶段附加轻量级、每任务头，从而实现针对每个任务首选表示深度的可变深度推理。我们的结果表明，这些中间层特征不仅显著加速推理（FLOPs减少高达93%），而且提供了更具迁移性的表示，在未见过的任务上超过了完整编码器的准确性。我们进一步证明，针对每个任务采用简单的固定退出策略比传统的基于样本感知难度将不同样本路由到不同退出的策略更有效。

## Abstract
While wireless foundation models (FMs) are demonstrating strong potential to enable AI-Native 6G networks, their high computational cost remains a critical barrier to deployment. The large computational cost stems from the rigid, full-depth execution of the FM backbone for every task, a process we show is not only inefficient but can also degrade performance on unseen out-of-distribution (OOD) tasks. In this paper, we propose a novel early-exit FM framework that attaches lightweight, per-task heads, at the most appropriate exit-stage of a frozen wireless FM encoder, enabling variable-depth inference tailored to each task's preferred representation depth. Our results demonstrate that these intermediate-layer features not only speed-up inference significantly (up to 93% fewer FLOPs), but also provide more transferable representations that exceed the full encoder accuracy on unseen tasks. We further demonstrate that a simple fixed-exit strategy per task is more effective than traditional early-exiting policies that route different samples to different exits based on their perceived difficulty levels.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
无线基础模型（WFM）在实现 AI 原生 6G 网络中展现出巨大潜力，但其高计算成本是部署的关键障碍。现有 WFM 通常采用全深度执行编码器进行推理，这一过程不仅计算效率低，而且在未见的分布外（OOD）任务上可能因深层特征过度专化而导致性能下降。作者提出：不同任务可能偏好不同深度的表示，中间层特征可能更通用且更利于 OOD 泛化。因此，本文的核心问题是如何在保持 WFM 能力的同时，通过早期退出机制实现低延迟、高效率的推理，并提升 OOD 性能。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：在冻结的无线 FM 编码器的多个中间层（Stage 2,3,4）附加轻量级、任务特定的分类头，允许每个任务选择最合适的退出层（fixed-exit），从而实现可变深度推理。
- **关键技术细节**：
  - 编码器：采用预训练的 IQFM 骨干（ShuffleNetV2-x0.5），冻结参数，分解为 K 个顺序块。
  - 在每块后设置退出点：对中间特征图 Z_k 进行全局平均池化（GAP），然后通过线性层得到 logits 和 softmax 概率。
  - 推理策略：对比两种路由方式：
    - **传统早期退出（动态路由）**：按样本置信度（熵阈值）决定是否提前退出，如 Algorithm 2。
    - **固定退出策略（Proposed）**：离线为每个任务选择一个最优退出深度（k*_t），所有样本都执行到该深度，如 Algorithm 3。
  - 最优退出选择：支持两种低标签需求的方法——无标签的平均置信度/熵选择、小样本验证集选择（few-shot validation）。
- **算法流程**（文字说明）：
  - Algorithm 1：训练所有退出头的流程。对于每个任务 t 和每个退出深度 k，独立训练线性头（冻结编码器），使用交叉熵损失。
  - Algorithm 2：动态早期退出推理。依次经过块，计算预测熵，若小于阈值则提前返回，否则继续到最后一层。
  - Algorithm 3：固定退出选择与推理。先离线：若有小样本集，选择验证准确率最高的 k；若无，选平均置信度最高的 k（或等价地，最小熵）。然后固定该深度推理。

### 3. 实验设计：数据集 / 场景、基准、对比方法
- **数据集**：
  - **同分布（ID）任务**：基于 IQFM 测试平台的波达角（AoA）和调制分类（Mod.）。
  - **分布外（OOD）任务**：
    - DeepBeam（毫米波波束预测，5 类）
    - RML2016.10a（调制识别，11 类，SNR -20~+18 dB）
    - POWDER（RF 指纹识别，4 类）
    - OWL-INT（干扰分类，15 类）
- **基准**：传统全编码器（Full）最终层输出作为基线。
- **对比方法**：
  - 不同退出深度（Stage 2/3/4）与 Full 的准确率。
  - 动态路由策略：共享熵阈值（shared-threshold）、逐出口调优的贪婪阈值（Greedy）、帕累托阈值选择（Pareto）。
  - 固定退出选择方法：无标签的置信度/熵、小样本验证（20 样本/类）。
- **实验设置**：每个任务/出口用 500 样本/类训练线性头 100 个 epoch，优化器 AdamW，使用 NVIDIA GeForce RTX 3080 Ti GPU。

### 4. 资源与算力
- 文中明确提到所有实验使用 **NVIDIA GeForce RTX 3080 Ti GPU**。
- 未给出详细的训练时长（如每个任务多少小时），也未说明使用的 GPU 数量（推测为单卡）。
- **不足**：算力资源描述较简略，缺乏具体时间开销数据，不利于复现或评估实际计算需求。

### 5. 实验数量与充分性
- **实验数量**：6 个任务（2 ID + 4 OOD），每个任务测试了 3 个退出深度和 Full 共 4 个配置；对比了 3 种动态路由策略；对比了 3 种退出选择方法（置信度、熵、小样本）。总计约 6×4=24 个主要准确率结果，加上多组动态路由对比和热图分析。
- **充分性评估**：
  - 实验覆盖了多种典型无线任务（调制识别、波束预测、指纹识别、干扰分类），且包含同分布和分布外场景，覆盖面较广。
  - 对比了经典 EE 动态策略，并分析了为何固定退出更优（热图显示样本主要集中在单一出口）。
  - **不足之处**：缺乏对更多骨干网络（如 ResNet、Transformer）的验证；仅使用单一 WFM（IQFM），泛化性需进一步验证；退出选择方法中，无标签方法效果不佳，小样本方法稳定但未深入探讨样本数量敏感度；未进行更详细的消融实验（如不同头容量、不同池化方式）。

### 6. 论文的主要结论与发现
- 在冻结的 WFM 上，中间层出口在 OOD 任务上准确率超过全编码器（最高提升 8.0 pp），且计算量减少达 93%（Stage 2）。
- 对于 ID 任务，全编码器仍最优，但中间层出口准确率下降极小（<0.34 pp）。
- 固定退出策略（即每任务选择一个固定深度）优于传统的动态样本路由（样本级置信度路由），因为多数任务中样本倾向于集中在一个中间出口，动态路由额外开销大且无明显收益。
- 小样本验证（20 样本/类）可可靠地选出最优出口；无标签的置信度/熵方法则不可靠。

### 7. 优点：方法或实验设计上的亮点
- **创新性**：首次将早期退出引入无线基础模型，并提出“每任务固定退出”的新观点，打破传统准确率-延迟的 trade-off。
- **实用性**：冻结主干、轻量头训练，适应少样本场景；提出无标签/小样本的退出选择方法，降低部署成本。
- **实验全面**：覆盖 6 个不同任务、多种 OOD 环境，并与多种动态策略对比，结论有说服力。
- **分析深入**：通过 Calinski-Harabasz 分数解释中间层特征的可迁移性，解释为何 OOD 任务受益于浅层特征。

### 8. 不足与局限
- **骨干网络单一**：仅使用 ShuffleNetV2-x0.5 一种架构，未验证更深的或 Transformer 类 WFM。
- **数据集限制**：ID 任务仅来自内部测试台，OOD 数据集虽多样但规模较小（部分仅 5-15 类），真实复杂场景下效果有待验证。
- **退出选择方法**：无标签方法不可靠，小样本方法虽好但需要一定量的标签，未探索完全无监督或自监督的出口选择。
- **动态路由评估**：文中的动态路由策略（Greedy、Pareto）似乎未进行精细调参，可能未能展现其全部潜力（作者提到阈值调优空间有限）。
- **缺少对时延的详细分析**：表 II 给出平均推理时间（us），但未考虑不同设备上的实际延迟开销（如边缘设备）。
- **未考虑后部署变化**：论文仅讨论离线选择，未涉及部署后信道或硬件变化对退出性能的影响（作者列为未来工作）。

（完）
