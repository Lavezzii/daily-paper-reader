---
title: Semantic Communications in the THz Band
title_zh: 太赫兹频段中的语义通信
authors: "Fatima Ismail, Hadi Sarieddeen, Jihad Fahs"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07455v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 基于深度学习的太赫兹信道语义通信及轻量神经检测器
tldr: "太赫兹通信面临严重信道损伤，传统方法依赖信道估计且鲁棒性不足。本文提出基于深度学习的语义通信系统，设计无需信道状态信息的轻量级神经检测器，实现数据驱动接收。在0.3 THz频段，该系统在0-12 dB信噪比范围内比传统编码方案BLEU-1得分高50%以上，且所提检测器对高达50 MHz的频偏保持鲁棒。工作验证了语义通信对太赫兹信道损伤的弹性，为未来网络提供高效鲁棒的通信范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07455v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1647, \"height\": 337, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07455v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1625, \"height\": 1363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07455v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1629, \"height\": 1365, \"label\": \"Figure\"}]"
motivation: 太赫兹信道损伤严重且CSI获取困难，传统通信性能受限，需探索鲁棒性更强的语义通信方案。
method: 提出DeepSC系统，设计无需CSI的轻量级神经检测器，在接收端补偿后实现数据驱动接收。
result: "0.3 THz下，DeepSC的BLEU-1比传统编码系统高50%以上；无导频检测器优于MMSE均衡，抗频偏达50 MHz。"
conclusion: 语义通信能有效应对太赫兹信道损伤，无导频检测器提升了系统鲁棒性，适用于未来无线网络。
---

## 摘要
语义通信和太赫兹（THz）频段通信是未来无线网络的算法和频谱使能技术。本文研究了基于深度学习的语义通信（DeepSC）在太赫兹信道上的应用。我们表明，在接收端进行补偿的情况下，仅在加性高斯白噪声下训练的DeepSC模型能够很好地泛化到所测试的太赫兹块衰落和快衰落信道。为了实现完全数据驱动的接收，我们提出了一种轻量级的神经检测器，无需信道状态信息（CSI）。在0.3 THz频率下，DeepSC在0-12 dB信噪比（SNR）范围内优于吞吐量匹配的传统编码通信系统基线，实现了超过50个百分点的更高双语评估替补一元组（BLEU-1）得分。所提出的无导频检测器在完美和不完美CSI情况下均优于最小均方误差（MMSE）均衡，并且对高达50 MHz的频率偏移保持鲁棒性，突显了语义通信对太赫兹信道损伤的韧性。

## Abstract
Semantic and terahertz (THz)-band communications are algorithmic and spectral enablers of future wireless networks. This work investigates deep learning-based semantic communication (DeepSC) over THz channels. We show that DeepSC models trained solely under additive white Gaussian noise generalize well to the tested THz block- and fast-fading channels when receiver-side compensation is applied. To enable fully data-driven reception, we propose a lightweight neural detector that does not require channel state information (CSI). At 0.3 THz, DeepSC outperforms a throughput-matched traditional coded communication system baseline over 0-12 dB signal-to-noise ratio (SNR), achieving more than 50 percentage-point higher Bilingual Evaluation Understudy unigram (BLEU-1) score. The proposed pilot-free detector outperforms minimum mean square error (MMSE) equalization with both perfect and imperfect CSI and remains robust to frequency offsets up to 50 MHz, highlighting the resilience of semantic communication to THz channel impairments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：太赫兹（THz）频段通信面临严重的路径损耗、频率选择性衰落和分子吸收，导致信道状态信息（CSI）获取困难且不可靠，尤其在低信噪比和移动场景下。传统通信系统依赖精确CSI，性能受限。语义通信通过传输含义而非原始比特，有望在低SNR下提供鲁棒性，但现有深度语义通信（DeepSC）模型大多在简单信道（如AWGN、Rayleigh）下评估，未在真实的THz信道环境中验证。
- **整体含义**：本文旨在探索DeepSC在THz信道下的行为，并设计一种无需CSI的、完全数据驱动的接收方案，以克服THz信道损伤，提升未来无线网络的通信效率与鲁棒性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：利用两阶段训练策略。第一阶段在AWGN信道下端到端预训练DeepSC编码器-解码器（冻结后用于推理）；第二阶段冻结DeepSC，在THz信道上训练一个轻量级神经检测器（Neural Detector, ND），直接学习将从THz信道接收到的受损符号映射回干净符号，无需CSI或导频。
- **关键技术细节**：
  - **系统模型**：发射端由语义编码器 \(S_\beta\) 和信道编码器 \(C_\alpha\) 组成，输出连续符号向量 \(\mathbf{x}\)。接收端信号 \(\mathbf{y}=h\mathbf{x}+\mathbf{n}\)，其中 \(h\) 为波束成形后的有效标量信道，\(\mathbf{n}\) 为AWGN。
  - **ND架构**：输入为 \([B, M, 2]\) 的实部/虚部张量；使用1D卷积（核3，padding1）投影到 \(d_{\text{model}}=64\)；添加正弦位置编码；两层Transformer编码器（8头注意力，FFN维度256）捕获句内全局依赖；最后1D卷积映射回2维输出。参数量仅100,802。
  - **训练算法（Algorithm 1）**：
    1. 阶段1：在AWGN信道上端到端训练DeepSC，最小化联合损失 \(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{CE}} - \lambda \mathcal{L}_{\text{MI}}\)，训练约75个epoch，SNR 5-10 dB，之后冻结所有参数。
    2. 阶段2：在THz信道上训练ND，输入\(\mathbf{y}\)，目标为\(\mathbf{x}\)，最小化MSE损失 \(\|\hat{\mathbf{x}}-\mathbf{x}\|^2\)，使用Adam优化器，初始学习率 \(10^{-4}\)，余弦退火，80 epochs。
- **公式**：MMSE均衡后的符号近似为 \(\hat{x}_{\text{MMSE}} \approx \alpha x + \tilde{n}\)（\(\tilde{n}\) 仍为白高斯），这解释了AWGN训练可泛化至衰落信道的原因。对于不完美CSI，\(\hat{x} \approx (1 - \Delta h/h)x + n/h\)，引入乘性失真。

### 3. 实验设计：数据集、场景、基准与对比方法
- **数据集**：Europarl语料库（80k训练句，10k测试句），文本语义任务。
- **信道场景**：
  - 室内THz场景，0.3 THz，带宽10 GHz，使用TeraMIMO仿真器生成THz MIMO信道（Saleh-Valenzuela模型，HITRAN分子吸收）。波束成形后等效为SISO信道。
  - 测试多种衰落：块衰落（每batch更新）、快衰落（每句子/每词更新）；AWGN、Rayleigh、Rician、THz（有无分子吸收）。
  - 附加噪声类型：高斯、拉普拉斯、t分布。
  - 频偏：±50 MHz。
  - CSI不完美：\(\alpha = 0.1, 0.5\) 的估计误差。
- **基准方法**：
  - 传统编码系统：ASCII编码、LDPC（码率4/5）、64-QAM调制，MMSE均衡，吞吐量匹配4.89 bits/符号。
  - 消融变体：仅卷积ND（移除Transformer层）、两层MLP（128神经元/层）。
  - MMSE均衡（完美CSI、基于导频的估计CSI、不完美CSI）。
- **对比方法**：图2(a,b)比较AWGN训练 vs 同信道训练；图3(a)比较DeepSC+ND vs DeepSC+MMSE vs 传统+MMSE；图3(b,c)比较ND与MMSE在频偏、不完美CSI下的表现。

### 4. 资源与算力
- 文中仅提及推理阶段在单张NVIDIA V100 GPU（32 GB，v100d32q）上进行。训练时长未明确给出，但指出阶段1约75 epochs，阶段2约80 epochs，使用Adam和余弦退火。未报告GPU数量、总训练耗时等具体算力信息。

### 5. 实验数量与充分性
- **实验数量**：共两组主图（Fig.2含3个子图，Fig.3含3个子图），覆盖了：
  - 不同信道模型（AWGN、Rayleigh、Rician、THz、THz+吸收）下的泛化实验。
  - 不同噪声分布（高斯、拉普拉斯、t分布）的敏感性实验。
  - 不同衰落更新率（每batch/每句子/每词）的影响。
  - 与多种ND变体、MMSE变体、传统系统的对比。
  - 频偏鲁棒性（±50 MHz）和CSI不完美鲁棒性。
- **充分性**：实验设计较全面，覆盖了关键因素（信道类型、噪声特性、快慢衰落、频偏、CSI质量）。统计上报告了最大样本标准差（1.3e-2 BLEU-1）和95%置信区间半宽（<5e-3 BLEU-1），说明结果稳定。但仅限于单天线等效信道、文本任务，未扩展至宽频MIMO或图像/语音等模态。
- **公平性**：传统系统吞吐量匹配（4.8 vs 4.89 bits/符号），基准合理。消融实验验证了ND各组件贡献。

### 6. 论文的主要结论与发现
- AWGN训练的DeepSC在接收端有效补偿（如MMSE均衡）后，可泛化至THz块衰落和快衰落信道，无需信道特定重训练。
- 所提无导频神经检测器（ND）在0.3 THz下，0-12 dB SNR内优于完美CSI-MMSE，且无需任何CSI；对高达50 MHz频偏保持鲁棒。
- 语义通信对CSI误差的容忍度高于传统LDPC编码系统，因为连续高维表示对乘性失真更鲁棒。
- 在低SNR时，DeepSC+ND比吞吐量匹配的传统系统BLEU-1得分高50个百分点以上。

### 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次提出无导频神经检测器用于文本语义通信的THz信道均衡，避免了CSI估计和导频开销。
- **轻量高效**：ND仅100k参数，推理速度与MMSE相当，且避免了CSI估计的额外计算。
- **泛化性强**：AWGN训练即可应对多种THz衰落场景，简化了训练流程。
- **鲁棒性验证充分**：系统考察了频偏、CSI误差、快衰落、噪声分布等实际损伤，结果全面。
- **公平对比**：与传统系统进行吞吐量匹配，消融实验拆解ND组件贡献。

### 8. 不足与局限
- **实验范围局限**：仅考虑单天线等效信道（波束成形后），未验证宽频MIMO场景；仅测试文本语义任务，未扩展至图像、语音等模态。
- **模型假设**：ND训练需要监督信号（发送的干净符号），在实际中可能需要导频或反馈，与“完全无导频”略有矛盾（但训练后推理无导频）。
- **信道模型仿真**：基于TeraMIMO仿真，未在实际THz信道测量数据上验证，实际效果有待检验。
- **未讨论复杂度**：虽然参数少，但Transformer层推理延迟可能高于简单均衡器，文中仅报告端到端推理时间（约311.7s/10k句），未分析实时性。
- **训练细节缺失**：未报告实际训练时长、GPU数量、超参数调优过程，可复现性不足。
- **不完美CSI对比**：仅考虑MMSE受CSI误差影响，未将ND与基于深度学习但需要CSI的检测器（如MMNet、OAMP-Net）对比，因为后者设计用于离散星座，不适用于连续嵌入。

（完）
