---
title: "AdaKAN: A dual-branch adaptive Kolmogorov-Arnold network for medical image segmentation"
title_zh: "AdaKAN: 用于医学图像分割的双分支自适应Kolmogorov-Arnold网络"
authors: "Dalia Alzu'bi, Deep Bhattacharyya, Ali Ayub, A. Ben Hamza"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22891v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 提出AdaKAN双分支自适应Kolmogorov-Arnold网络用于医学图像分割
tldr: 医学图像分割因解剖结构复杂与模态差异大而具挑战。本文提出AdaKAN，融合卷积与新型高效KAN（EffiKAN）的双分支自适应网络。其一分支采用Bernstein多项式激活的KAN层实现全局平滑逼近，另一分支进行通道细化。U形架构配合跳跃连接，在多个医学影像数据集上达到分割精度最优，兼顾长程依赖与局部细节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22891v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1527, \"height\": 1350}, {\"url\": \"assets/figures/arxiv/2607.22891v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1606, \"height\": 940}, {\"url\": \"assets/figures/arxiv/2607.22891v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 926, \"height\": 665}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22891v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1707, \"height\": 439}, {\"url\": \"assets/tables/arxiv/2607.22891v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 356}, {\"url\": \"assets/tables/arxiv/2607.22891v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 835, \"height\": 339}, {\"url\": \"assets/tables/arxiv/2607.22891v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 840, \"height\": 315}, {\"url\": \"assets/tables/arxiv/2607.22891v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 827, \"height\": 237}]"
motivation: 现有卷积和Transformer模型难以同时捕获医学图像中的长程依赖和精细局部特征，且分割精度受限于结构复杂性和模态差异。
method: AdaKAN采用U形架构，双分支AdaptKAN模块包含Bernstein多项式KAN层和通道细化分支，融入高效注意力机制，并通过跳跃连接保留空间细节。
result: 在多种医学影像数据集上，AdaKAN的分割精度超越现有方法，达到当前最优性能。
conclusion: 融合KAN与卷积的自适应双分支网络能有效提升医学图像分割效果，验证了该方法的泛化能力与实用性。
---

## 摘要
医学图像分割是计算机辅助诊断中的一项基础任务，但由于解剖结构的复杂性和不同成像模态的变异性，该任务仍然具有挑战性。在本文中，我们提出了AdaKAN，一种自适应Kolmogorov-Arnold网络（KAN），它协同集成了卷积操作与一种新颖的高效KAN（EffiKAN）模块，该模块由高效注意力机制和自适应KAN（AdaptKAN）模块组成。该模块采用双分支设计：一个分支使用具有Bernstein多项式激活函数的KAN层进行全局平滑且稳定的函数逼近，而另一个分支通过投影操作和自适应缩放执行通道级细化。AdaKAN采用U形架构，有效捕捉长程依赖关系和细粒度局部特征，克服了传统卷积和基于Transformer的分割模型的局限性。跳跃连接用于在编码过程中保留空间细节，并在解码过程中促进精确重建。在多种医学成像数据集上进行的大量实验表明，AdaKAN在分割精度上达到了最先进的性能。

## Abstract
Medical image segmentation is a fundamental task in computer-aided diagnosis, yet it remains challenging due to the complexity of anatomical structures and the variability across imaging modalities. In this paper, we propose AdaKAN, an Adaptive Kolmogorov-Arnold Network (KAN) that synergistically integrates convolutional operations with a novel efficient KAN (EffiKAN) block, comprised of an efficient attention mechanism and an adaptive KAN (AdaptKAN) module. This module features a dual-branch design: one branch employs a KAN layer with Bernstein polynomial activations for globally smooth and stable function approximation, while the other branch performs channel-wise refinement through projection operations and adaptive scaling. AdaKAN adopts a U-shaped architecture that effectively captures both long-range dependencies and fine-grained local features, overcoming the limitations of conventional convolutional and Transformer-based segmentation models. Skip connections are employed to preserve spatial details during encoding and facilitate accurate reconstruction during decoding. Extensive experiments conducted on diverse medical imaging datasets demonstrate that AdaKAN achieves state-of-the-art performance in segmentation accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：医学图像分割中，解剖结构的复杂性（如边界模糊、形态多变）和不同成像模态（超声、CT、MRI等）的差异性，使得精确分割极具挑战。
- **现有方法局限**：
  - **基于FCN（如U-Net）的模型**：受限于卷积的局部感受野，难以捕获长程空间依赖，导致分割边界模糊或不完整。
  - **基于Transformer的模型**：通过自注意力机制建模全局依赖，但计算复杂度呈二次增长（$O(n^2)$）。
  - **基于MLP的轻量模型**：计算效率高但表达容量有限，难以处理复杂结构。
  - **已有KAN方法（如U-KAN）**：引入B样条激活函数，但缺乏全局上下文建模能力。
- **研究动机**：急需一种能同时兼顾**局部精度**、**全局感知**和**计算效率**的平衡框架。
- **整体含义**：本文提出AdaKAN，一种将卷积操作与新型KAN模块（EffiKAN）相结合的自适应分割网络，在多个医学影像数据集上达到当前最优（SOTA）性能，实现了高效且准确的分割。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：采用对称的U形编码器-解码器架构，通过**卷积阶段**提取多尺度低层语义，再通过**EffiKAN阶段**融合高效注意力和自适应KAN模块，捕获全局依赖并细化局部特征。
- **关键技术细节**：
  1. **总体架构**：
     - 输入图像 $I \in \mathbb{R}^{H \times W \times 3}$。
     - **编码器**：三个卷积块（Conv+BN+ReLU+2×2最大池化）逐步下采样至 $H/8 \times W/8 \times C_3$，随后经两个EffiKAN块进一步处理。
     - **瓶颈**：连接编码器与解码器，实现深度特征学习。
     - **解码器**：对称结构，使用双线性插值上采样，并通过跳跃连接融合编码器特征，最后经1×1卷积和softmax生成分割掩码。
  2. **EffiKAN块**：包含四个子模块：
     - **Patch Embedding**：将特征图划分为非重叠块并线性投影为token序列 $Y \in \mathbb{R}^{n \times d}$。
     - **Layer Normalization (LN)**：稳定特征分布。
     - **Efficient Attention**：通过行/列softmax重新排列乘法顺序，实现线性复杂度 $O(n)$，相比标准自注意力 $O(n^2)$ 大幅降低：
       $$\text{EA}(\text{LN}(Y)) = \sigma_{\text{row}}(Q) \big( \sigma_{\text{col}}(K)^\top V \big)$$
       其中 $Q, K, V$ 为线性投影得到，$\sigma_{\text{row}}, \sigma_{\text{col}}$ 分别为行/列softmax。残差连接得到 $Z$。
     - **AdaptKAN模块**：双分支结构：
       - **左分支**：LN + KAN层，激活函数为Bernstein多项式与SiLU的加权和：
         $$\tilde{\phi}(z) = \tilde{w}_b \text{SiLU}(z) + \tilde{w}_s \text{Bern}(z)$$
         其中 $\text{Bern}(z) = \sum_{r=0}^R \theta_r \binom{R}{r} z^r (1-z)^{R-r}$，$R$ 为多项式阶数（实验中取3）。
       - **右分支**：通道细化，通过降维投影（$W_{\text{down}}$）+ ReLU + 升维投影（$W_{\text{up}}$），结果乘以可学习缩放系数 $s$（初始化为0.1）。
       - **输出**：两分支求和，再加残差连接：
         $$\text{AdaptKAN}(Z) = \text{KAN}(\text{LN}(Z)) + s \cdot \tilde{Z}, \quad \tilde{Z} = \text{ReLU}(Z W_{\text{down}}) W_{\text{up}}$$
  3. **损失函数**：二分类任务使用BCE+Dice平均损失；多类ACDC使用交叉熵+Dice平均损失。

### 3. 实验设计：数据集、基准、对比方法

- **数据集（5个）**：
  - **BUSI**（乳腺超声图像，二分类）
  - **GlaS**（结肠组织学腺体分割，二分类）
  - **CVC**（结肠镜息肉分割，二分类）
  - **ISIC 2018**（皮肤病变分割，二分类）
  - **ACDC**（心脏MRI多结构分割，三类：RV、Myo、LV）
- **评估指标**：IoU（交并比）和Dice系数（DSC）。
- **对比方法（6种基线 + 2种KAN变体）**：
  - Att-UNet, MedT, UNeXt, Rolling-Unet, U-KAN, PDS-UKAN, ResU-KAN, 以及针对ISIC额外对比SET、U-Net V2；针对ACDC对比QMaxViT-UNet+, ScribFormer, SelfRegularized-UNet, PVT-EMCAD-B2。
- **数据划分**：每个数据集80%训练，20%验证。所有结果取5次不同随机种子的均值和标准差。

### 4. 资源与算力

- **硬件**：单块NVIDIA A4500 GPU。
- **框架**：PyTorch。
- **训练配置**：
  - 批量大小：BUSI、CVC、ISIC为8；GlaS为6；ACDC为16。
  - 图像尺寸：BUSI、CVC、ISIC、ACDC为256×256；GlaS为512×512。
  - 学习率：初始 $10^{-4}$，余弦退火衰减至 $10^{-5}$。
  - 优化器：Adam。
  - 训练轮数：400 epochs。
- **模型参数量与FLOPs**：AdaKAN为14.21M参数、8.12G FLOPs（见表1）。
- **说明**：论文未明确给出总训练时间或GPU小时数，但提供了上述实现细节。

### 5. 实验数量与充分性

- **实验组数**：
  - **定量对比**：5个数据集上分别与6~8种方法对比，结果均以均值±标准差报告。
  - **定性对比**：在BUSI数据集上给出了4个样本的可视化热图对比。
  - **消融实验**：在BUSI上对高效注意力、AdaptKAN模块（下/上投影）的去除进行4组消融（见表4）。
  - **参数分析**：Bernstein多项式阶数 $R$ 从2到5的影响（表5）。
  - **效率分析**：展示了DSC、FLOPs、参数量散点图（图3）。
- **充分性评价**：
  - **充分**：覆盖了不同模态（超声、组织学、内镜、皮肤镜、心脏MRI）、不同任务（二分类/多分类），与主流SOTA方法公平比较。消融实验验证了各组件贡献。参数分析提供了设计依据。
  - **客观**：多次随机种子统计，结果可靠。代码已开源（GitHub），可复现。
  - **公平**：对比方法均采用原文推荐设置或默认配置，Efficient Attention的线性复杂度论证充分。

### 6. 论文的主要结论与发现

- **性能优势**：AdaKAN在所有五个数据集上均达到或超过SOTA：
  - BUSI：IoU 68.84%，DSC 80.95%（比ResU-KAN分别高1.1%和1.03%）。
  - GlaS：IoU 88.58%，DSC 93.94%（与PDS-UKAN持平并略优）。
  - CVC：IoU 85.73%，DSC 92.26%（优于ResU-KAN）。
  - ISIC：IoU 84.88%，DSC 91.71%（优于SET和U-Net V2）。
  - ACDC：平均DSC 93.00%（比PVT-EMCAD-B2高0.88%），尤其在RV结构上提升显著。
- **效率平衡**：在参数量（14.21M）和计算量（8.12G FLOPs）均适中的前提下取得最优DSC，效率优于ResU-KAN（20.06M / 8.57G）和Att-UNet（50.27M / 60.06G）。
- **组件贡献**：高效注意力和AdaptKAN模块均不可或缺，去除任一模块导致IoU/DSC显著下降（降幅达4.75%/3.44%）。
- **多项式阶数**：Bernstein多项式阶数 $R=3$ 在表达能力和稳定性间取得最佳平衡。

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **双分支AdaptKAN**：左分支利用Bernstein多项式实现全局平滑函数逼近，右分支通过轻量瓶颈结构进行通道级特征调整，并通过可学习缩放系数 $s$ 稳定融合，兼具表达力与效率。
  - **高效注意力**：将标准自注意力的二次复杂度降至线性，且仍保持全局依赖建模能力（通过 $\sigma_{\text{col}}(K)^\top V$ 的聚合）。
  - **U形混合架构**：卷积阶段保留局部细节，EffiKAN阶段捕获全局上下文，跳跃连接保持空间分辨率。
- **实验设计亮点**：
  - 覆盖5个不同成像模态数据集，验证泛化能力。
  - 提供定性可视化，直观展示边界分割优势。
  - 消融实验和超参数分析全面。
  - 代码开源，便于复现和后续研究。

### 8. 不足与局限

- **实验覆盖方面**：
  - 仅在2D医学图像上验证，未扩展到3D数据（如CT/MRI体积数据），对3D分割的适用性未知。
  - 未在更多公开大型基准（如Synapse、LiTS）上测试，可能存在选择偏差。
- **偏差风险**：
  - 对比方法中，U-KAN和ResU-KAN等KAN变体是近期提出的，可能未做充分调参。论文使用了默认设置，但未说明是否为其最优配置。
  - ACDC数据集上仅对比了4种方法，且部分方法（如SelfRegularized-UNet）在Myo结构上表现更好（89.49% vs 89.09%），AdaKAN并非在所有子结构上都最优。
- **应用限制**：
  - 模型需预定义Bernstein多项式阶数 $R$，该超参数可能依赖数据集特性（如BUSI取3最优，但其他数据集未见分析）。
  - 可学习缩放系数 $s$ 初始化为0.1，未讨论其他初始化策略的影响。
  - 未分析训练/推理时间（或GPU小时数），效率对比主要基于FLOPs和参数量，缺少实际耗时比较。
- **其他**：
  - 消融实验仅针对BUSI一个数据集，未在其他数据集上验证组件贡献的通用性。
  - 论文未讨论对噪声或伪影的鲁棒性，可视化虽显示抗噪能力，但缺乏定量指标。

（完）
