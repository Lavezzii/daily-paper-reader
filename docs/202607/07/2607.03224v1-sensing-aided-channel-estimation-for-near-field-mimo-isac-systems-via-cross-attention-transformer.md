---
title: Sensing-Aided Channel Estimation for Near-Field MIMO ISAC Systems via Cross-Attention Transformer
title_zh: 基于交叉注意力Transformer的传感辅助近场MIMO ISAC系统信道估计
authors: "Peihao Dong, Renbin Li, Shen Gao, Shuangshuang Li, Fuhui Zhou, Wei Xu, Qihui Wu"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03224v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 交叉注意力变压器用于近场MIMO ISAC信道估计
tldr: 近场集成感知与通信中，散射体与目标部分重叠，感知信息可辅助信道估计。提出CAT-CENet，利用交叉注意力Transformer融合通信导频与感知模态，自动关注重叠目标特征。Shapley值理论分析证实交叉注意力增益。仿真显示CAT-CENet在高重叠比例下性能显著超越现有方案，且对模型剪枝鲁棒。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03224v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 873, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03224v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1625, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03224v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 858, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03224v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 853, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03224v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 855, \"height\": 591, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03224v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 196, \"label\": \"Table\"}]"
motivation: 近场ISAC中通信散射体与雷达目标重叠，感知信息可增强信道估计，但异构模态融合困难。
method: 提出CAT-CENet，通信导频分支生成Key/Value，感知分支生成Query，通过交叉注意力自动聚焦重叠目标特征。
result: CAT-CENet在高重叠比例下信道估计精度优于现有方案，且对模型剪枝具鲁棒性。
conclusion: 交叉注意力机制有效融合感知与通信模态，提升近场ISAC信道估计性能，并支持模型轻量化。
---

## 摘要
近场集成感知与通信（ISAC）能够在共享频谱和硬件条件下实现高空间分辨率与传输能力。由于通信散射体与雷达目标之间存在部分重叠，感知信息可为增强信道估计提供有价值的先验知识，但融合这两种异构模态仍具挑战。为解决该问题，本文提出一种基于交叉注意力Transformer的信道估计神经网络（CAT-CENet），其包含一个生成键和值特征的通信导频分支，以及一个生成查询特征的感知信息分支。通过精心设计的三模块结构，CAT-CENet能够自动聚焦于重叠目标的特征，而无需预先识别它们。基于Shapley值从理论上分析了模态贡献，验证了CAT-CENet所实现的交叉注意力增益。仿真结果表明，CAT-CENet优于现有方案，尤其在重叠比例较高时表现更佳，并且对模型剪枝具有鲁棒性。

## Abstract
Near-field integrated sensing and communication (ISAC) can deliver the high spatial resolution and transmission capability with the shared spectrum and hardware. Due to the partial overlap between communication scatterers and radar targets, the sensing information can provide valuable priors to enhance the channel estimation while fusing the two heterogeneous modalities remain challenging. To address this problem, a Cross-Attention Transformer based Channel Estimation Neural Network (CAT-CENet) is developed, which includes a communication pilot branch generating the the Key and Value features and a sensing information branch generating the Query feature. By elaborating the three-module structure, CAT-CENet can focus on features of overlapped targets automatically without need of identifying them in advance. The modality contribution is theoretically analyzed based on the Shapley value to verify the cross-attention gain achieved by CAT-CENet. Simulation results show that CAT-CENet outperforms the state-of-the-art schemes, especially with the higher overlapping proportion, and is robust to the model pruning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：近场大规模MIMO（XL-MIMO）与集成感知与通信（ISAC）是未来6G的关键技术。在近场环境下，球面波前导致信道维度剧增、距离依赖性增强，传统远场平面波假设失效。同时，通信散射体与雷达目标往往存在部分重叠，雷达感知信息可作为先验知识辅助信道估计，但通信（导频）与感知（雷达回波）是两种异构模态，融合困难。
- **核心问题**：如何在不预先知道哪些目标重叠的情况下，自动利用雷达感知信息提升近场ISAC系统的信道估计精度？
- **整体含义**：本文提出了基于交叉注意力Transformer的信道估计神经网络（CAT-CENet），通过端到端学习自动聚焦重叠目标特征，实现异构模态有效融合，从而提升信道估计性能。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用交叉注意力机制，将通信导频信息作为Key/Value，雷达感知信息作为Query，让网络自动学习两者之间的关联，从而增强对重叠目标对应特征的提取。
- **网络结构（三模块）**：
  1. **模态预处理模块**：将LS初始信道估计 $\tilde{\mathbf{H}}_c$（实数虚数拆分后reshape为 $\sqrt{M}\times\sqrt{M}\times2$）和K个目标感知响应张量 $\tilde{\mathbf{A}}_s$（reshape为 $\sqrt{M}\times\sqrt{M}\times2K$）分别通过2D卷积嵌入，得到特征图 $\mathbf{H}_1$（通信分支）和 $\mathbf{H}_2$（感知分支）。
  2. **交叉模态融合模块**：包含两个编码器（Encoder1和Encoder2），每个编码器由交叉注意力块和双注意力块串接组成。
     - **交叉注意力块**：感知分支 $\mathbf{H}_2$ 生成Query，通信分支 $\mathbf{H}_1$ 生成Key和Value（公式5-8）。通过多头交叉注意力计算注意力矩阵 $\mathbf{E}_j = \text{Softmax}(\mathbf{Q}_j \mathbf{K}_j^T / \sqrt{F})$，输出 $\mathbf{Z}_j = \mathbf{E}_j \mathbf{V}_j$，然后拼接投影后与残差连接得到 $\mathbf{H}_3$。
     - **双注意力块**：对 $\mathbf{H}_3$ 依次进行特征图自注意力和空间自注意力，进一步融合模态信息。
     - Encoder2以 $\mathbf{H}_6$（Encoder1输出）作为通信输入，$\mathbf{H}_2$ 作为感知输入重复上述操作，输出 $\mathbf{H}_7$。
  3. **卷积后处理模块**：将 $\mathbf{H}_7$ 重新reshape并通过三个卷积层提取噪声张量 $\mathbf{H}_9$，最后通过跨模块残差连接：$\hat{\mathbf{H}}_c = \tilde{\mathbf{H}}_c - \mathbf{H}_9$，得到最终估计。
- **损失函数**：MSE损失（公式9）。
- **复杂度分析**：总FLOPs为28.93M，参数量0.70M，低于对比的MAT-CENet（1.03M参数，37.01M FLOPs）。可进行权重剪枝（PCAT-CENet）进一步减小模型尺寸。
- **模态贡献理论分析**：引入Shapley值量化每个输入张量（通信导频、各目标感知信息）对最终估计的贡献，通过近似公式（11）计算，用于验证交叉注意力增益。

### 3. 实验设计：数据集/场景、基准、对比方法

- **场景**：近场TDD XL-MIMO ISAC系统，BS天线数M=256，ULA排列，波长0.01m，用户单天线，路径数L，目标数K，部分重叠（重叠数X）。
- **数据集**：仿真生成，训练集45,000样本，验证集5,000，测试集2,000。AoA均匀分布，距离10~80m，感知误差：AoA误差标准差10⁻⁴ rad，距离误差标准差10⁻² m。
- **对比方法**：
  - 传统方法：LS、LMMSE、P-SOMP（极域正交匹配追踪）。
  - 深度学习方法：MIA-GAN、SGM（基于分数的生成模型）、MAT-CENet（混合注意力Transformer）。
- **实验变量**：SNR范围-10~15 dB；不同重叠目标数（1/2/3/4/6）；不同路径数（L=K=3和L=K=6）；剪枝比例κ=0.5/0.8。

### 4. 资源与算力

- **未明确说明**：论文未提及GPU型号、数量、训练时长等具体硬件资源。仅提到使用Adam优化器，batch size 128，训练200个epochs。因此无法评估所需算力规模。

### 5. 实验数量与充分性

- **实验数量**：约4组主要实验（Fig.3～Fig.5）：
  - Fig.3：Shapley值贡献可视化（0/1/2/3个重叠目标共4个子图）。
  - Fig.4(a)(b)：NMSE vs SNR，分别对应L=K=3和L=K=6，每个子图包含重叠目标数1/2/3（或1/4/6）共3条CAT-CENet曲线，以及7条baseline曲线。
  - Fig.5(a)(b)：剪枝对比（CAT-CENet vs PCAT-CENet），同样两个场景。
- **充分性评价**：实验设计较全面：
  - 覆盖了不同SNR、不同重叠程度、不同路径数。
  - 包含了传统方法和多种深度学习方法对比。
  - 有消融性的Shapley可视化（虽然不是严格消融，但验证了机制）。
  - 有模型剪枝鲁棒性实验。
  - 不足之处：未进行更复杂场景（如多用户、不同阵列配置、不同误差模型）的实验，也未在实际信道或硬件平台验证。但作为学术论文已足够。

### 6. 论文的主要结论与发现

- CAT-CENet在所有SNR和重叠条件下NMSE均优于所有baseline，尤其在高重叠比例时优势更明显（例如SNR=10 dB时CAT-CENet-1达到10⁻³量级，其他方法仍为10⁻²）。
- 增加重叠目标数可进一步提升性能（CAT-CENet-3优于CAT-CENet-1）。
- Shapley值可视化证实CAT-CENet能够自动识别重叠目标并赋予其高贡献，非重叠目标贡献可忽略，验证了交叉注意力机制的增益。
- 权重剪枝（PCAT-CENet，κ=0.8）可大幅减小模型尺寸（非零参数从0.70M降至0.61M）而性能几乎不变，展示了良好的鲁棒性。

### 7. 优点

- **方法创新**：首次将交叉注意力应用于ISAC异构模态融合，无需预知重叠关系，实现端到端自动聚焦。
- **三模块设计**：预处理、融合、后处理分工明确，结构清晰；跨模块残差连接增强训练稳定性。
- **理论分析**：引入Shapley值量化模态贡献，为交叉注意力增益提供可解释性证据。
- **轻量化潜力**：支持权值剪枝，利于边缘部署。
- **实验对比充分**：与多种传统和深度学习基准对比，结果具说服力。

### 8. 不足与局限

- **实验仅基于仿真**：所有数据均为理想化模型生成，未考虑实际硬件损伤、信道非平稳性、目标运动等复杂因素，真实场景效果未知。
- **假设目标数量K已知**：虽然论文指出K可从目标跟踪获得，但未讨论K估计误差对网络的影响。
- **感知误差模型简单**：仅考虑固定高斯误差，未探索不同误差分布或时变误差的影响。
- **计算资源未报告**：无法复现训练成本。
- **消融研究不够深入**：仅通过Shapley值间接验证交叉注意力，未设计去除交叉注意力或替换为自注意力的消融实验直接对比。
- **应用限制**：仅针对单用户场景，多用户或干扰环境下性能尚待验证。

（完）
