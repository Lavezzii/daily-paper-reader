---
title: Shift-MoE-Based DJSCC for CSI Feedback in Multi-User Pinching-Antenna Systems
title_zh: 基于Shift-MoE的DJSCC在多用户Pinching天线系统中的CSI反馈
authors: "Jian Zou, Yifan Lian, Yongsheng Liang, Fanyang Meng, Wenwu Xie, Liang Yang, Jian Xiao"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04795v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 基于深度联合源信道编码的CSI反馈方案
tldr: 针对频分双工可调天线系统（PASS）中多用户信道状态信息（CSI）反馈的结构相关性和异构性挑战，提出基于Shift-MoE的深度联合源信道编码方案。该方法通过通道分组一步移位操作捕获网格依赖性，并利用门控MLP混合专家模块适应不同用户的CSI统计特性。实验表明，该方案在归一化均方误差上持续优于现有学习基线，且在不同系统参数下保持有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04795v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04795v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04795v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 657, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04795v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 658, \"height\": 519, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04795v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1811, \"height\": 493, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04795v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 834, \"height\": 631, \"label\": \"Table\"}]"
motivation: 多用户PASS的CSI存在网格结构相关性和用户间异构性，传统固定反馈映射难以泛化。
method: 提出Shift-MoE，采用通道分组一步移位操作和门控MLP混合专家模块，实现端到端联合源信道编码。
result: 在归一化均方误差上超越代表性学习基线，且在不同参数设置下保持鲁棒性能。
conclusion: 为PASS系统提供有效的CSI反馈方案，验证了Shift-MoE在异构信道场景的适用性。
---

## 摘要
在频分双工系统中，Pinching天线系统（PASS）的性能增益关键依赖于基站处准确的信道状态信息（CSI）。然而，PASS的CSI在波导-天线网格上表现出结构化相关性，并且用户间存在显著异质性，这使得传统的固定反馈映射难以泛化。为了解决这一挑战，本文提出了一种基于深度联合源信道编码的端到端CSI反馈方案，用于有噪声的上行反馈链路，称为基于Shift的混合专家网络（Shift-MoE）。具体来说，Shift-MoE利用信道分组的一步移位操作来捕获网格依赖性，而无需全局注意力，并采用门控多层感知机混合专家模块来适应不同用户的异质性CSI统计信息。数值结果表明，所提出的Shift-MoE在归一化均方误差方面始终优于代表性的基于学习的CSI反馈基线，并且在不同的系统参数设置下仍然有效。

## Abstract
In frequency-division duplexing systems, the performance gains of pinching-antenna systems (PASS) critically depend on accurate channel state information (CSI) at the base station. However, PASS CSI exhibits structured correlations over the waveguide-antenna grid and pronounced heterogeneity across users, making conventional fixed feedback mappings difficult to generalize. To address this challenge, this letter proposes an end-to-end CSI feedback scheme over a noisy uplink feedback link based on deep joint source-channel coding, termed Shift-based Mixture-of-Experts (Shift-MoE). Specifically, Shift-MoE leverages channel-grouped one-step shift operations to capture grid dependencies without global attention, and employs a gated multilayer perceptron mixture-of-experts module to adapt to heterogeneous CSI statistics across users. Numerical results demonstrate that the proposed Shift-MoE consistently outperforms representative learning-based CSI feedback baselines in normalized mean squared error and remains effective under different system parameter settings.

---

## 论文详细总结（自动生成）

# 基于Shift-MoE的DJSCC在多用户Pinching天线系统中的CSI反馈：深入总结

## 1. 核心问题与整体含义
- **研究动机**：
  - Pinching天线系统（PASS）是未来无线通信中一种新型可重构天线架构，具有低功耗、低成本、灵活部署等优势。然而，在频分双工（FDD）系统中，下行链路性能严重依赖于基站侧对信道状态信息（CSI）的精确获取。
  - PASS的CSI具有两个独特挑战：
    - **结构化空间相关性**：CSI在波导-天线网格（M×N）上存在沿波导维度和天线维度的结构化相关。
    - **用户间异质性**：不同用户因位置、散射环境不同，CSI统计特性差异显著，导致固定反馈映射难以泛化。
- **研究空白**：
  - 现有深度学习CSI反馈方法（如CsiNet）多假设无误差反馈链路，忽略了实际噪声和衰落导致的源-信道失配。
  - 针对PASS的CSI反馈方案尚未被系统研究，尤其缺乏对多用户异质性和网格结构建模的统一框架。
- **整体含义**：
  - 本文首次系统地研究了多用户PASS在FDD下的CSI反馈问题，旨在通过端到端联合源信道编码（DJSCC）提升反馈鲁棒性，并针对PASS特性设计了新型网络架构Shift-MoE。

## 2. 方法论
### 2.1 核心思想
- 采用**深度联合源信道编码（DJSCC）** 框架，将编码器（用户端）与解码器（基站端）联合优化，直接在有噪反馈链路上传输压缩后的CSI码字，避免传统分离式编码的源-信道失配。
- 提出**Shift-MoE**架构，包含两个关键创新：
  - **移位机制**：用通道分组的一步移位操作替代全局注意力（如Transformer中的MHSA），以低复杂度捕获波导-天线网格的结构依赖性。
  - **门控混合专家（MoE）**：通过多个专家MLP和自适应门控权重，使模型能够根据输入CSI动态组合不同专家，适应不同用户的异质性统计。

### 2.2 关键技术细节
- **CSI表示**：将复数CSI矩阵Hₖ∈ℂ^{M×N}的实部和虚部沿最后一维拼接，得到M×N×2的实值张量作为网络输入。
- **编码器结构**（如图2）：
  1. 输入经1×1卷积投影到特征空间F₀∈ℝ^{dₘ×M×N}，后接层归一化。
  2. N₁个级联的Shift-MoE块，每个块包括：
     - **Shift模块**：将输入特征F按通道分组（组宽g=⌊dₘ/n_div⌋），对前四组分别沿PA维度左右移位和波导维度上下移位，其余通道不变。操作无参数，计算复杂度低。
     - 移位后经LN，展平为token序列T∈ℝ^{MN×dₘ}。
     - **MoE模块**：
       - 先通过门控机制生成掩码G（两全连接层+Sigmoid），与T逐元素相乘得到门控token Tg。
       - N₂个专家MLP（两层，中间维度α₂·dₘ，激活函数GELU）。
       - 池化Tg得到全局描述子，输入门控线性层+Softmax得到专家权重w，加权聚合各专家输出T_moe。
     - 残差连接：F_out = F_sh ⊕ F_moe（⊕为逐元素加）。
- **解码器**：对称结构，镜像编码器，最终输出重建的2通道张量Ĥ∈ℝ^{2×M×N}，重组为复数CSI。
- **反馈链路**：编码器输出实码字Zₖ∈ℝ^{L}，分成两组I/Q序列形成复符号sₖ∈ℂ^{L/2}。上行链路建模为准静态平坦Rician衰落+AWGN，接收端理想信道估计后进行单抽头均衡，得到̃sₖ = sₖ + n/h。
- **训练目标**：最小化期望Frobenius范数误差：E[‖Hₖ－Ĥₖ‖²_F]。

### 2.3 公式与算法流程（文字说明）
- 输入：Hₖ(实数拼接)→Encoder(f₁)→Zₖ→映射为复符号sₖ→经衰落+噪声信道→接收均衡→̃sₖ→Decoder(f₂)→Ĥₖ。
- 优化：联合训练θ₁,θ₂通过梯度下降最小化重建误差。

## 3. 实验设计
- **数据集/场景**：
  - 基于PASS几何信道模型生成CSI数据：K个用户随机分布，M×N个波导-天线网格（默认M=16,N=8），考虑LoS+NLoS（P=3条散射路径），载频28 GHz。
  - 训练/测试：未明确划分，但使用批量大小为32、300个epoch。
- **Benchmark方法**：
  - CsiNet（经典CSI反馈网络）
  - MLP基线（无移位模块）
  - Transformer方案（如TransNet、Quan-Transformer）
  - ShiftViT（无MoE）
  - 提出的Shift-MoE（完整模型）
- **对比指标**：归一化均方误差（NMSE）。
- **实验变量**：
  - 反馈信噪比（SNR）：-10 dB~8 dB
  - 用户数K=5（默认）及其他（具体数值在图4a展示，K=3,5,10? 文本未完全给出但提到K增加性能下降）
  - 压缩比η：1/2（默认）及其他（图4b）
  - 天线配置（M,N）：(8,4),(16,8),(32,16)等（图4c）

## 4. 资源与算力
- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长等具体算力信息。仅给出了训练超参数（学习率1e-4、批量32、epoch 300），未提及硬件配置。

## 5. 实验数量与充分性
- **实验组数**：
  - 主对比实验（图3）：1组，在固定配置（K=5,M=16,N=8,η=1/2）下比较5种方法在不同SNR下的NMSE。
  - 参数敏感性实验（图4）：3组，分别改变用户数、压缩比、天线配置，均在多种SNR下对比。
  - 专家权重可视化（图5）：1组，展示不同用户的门控权重。
  - 共计**至少5组**实验（1组主对比+3组参数扫描+1组可视化），但缺乏完整表格汇总。
- **充分性评价**：
  - **优点**：覆盖了关键系统参数（用户数、压缩比、天线规模、信噪比），验证了Shift-MoE在各维度上的鲁棒性。
  - **不足**：
    - 未进行**消融实验**的量化对比（如分别移除移位或MoE的效果，文中仅在文字上对比了ShiftViT vs Shift-MoE，但未给出独立消融的表格或曲线）。
    - 未提供**更多基准方法**（如传统非学习方法、其他DJSCC方法）。
    - 未测试**实际信道估计误差**（文中假定理想信道知识，且仅考虑平坦衰落，未涉及频率选择性信道）。
    - 未讨论**不同反馈链路模型**（如毫米波信道、更复杂的干扰）。
    - **统计可靠性**：未报告多次运行的标准差或置信区间，仅给出单次曲线。
- **客观性/公平性**：比较方法为代表性基线，但未说明是否采用相同或公平的实现细节（如训练策略、超参数调优）。整体客观性中等。

## 6. 主要结论与发现
- Shift-MoE在**所有SNR范围**上NMSE均低于CsiNet、MLP、Transformer及ShiftViT（无MoE）。
- 随着SNR提高，性能差距扩大，说明MoE在低噪声区域能更好地利用表征容量。
- 用户数增加时NMSE上升，但Shift-MoE的退化幅度相对较小，体现了其对异构CSI的适应性。
- 压缩比越大（η越大），重建精度越高，尤其在中高SNR下增益显著。
- 天线阵规模增大时，NMSE下降，表明Shift-MoE能有效利用更大的网格结构信息。
- 专家权重的可视化表明：不同用户触发的门控权重不同，说明MoE实现了输入自适应路由。

## 7. 优点
- **创新性**：首次将移位操作与混合专家架构结合应用于PASS CSI反馈，替代了高复杂度的全局注意力。
- **低复杂度**：移位操作为无参数操作，避免Transformer的二次复杂度（O(M²N²)），仅需O(α₁MNdₘ²)，适合资源受限的UE端。
- **端到端优化**：采用DJSCC框架，直接在有噪信道上联合训练，缓解了源-信道失配。
- **鲁棒性验证**：在多种系统参数下验证了模型的有效性，具有良好的泛化能力。
- **可视化分析**：专家权重可视化提供了模型行为的可解释性，证实了输入自适应路由机制。

## 8. 不足与局限
- **实验覆盖不全面**：
  - 缺少对大用户规模（如K>10）、更宽压缩比、不同信道模型（如毫米波簇延迟线模型、移动场景）的测试。
  - 未与**最新DJSCC方法**（如基于Transformer的DJSCC、注意力DJSCC）进行对比。
  - 未评估**实际反馈开销**（如信令开销、延迟）。
- **假设理想性**：
  - 反馈链路只考虑了准静态平坦Rician衰落和AWGN，未考虑频率选择性、快衰落、多径时延扩展等更实际因素。
  - 接收端假设完美信道知识（̂h=h），简化了实际中信道估计误差的影响。
  - 用户位置均匀分布，未测试非均匀、集群分布等更具挑战的场景。
- **模型局限性**：
  - 移位操作的设计依赖于特定网格结构（矩形均匀网格），若天线位置不规则或波导分布不均匀，可能不适用。
  - MoE的专家数量和扩展因子未进行系统调优，可能不是最优配置。
- **算力/可复制性**：未提供代码或具体实验设置细节（如随机种子、数据生成协议），降低可复现性。
- **缺乏统计显著性**：未报告多次运行的方差，单次实验结果可能存在偏差。

（完）
