---
title: Learning-Based Beamforming for Energy Efficiency of Continuous Aperture Array Systems
title_zh: 基于学习的连续孔径阵列系统能效波束赋形
authors: "Shiyong Chen, Jia Guo, Shengqian Han"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12321v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 级联图神经网络与隐式神经表示的波束成形方法
tldr: 针对下行多用户连续孔径阵列系统，联合优化基站孔径尺寸与波束赋形函数以最大化能效。提出级联网络架构：图神经网络学习孔径尺寸，基于函数梯度的隐式神经表示（FGB-INR）学习连续波束赋形。方法利用置换等变性，仿真表明能效接近数值方法但推理延迟大幅降低，同时FGB-INR提升了能效并减少了样本复杂度和训练时间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12321v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 674, \"height\": 176, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12321v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 397, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12321v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 198, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12321v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 239, \"label\": \"Table\"}]"
motivation: 连续孔径阵列的能效最大化涉及孔径尺寸与波束赋形函数的耦合非线性优化，传统数值方法计算复杂且难以实时处理。
method: 提出GNN与FGB-INR级联网络，GNN利用置换等变性学习孔径尺寸，FGB-INR基于EE目标函数梯度设计更新方程以优化波束赋形。
result: 仿真显示所提方法能效接近数值最优，推理延迟显著降低；FGB-INR结构较无梯度先验方法能效更高，且样本复杂度与训练时间更少。
conclusion: 该方案有效解决了CAPA系统能效优化中的连续函数优化难题，实现了低延迟、高能效的波束赋形，适用于实时通信场景。
---

## 摘要
本文联合优化基站连续孔径阵列的尺寸和波束赋形函数，以最大化下行多用户多CAPA系统的能效，其中基站和用户均配备CAPA。由于波束赋形函数是基站CAPA上的连续电流分布，由此产生的能效最大化问题是一个耦合了孔径尺寸和波束赋形设计的非平凡泛函优化问题。为应对这一挑战，我们提出了一种级联网络架构，由图神经网络和基于函数梯度的隐式神经表示组成，分别学习基站CAPA尺寸和波束赋形函数。两个网络均利用最优优化策略的排列等变性，且FGB-INR的更新方程根据能效目标的函数梯度结构设计。仿真结果表明，所提方法在显著降低推理延迟的同时，能逼近数值方法的能效。结果还表明，FGB-INR中的函数梯度结构在降低样本复杂度和训练时间的同时提高了能效。

## Abstract
This paper jointly optimizes the base-station (BS) continuous aperture array (CAPA) dimensions and beamforming functions to maximize energy efficiency (EE) of the downlink multiuser multi-CAPA system, where both the BS and the users are equipped with CAPAs. Since the beamforming functions are continuous current distribution over the BS CAPA, the resulting EE maximization problem is a nontrivial functional optimization problem that couples aperture sizing and beamforming design. To address this challenge, we propose a cascaded network architecture consisting of a graph neural network (GNN) and a functional-gradient based implicit neural representation (FGB-INR) to learn the BS CAPA dimensions and beamforming functions, respectively. Both networks exploit the permutation equivariance of the optimal optimization policy, and the update equations of FGB-INR are designed according to the functional-gradient structure of the EE objective. Simulation results show that the proposed method approaches the EE of the numerical method while substantially reducing inference latency. They also demonstrates that the functional-gradient structure in FGB-INR improves EE while reducing sample complexity and training time.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **研究背景**：连续孔径阵列（CAPA）是一种新兴天线架构，通过在有限物理表面上生成近似连续的电流分布，实现比传统空间离散阵列更精细的波束赋形。但CAPA的波束赋形优化是定义在连续孔径上的泛函优化问题，传统凸优化方法无法直接求解。现有方法（如傅里叶基展开、泛函WMMSE）虽然能逼近最优，但计算复杂度高，难以实时应用。
- **核心问题**：本文研究下行多用户多CAPA系统中，**联合优化基站CAPA的物理尺寸和波束赋形函数**，以最大化系统的能量效率（EE）。该问题耦合了孔径尺寸（有限维变量）和连续波束赋形函数（无限维变量），是一个非平凡的泛函优化问题。快速低延迟的在线求解方法尚缺。
- **研究意义**：能效是6G关键指标，CAPA有望提升频谱和能量效率，但现有工作多聚焦频谱效率，能效优化研究较少。本文提出的学习方法可在保持接近最优能效的同时大幅降低推理延迟，对实际部署有重要价值。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用**置换等变性（Permutation Equivariance, PE）** 设计神经网络结构，将联合优化问题分解为两阶段：首先由图神经网络（GNN）学习基站CAPA尺寸（标量输出，置换不变性）；然后由基于函数梯度的隐式神经表示（FGB-INR）学习连续波束赋形函数（向量输出，置换等变性）。FGB-INR的更新方程源自EE目标的泛函梯度结构，实现了物理驱动的隐式神经表示。
- **关键技术细节**：
  1. **归一化坐标映射**：将物理孔径坐标s映射到归一化坐标̃s，使波束赋形函数定义域与孔径尺寸解耦，避免不同尺寸下网络输入维度变化。
  2. **GNN学习孔径尺寸**：以用户几何位置（r_o）为节点特征，在全连接用户图上进行消息传递，最后通过均值池化得到孔径边长Lx_B, Ly_B（置换不变）。
  3. **FGB-INR学习波束赋形函数**：将波束赋形函数表示为以空间坐标s和用户位置为输入的隐式神经网络。设计了基于EE函数梯度迭代的新颖更新方程（式23），其中聚合项和组合项模仿经典泛函梯度迭代（式22），但用参数化网络层代替复杂的积分和矩阵求逆。为降低计算开销，用两个附加INR（B_θ, Q_θ）分别参数化梯度迭代中的中间量α和β。
  4. **PE保证**：GNN聚合采用求和操作（permutation equivariant），FGB-INR中更新的参数矩阵W_b, W_q采用参数共享策略（如所有用户共享相同的可训练矩阵），确保输出随用户索引排列而等变。
- **算法流程**：输入用户位置 → GNN输出尺寸 → 计算归一化坐标映射 → 以(s, Ro)为输入，经FGB-INR各层（每层按式23更新），输出每个用户每个离散采样点上的波束赋形函数值。

### 实验设计

- **场景与数据集**：下行多用户（默认K=3）CAPA系统，用户位置随机分布（x,y~U[-5,5]m, z~U[20,30]m）。基站CAPA边长范围[0.1,2]m，用户CAPA 0.5m见方。载频2.4GHz，波长0.125m。数据流d=2。训练集500,000样本，测试集10,000样本（不同用户位置）。
- **Benchmark**：数值最优方法**Nest-Opt**（外层搜索尺寸，内层Dinkelbach优化波束赋形，精度高但慢）；无尺寸优化的学习基准**INR-Beam**（固定最大尺寸）；无尺寸优化的数值基准**Dink-Beam**（固定最大尺寸，仅Dinkelbach）。
- **对比方法**：
  - 所提方法（GNN+FGB-INR）
  - **VAGNN**：所提GNN部分 + 常规GNN（式18）替代FGB-INR，消融FGB-INR增益。
  - **VAFNN**：用前馈网络（FNN）替换所有GNN和FGB-INR，消融置换等变性增益。
- **评价指标**：EE值（bps/Hz/W）；相对EE比（与Nest-Opt的比值）；推理延迟；训练样本数、训练时间、模型参数量。

### 资源与算力

- 论文**未明确说明GPU型号和数量**，仅提及使用Adam优化器，初始学习率1e-3，batch size=8。训练500,000样本用时**0.18小时**（约11分钟），模型参数量**3.19M**。推理延迟**0.051s**。由于未说明硬件细节，无法准确评估算力需求，但训练时间相对较短，推测使用了单GPU。

### 实验数量与充分性

- 实验覆盖：
  1. **不同α（CAPA表面功耗系数）** 下的EE对比（图2a）。
  2. **不同用户数K**（2~7）下的EE对比（图2b）。
  3. **不同训练样本数**（5~5000）下学习性能对比（Table I），包括Proposed、VAGNN、VAFNN三者。
  4. **推理延迟与训练复杂度**对比（Table II），包括样本数、训练时间、参数量。
- **充分性评价**：实验设计较全面，涵盖了超参数（α）、系统规模（K）、数据规模、消融等维度。但缺少真实信道模型或非视距场景验证；仅考虑K=3为主，其他K仅见于图2b，消融实验默认K=3。此外，数值基准Nest-Opt仅以正方形孔径（Lx=Ly）搜索，未考虑非正方形情况，可能略失公平。总体实验较充分。

### 论文的主要结论与发现

- 所提级联方法（GNN+FGB-INR）的能效**接近数值基准Nest-Opt**（当α合理时，相对比约95%），**推理延迟降低3个数量级**（0.051s vs 3.42h）。
- **功能梯度结构（FGB-INR）** 相比常规GNN（VAGNN）显著提升能效（样本数少时提升更明显），同时**样本复杂度和训练时间更低**（仅需50样本即可达90%+能效，VAGNN需5000样本）。
- **利用置换等变性**（Proposed vs VAFNN）带来巨大性能增益，尤其在样本数少时（1000样本时Proposed: 92.07%, VAFNN: 68.84%）。
- 全部学习方法均优于仅优化波束赋形（固定尺寸）的INR-Beam和Dink-Beam，证明了联合优化孔径尺寸的必要性。

### 优点

- **创新性**：将泛函梯度迭代的结构知识注入隐式神经表示设计，而非简单端到端黑箱学习，物理先验提升了数据效率和性能。
- **实用性**：推理延迟极低（毫秒级），适合实时或近实时波束赋形；训练样本需求少，便于实际部署。
- **理论支撑**：证明了最优策略的排列等变性，并严格设计了满足该性质的网络结构，保证了学习一致性。
- **实验设计严谨**：进行了充分的消融实验（替换FGB-INR、替换GNN），孤立验证了每个创新点的贡献。

### 不足与局限

- **实验场景局限**：仅考虑K=3为主，更大规模用户（如K=10+）未验证；仅采用线视距信道模型，未评估NLoS或更复杂信道；仅仿真，无实际硬件或射线追踪数据。
- **功率模型简化**：CAPA表面功耗线性正比于面积，实际硬件可能非线性（如驱动电路随面积指数增长）；未考虑RF链数随尺寸变化（文中固定NRF=Kd）。
- **数值基准缺陷**：Nest-Opt仅在正方形孔径上搜索（Lx=Ly），未考虑矩形极优解，与所提方法可能不公平。
- **收敛性未分析**：无理论保证FGB-INR的隐式网络参数收敛到全局最优，仅实验验证。
- **代码未开源**：可复现性受影响。

（完）
