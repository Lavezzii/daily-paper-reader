---
title: Restoration Flow Matching-Based Channel Refinement and Equalization Correction for MIMO Semantic Communications
title_zh: 基于复原流匹配的MIMO语义通信信道细化与均衡校正
authors: "Wenkai Liu, Nan Ma, Jianqiao Chen, Xiaodong Xu, Meixia Tao, Ping Zhang"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23615v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 用于MIMO语义通信的信道细化和均衡校正的神经流匹配
tldr: MIMO语义通信中，不完美信道状态信息和均衡失配会降低语义重建质量。为此，提出统一恢复流匹配框架，包含信道细化和均衡校正两个模块，并设计双锚点扰动训练策略以增强鲁棒性。实验表明，该方法能提升信道估计精度和语义重建质量，且相比扩散模型所需采样步数更少。该框架统一了信道估计与均衡校正的级联逆问题，为MIMO语义通信提供了高效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23615v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1501, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1230, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1827, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1844, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 526, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 527, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 803, \"height\": 835, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23615v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1845, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23615v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 211, \"label\": \"Table\"}]"
motivation: MIMO语义通信中，不完美CSI和均衡失配严重损害语义重建质量，现有方法难以联合处理这两个级联逆问题。
method: 提出恢复流匹配框架，包括CRFM和SRFM模块，分别用于信道细化和均衡校正；采用双锚点扰动训练策略联合学习精炼与纠错。
result: 在MIMO信道和视觉语义传输任务上，所提方法显著提升信道估计与语义重建指标，且采样步数少于扩散基线。
conclusion: 统一框架有效解决了信道估计与均衡校正问题，提升了语义通信的鲁棒性和效率。
---

## 摘要
在多输入多输出（MIMO）语义通信中，非完美的信道状态信息（CSI）和均衡失配会严重降低语义重构质量。为解决该问题，我们提出了一种基于统一复原流匹配（RFM）的信道细化与均衡校正框架。具体地，开发了信道RFM（CRFM）模块以细化粗信道，从而提高信道估计精度。基于细化后的信道，采用语义RFM（SRFM）模块来校正后均衡潜在空间中的残余失真。关键思想是将信道估计和均衡这两个级联逆问题表述为统一的条件复原任务，其中学习的条件速度场引导扰动分布向目标分布演化。为了增强这两个模块在不同失真条件下的鲁棒性，我们开发了一种双锚点扰动训练策略，联合学习近流形细化与大误差校正，并通过几步确定性常微分方程（ODE）求解器实现推理。在MIMO信道和视觉语义传输任务上的大量实验表明，所提方案改善了信道估计和语义重构质量的关键指标。此外，与基于扩散的生成式基线相比，所提方法需要更少的采样步数。

## Abstract
In multiple-input multiple-output (MIMO) semantic communication, imperfect channel state information (CSI) and equalization mismatch can seriously degrade semantic reconstruction quality. To address this issue, we propose a unified restoration flow matching (RFM)-based framework for channel refinement and equalization correction. Specifically, the channel RFM (CRFM) module is developed to refine the coarse channel, thereby improving channel estimation accuracy. Based on the refined channel, the developed semantic RFM (SRFM) module is employed to correct the residual distortions in the post-equalization latent space. The key idea is to formulate the two cascaded inverse problems of channel estimation and equalization as the unified conditional restoration task, in which the learned conditional velocity field guides the perturbed distribution towards the target distribution. To enhance the robustness of these two modules under various distortion conditions, we develop a dual-anchor perturbation training strategy that jointly learns near-manifold refinement and large-error correction, and implement inference through a few-step deterministic ordinary differential equation (ODE) solver. Extensive experiments on MIMO channels and visual semantic transmission tasks demonstrate that the proposed scheme improves key metrics for channel estimation and semantic reconstruction quality. Moreover, compared with representative diffusion-based generative baselines, the proposed method requires fewer sampling steps.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：在MIMO语义通信中，接收端通常需要执行两个级联的逆问题：首先基于导频信号估计信道（例如通过最小二乘LS），再基于估计出的信道进行MIMO均衡以恢复语义潜在表示。然而，实际系统中信道状态信息（CSI）不完美，导致均衡失配，误差会从信道估计传播到语义重构，严重降低图像重建质量。现有工作大多假设完美CSI，或仅关注SISO场景，缺乏对MIMO系统中不完美CSI条件下语义恢复的系统性研究。
- **整体含义**：本文旨在解决MIMO语义通信中由导频获取CSI到CSI依赖的均衡失配所引起的级联失真问题，提出一个统一的复原流匹配框架，在信道域和语义潜在域分别进行细化和校正，从而提升语义通信的鲁棒性和重建质量。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将信道估计和均衡校正这两个级联逆问题统一为条件复原任务，利用流匹配（Flow Matching）框架学习从扰动状态到干净状态的确定性速度场，通过几步ODE求解实现快速推理。
- **关键技术细节**：
  - **整体框架**：基于SwinT-JSCC的语义发射机-接收机架构。发射端：SwinT-JSCC编码器提取潜在表示，量化后映射为MIMO基带信号。接收端：先进行LS粗信道估计，然后引入**信道复原流匹配（CRFM）**模块细化信道；基于细化后的信道构造MMSE均衡器，得到粗语义潜在表示；再引入**语义复原流匹配（SRFM）**模块校正残余失真；最后经SwinT-JSCC解码器重建图像。
  - **流匹配统一建模**：定义通用源-目标对$(S_0,S_1)$，对于CRFM是（粗信道估计，真实信道），对于SRFM是（粗语义潜在，干净语义潜在）。采用最优传输路径$S_t = tS_1 + (1-t)S_0$，条件速度场$v^*_t(S_t|S_1)=S_1-S_0$。训练时通过条件流匹配目标（CFM loss）学习速度场网络。
  - **双锚点扰动训练策略**：为了解决不同失真程度（轻度和严重）下的鲁棒性，采用两个扰动强度锚点$\tau_\ell=0.1$和$\tau_h=1.0$。构建低扰动路径$S_{t}^{\tau_\ell}=S_1+(1-t)\tau_\ell\xi$和高扰动路径$S_{t}^{\tau_h}=S_1+(1-t)\tau_h\xi$，对应的目标速度分别为$-\tau_\ell\xi,-\tau_h\xi$。训练目标为加权混合：$\mathcal{L}_{\text{RFM}}(\theta;\tau_\ell,\tau_h) = \omega_\ell \mathbb{E}[\|v_\theta(S_{t}^{\tau_\ell},t) + \tau_\ell\xi\|^2] + \omega_h \mathbb{E}[\|v_\theta(S_{t}^{\tau_h},t) + \tau_h\xi\|^2]$，设置$\omega_\ell=\omega_h=1/2$。该策略使得单个速度场能自适应地结合不同锚点的信息。
  - **推理**：基于学习的速度场，通过显式欧拉ODE求解器进行几步确定性推理。CRFM使用$k_H=5$步，SRFM使用$k_U=2$步。初始状态分别为LS估计和MMSE均衡后的粗潜在表示。
- **公式/算法流程**（文字说明）：
  - 训练阶段（Algorithm 1 & 2）：
    - 对于CRFM：采样干净信道$H_1$，随机采样$t\sim U(0,1)$和$\tau\in\{\tau_\ell,\tau_h\}$，构造$H_t = H_1 + (1-t)\tau\xi$，计算目标速度$-\tau\xi$，最小化$\mathcal{L}_H(\theta_H;\tau_\ell,\tau_h)$。
    - 对于SRFM：从图像数据集经固定编码器得到干净语义潜在$U_1$，类似构造$U_t$和速度，最小化$\mathcal{L}_U(\theta_U;\tau_\ell,\tau_h)$。
  - 推理阶段：
    - CRFM：$H^{[0]} = H_{\text{LS}}$，$H^{[k+1]} = H^{[k]} + \Delta t_H \cdot v_{\theta_H^*}(H^{[k]}, t^{[k]}_H)$，$k=0,...,k_H-1$，得到$\hat{H}=H^{[k_H]}$。
    - SRFM：$U^{[0]} = \hat{U}_{\text{MMSE}}$，$U^{[k+1]} = U^{[k]} + \Delta t_U \cdot v_{\theta_U^*}(U^{[k]}, t^{[k]}_U)$，$k=0,...,k_U-1$，得到$\hat{U}=U^{[k_U]}$。

## 3. 实验设计：数据集/场景、基准、对比方法
- **数据集与场景**：
  - 信道数据：根据3GPP TR 38.901的CDL-B和CDL-C模型生成，载频40GHz，标准化天线间隔0.5，MIMO配置：$N_t=64$发，$N_r=16$收，空间流$N_s=4$。10,000个独立信道用于训练，1,000个用于测试。
  - 图像数据：ImageNet-mini（34,745训练，3,923测试）和CelebA（2,693训练，300测试）。图像中心裁剪并缩放到512×512。
- **基准/对比方法**：
  - 信道估计：LS、近似MMSE、OMP（正交匹配追踪）、Score-based方法（扩散模型）、DDIM-based方法（扩散隐式模型）。
  - 语义重建：多种消融配置：AWGN下的SwinT-JSCC（训练于10dB）、AWGN+SRFM；MIMO下的LS-MMSE、LS-MMSE+SRFM、LS-MMSE+CRFM、LS-MMSE+CRFM+SRFM；还有Perfect CSI-MMSE、Perfect CSI+SRFM作为上界；以及传统分离编码方案JPEG-1/2LDPC-4QAM。
- **评价指标**：
  - 信道估计：归一化均方误差（NMSE）。
  - 语义重建：PSNR和MS-SSIM。信道带宽比（CBR）固定为$\rho=1/96$。

## 4. 资源与算力
- 文中明确说明：所有实验在配备RTX 4090 GPU的Linux服务器上完成。SwinT-JSCC预训练在AWGN信道下，学习率$1\times10^{-4}$；CRFM和SRFM训练学习率$1\times10^{-5}$。但未给出具体训练时长、GPU数量等详细算力数据。

## 5. 实验数量与充分性
- **实验组数**：较为充分。
  - 敏感性分析（图3）：针对ODE步数（CRFM: 1/2/5/10/20/50步; SRFM: 1/2/4/8/10/20步）、不同扰动锚点（单锚点0.1/0.5/0.8/1.0 vs 双锚点组合）进行消融。
  - 信道估计性能（图4）：在两个信道模型（CDL-B, CDL-C）上，分别对比了NMSE vs SNR（固定导频长度4）和NMSE vs 导频长度（固定SNR=10dB），每个条件下包含6种方法。
  - 不同信道估计对语义重建的影响（图5-6）：展示MS-SSIM和PSNR vs SNR，包含8种方法/配置。
  - 完整语义重建消融（图7）：在两个数据集（ImageNet-mini, CelebA）上，展示MS-SSIM和PSNR vs SNR，包含10种方法/配置。
  - 复杂度与延迟比较（表I）：对比了Score-based、DDIM、CRFM、SRFM的MACs、参数量和推理延迟。
  - 可视化示例（图8）：在SNR=3dB下展示了9种配置的重建图像。
- **公平性与客观性**：
  - 对比方法涵盖了经典模型方法、稀疏恢复、扩散生成基线以及自身消融，较为全面。
  - 在信道估计对比中，Score-based和DDIM使用了与CRFM相同的UNet骨干网络，确保了比较的公平性。
  - 语义重建实验中，解码器固定，只改变前端处理模块，能清晰体现CRFM和SRFM各自的贡献。
- **结论**：实验设计充分、客观，覆盖了多维度比较（SNR、导频长度、数据集、视觉指标），消融实验完整，证明了方法的有效性。

## 6. 论文的主要结论与发现
- **核心结论**：提出的级联RFM框架能够有效缓解MIMO语义通信中因不完美CSI导致的信道估计误差传播问题，提升信道估计精度和图像重建质量。CRFM显著降低NMSE，SRFM进一步校正残余潜在失真。
- **具体发现**：
  - CRFM在低SNR和短导频场景下增益尤为显著，优于LS、OMP、近似MMSE和扩散基线。
  - 双锚点训练策略（$\tau_\ell=0.1,\tau_h=1.0$）比单一锚点在不同SNR下性能更均衡。
  - 仅需几步ODE推理（CRFM 5步，SRFM 2步）即可接近收敛，推理延迟远低于扩散基线（CRFM 0.52ms vs Score-based 6.58s, DDIM 831ms）。
  - 级联CRFM+SRFM在NMSE、PSNR、MS-SSIM上均优于单独使用任一模块，并逼近完美CSI的上界。

## 7. 优点
- **方法论亮点**：
  - 首次将MIMO语义接收端的信道估计和均衡校正统一为流匹配复原任务，避免了逐模块独立设计的次优性。
  - 双锚点扰动训练策略简单有效，无需复杂的噪声调度，即可同时覆盖近流形精炼和大误差校正。
  - 采用确定性ODE推理，相比扩散模型（需多步迭代）显著降低采样步数和延迟，适合通信接收机低延迟需求。
- **实验设计亮点**：
  - 对比了多种经典和先进方法，包括模型驱动、稀疏恢复和生成式基线。
  - 消融实验完整，单独评估了CRFM和SRFM的贡献，并展示了其对信道估计和语义重建的级联影响。
  - 在两个不同图像数据集上验证了泛化性，并提供了可视化结果。

## 8. 不足与局限
- **实验覆盖的局限**：
  - 信道模型仅采用了3GPP CDL-B和CDL-C两个模型，未涉及更丰富的实测信道或时变信道场景（未来工作提及了时间变化和多用户MIMO）。
  - 训练数据规模相对较小（1万信道，3.5万图像），大规模场景下的鲁棒性有待验证。
  - 仅考虑了单用户MIMO，未扩展到多用户或OFDM系统。
- **偏差风险**：
  - 语义编码器（SwinT-JSCC）在AWGN下预训练并固定，可能会使SRFM的学习偏向特定噪声类型；尽管SRFM能适应额外失真，但初始编码器可能限制上限。
  - 双锚点选择通过实验确定（0.1和1.0），可能在其他系统配置下需要重新调优。
- **应用限制**：
  - SRFM模型参数量较大（129.81M），计算开销较高（21.8 G MACs），在计算资源受限的边缘设备上部署可能有挑战。
  - 推理延迟虽然比扩散模型低很多，但CRFM+SRFM合计约36.3ms，对于超高实时性要求（如毫秒级控制）仍需优化。
- **算力数据缺失**：未提供训练总时长、GPU数量等，难以量化训练成本。

（完）
