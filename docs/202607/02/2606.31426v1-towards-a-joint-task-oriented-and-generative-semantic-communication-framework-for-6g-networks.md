---
title: Towards a Joint Task-Oriented and Generative Semantic Communication Framework for 6G Networks
title_zh: 面向6G网络的联合任务导向与生成式语义通信框架
authors: "Soheyb Ribouh, Phil Polo Ditsia Di Ngoma"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31426v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 基于神经接收机和时空图神经网络的语义通信框架
tldr: "针对6G网络通信中带宽与语义保真度平衡的挑战，提出一种端到端OFDM语义通信框架。该框架将视觉场景转化为图结构表示，通过语义编码器提取对象特征与关系，在接收端利用时空图神经网络进行碰撞风险估计，同时采用扩散模型实现场景重建。在3GPP车载信道和MIMO配置下，相比像素域传输实现99.1%数据压缩，优于JPEG/HEVC，且FID分数显著低于现有语义通信方法，兼顾了任务推理与感知质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31426v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1863, \"height\": 1049, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31426v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 906, \"height\": 1166, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31426v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31426v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 898, \"height\": 436, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31426v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 863, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31426v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 826, \"height\": 657, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31426v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 898, \"height\": 987, \"label\": \"Figure\"}]"
motivation: 传统通信传输原始数据带宽开销大，亟需一种能提取任务相关语义并同时支持生成式重建的6G语义通信方案。
method: 构建语义编码器将场景转为图表示，接收端用ST-GNN做碰撞风险预测，扩散模型并行重建视觉图，实现任务导向与生成式联合。
result: "压缩率达99.1%，下游推理性能优于传统编解码，FID分数显著低于现有语义通信方法。"
conclusion: 该框架验证了联合任务导向与生成式语义通信在6G车载场景下的高效性与高保真度。
---

## 摘要
语义通信通过传输与任务相关的含义而非原始数据，显著降低带宽消耗同时保持通信意图，已成为6G无线系统的关键使能技术。本文提出一种端到端基于OFDM的语义通信框架，该框架将语义编解码流水线与在3GPP车辆信道上运行的神经接收器相结合。语义编码器通过将视觉场景转换为由对象级特征和关系结构组成的图表示来提取其底层含义。在接收端，重构的场景图由基于时空图神经网络（ST-GNN）的模块处理，用于碰撞风险评估，实现任务导向推理。同时，基于扩散的语义解码器从恢复的语义中重构视觉场景，提供双重功能：安全预测和图像重建。所提框架在多输入多输出（MIMO）配置下在不同信噪比条件下进行评估。实验结果表明，相对于像素域传输，该框架实现了高达99.1%的数据压缩，在保持下游推理性能的同时优于传统基于压缩的方法（JPEG和HEVC）。此外，基于扩散的重建相比现有语义通信方法获得了显著更低的弗雷歇初始距离（FID）评分，反映了更优的语义和感知保真度。

## Abstract
Semantic Communication (SC) has emerged as a key enabler for 6G wireless systems by transmitting task-relevant meaning rather than raw data, thereby significantly reducing bandwidth consumption while preserving communication intent. In this work, we propose an end-to-end OFDM-based semantic communication framework that integrates a semantic encoder-decoder pipeline with a neural receiver operating over a 3GPP vehicular channel. The semantic encoder extracts the underlying meaning of a visual scene by transforming it into a graph-based representation consisting of object-level features and relational structure. At the receiver, the reconstructed scene graph is processed by a spatio-temporal graph neural network (ST-GNN)-based module for collision-risk estimation, enabling task-oriented inference. In parallel, a diffusion-based semantic decoder reconstructs the visual scene from the recovered semantics, providing dual functionality: safety prediction and image reconstruction. The proposed framework is evaluated in a MIMO configuration under varying SNR conditions. Experimental results show that it achieves up to 99.1% data compression relative to pixel-domain transmission, outperforming conventional compression-based methods (JPEG and HEVC) while preserving downstream inference performance. Furthermore, the diffusion-based reconstruction attains significantly lower frechet inception distance (FID) scores than existing semantic communication approaches, reflecting superior semantic and perceptual fidelity.

---

## 论文详细总结（自动生成）

# 论文总结：Towards a Joint Task-Oriented and Generative Semantic Communication Framework for 6G Networks

## 1. 核心问题与整体含义（研究动机和背景）
传统无线通信传输原始像素数据，在6G车载等场景中带宽开销巨大、延迟高且冗余多。语义通信（Semantic Communication）通过仅传输与任务相关的“含义”来压缩数据，但现有工作要么缺乏结构化语义（如仅用特征向量），要么忽略真实物理层信道，或无法同时支持任务推理与图像重建。本文旨在提出一个**端到端框架**，在3GPP车载信道下同时实现**任务导向的碰撞风险预测**和**生成式语义图像重建**，填补结构化图语义、生成式重建与真实物理层联合建模的空白。

## 2. 方法论：核心思想、关键技术细节
### 2.1 核心思想
将视觉场景编码为**场景图**（Scene Graph），传输图结构的压缩表示（节点特征、邻接张量、活跃关系集），接收端重构图后并行执行：
- **碰撞预测**（ST‑GNN + LSTM）
- **图像重建**（由图生成文本描述，再驱动Stable Diffusion合成图像）

传输层使用端到端OFDM MIMO链路，包括LDPC编码、QAM调制、OFDM资源网格、神经接收器（联合信道估计/均衡/软解调）。

### 2.2 关键技术细节
- **语义编码器（Algorithm 1）**：
  1. 物体检测（Faster R‑CNN）+ IPM投影到BEV坐标系
  2. 关系估计：基于空间距离、方位等谓词计算边
  3. 输出：节点特征矩阵 **F**（含几何、外观、类别嵌入）、邻接张量 **T**（每层对应一种关系）、活跃关系集 **R′**（仅保留非零关系层）
- **语义解码器（Algorithm 2）**：
  - 通过零填充恢复完整张量 **T̂**
  - 重构场景图 **Ĝ**
- **碰撞预测**：图卷积（GCN）+ 图池化提取空间嵌入，LSTM建模时序，MLP输出碰撞/安全概率
- **图像重建**：
  - 图→文本（<subject, predicate, object> 三元组）
  - 文本→Stable Diffusion 1.4，使用混合扩散损失：`Loss = Loss_image + k × Loss_car`，强化车辆像素的重建权重
- **比特级**：LDPC码率未明示，64‑QAM，132子载波×14符号，2×4 MIMO，3GPP车载信道，神经接收器[21]（残差+可分离卷积+GNN结构）

## 3. 实验设计
### 3.1 数据集
- **对话链路训练/评估**：1043‑syn数据集（CARLA模拟，1044个序列，共50,499张图像），包含动态道路用户与静态基础设施的标注。
- **压缩率评估**：5000张验证图像。
- **神经接收器训练**：1200万样本（Sionna模拟）。

### 3.2 Benchmark与对比方法
- **压缩率对比**：RAW、JPEG、HEVC
- **图像重建质量（FID）对比**：GESCO、WITT、SG2SC、BPG+LDPC（引用[28]中的结果）
- **无线传输性能**：在不同SNR下测试碰撞预测准确率、MCC、AUC、语义保真度（SF）

### 3.3 评估指标
- 压缩率（字节数）
- 碰撞预测：Accuracy、MCC、AUC
- 语义保真度：`SF = |Ŝ ∩ S| / |S|`
- 图像重建：Fréchet Inception Distance (FID)

## 4. 资源与算力
- **GPU型号**：NVIDIA Tesla V100-PCIE-32GB（单卡）
- **训练细节**：
  - 碰撞预测：Adam优化器，lr=5e-5，batch_size=16
  - 扩散模型：AdamW优化器，lr=5e-6，batch_size=5，混合损失（k系数未给出具体值）
  - 神经接收器：Adam优化器，lr=1e-3，训练样本1200万
- **训练时长**：文中未明确说明各模块的训练时间。

## 5. 实验数量与充分性
### 5.1 实验组数
- 压缩率统计（1组，5K图像）
- 碰撞预测性能（1组，静态评估：Acc=0.898, MCC=0.546, AUC=0.836）
- 不同SNR下的碰撞预测曲线（图4，共约10个SNR点）
- 不同SNR下的语义保真度曲线（图5）
- FID与SOTA对比（图6，5种对比方法，多个SNR点）
- 训练损失曲线（图2，Global loss + Car loss，两个子图）
- 定性示例（图7，两对原图/重建图）

### 5.2 充分性与公平性
- **优势**：对比了传统编解码（JPEG/HEVC）和多种近期语义通信方法（GESCO, WITT, SG2SC），覆盖了主流量化指标。
- **不足**：缺乏**消融实验**（如去掉ST‑GNN或LSTM的影响、混合损失中k的敏感性、不同MIMO天线数的影响）。未对比同等压缩率下的传统方法（如HEVC+任务模型）的碰撞预测性能。**无线链路部分的实验仅呈现了碰撞预测和语义保真度与SNR的关系，未直接对比端到端吞吐量或误码率。**
- **公平性**：FID对比使用了文献[28]中报告的结果，并非同一实验设置（可能图像分辨率、测试集不同），存在一定比较偏差。

## 6. 主要结论与发现
- **极高压缩率**：相比RAW压缩99.1%，比HEVC小94倍，每帧仅约1.06 KB。
- **碰撞预测稳健**：在SNR>8dB时准确率可达~0.9，语义保真度>0.8，表明任务相关语义在中等信噪比下仍能保持。
- **图像重建质量领先**：FID在所有SNR下均低于30，远低于SG2SC（~200）和BPG（>300），证实图结构先验对生成质量的显著提升。
- **同时实现任务导向与生成式重建**：一个编码方案同时支持安全预测和可视化辅助，系统效率高。

## 7. 优点
- **首次**将场景图语义、扩散生成式重建和3GPP真实MIMO信道整合在统一端到端框架中。
- **压缩比极高**且不影响下游任务性能，体现语义通信在带宽效率上的潜力。
- **生成式重建质量显著优于现有语义通信方法**，说明结构化先验（场景图）对生成模型的约束作用。
- **考虑了真实物理层**（OFDM、MIMO、LDPC、3GPP车载信道），比大多数仅假设理想信道的工作更贴近实际。
- **混合扩散损失**针对安全关键物体（车辆）进行加权，体现了任务导向的精细设计。

## 8. 不足与局限
- **外观信息丢失**：场景图仅编码空间关系与类别，天气、光照、纹理等外观属性无法恢复，限制了视觉逼真度。
- **文本长度限制**：图转文本后可能超过CLIP最大token数，导致密集场景下的描述截断，丢失部分语义。
- **依赖预训练组件**：物体检测（Faster R‑CNN）、图提取（RoadScene2Vec）、扩散模型均独立预训练，未进行**端到端联合优化**，无法直接调整语义编码以适应信道噪声。
- **缺少消融研究**：未评估不同图压缩策略（如去掉关系层或节点特征）对性能的影响；混合损失中的k系数未提及敏感性分析。
- **数据集局限性**：仅使用CARLA模拟数据，未在真实车载数据集（如nuScenes、KITTI）上验证，泛化性不明。
- **计算开销**：扩散模型解码需多次迭代，延迟较高，可能不适合时延严格的车载场景。文中未提供推理时延数据。
- **碰撞预测性能仍有提升空间**：准确率0.898，但对安全关键应用需更高可靠性。

（完）
