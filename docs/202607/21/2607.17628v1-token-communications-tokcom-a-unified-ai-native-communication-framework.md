---
title: "Token Communications (TokCom): A Unified AI-Native Communication Framework"
title_zh: 令牌通信（TokCom）：一种统一的AI原生通信框架
authors: "Yaru Fu, Liang Ji, Sabita Maharjan, Tony Q. S. Quek"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17628v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 用于6G的新型基于令牌的通信框架
tldr: 经典Shannon范式以比特级可靠重建为中心，与6G中AI智能体间的通信需求不匹配。为此，本文提出Token Communications (TokCom)框架，将大语言模型的基本处理单元——token提升为信息交换的第一类实体。通过异构语言模型间token共享的案例验证了其有效性。该工作为6G走向AI原生、token驱动的通信范式提供了统一架构和方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17628v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1595, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17628v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1638, \"height\": 1440, \"label\": \"Figure\"}]"
motivation: Shannon范式面向人类用户的比特重建，无法满足AI智能体间生成式推理与自主协作的通信需求。
method: 提出TokCom，将token定义为6G通信的第一类实体，并设计从传统系统到TokCom架构的过渡方案。
result: 在异构语言模型间共享token的案例验证了TokCom能有效提升通信效率与语义一致性。
conclusion: TokCom为6G实现AI原生、token驱动的通信范式指明了方向，并提出了若干未来研究问题。
---

## 摘要
随着人工智能从静态感知演变为生成式推理和自主代理，无线通信的基本原理正经历一场范式转变。以用户可靠比特级重建为中心的经典香农范式，与网络主要用户为互联AI代理的新兴场景日益脱节。本文引入令牌通信（TokCom），这是一种新颖的框架，将令牌（即大型语言模型的基本处理单元）提升为第六代无线蜂窝网络（6G）中信息交换的一等实体。我们首先考察从传统通信系统到TokCom的架构转型，并识别实现这一转型的关键挑战及潜在解决方案。随后，我们通过一个实际案例研究来展示异构语言模型间令牌共享的有效性。最后，我们概述了实现适用于6G的AI原生、令牌驱动通信范式的未来研究方向。

## Abstract
As artificial intelligence (AI) evolves from static perception to generative reasoning and autonomous agency, the fundamental principles of wireless communications are undergoing a paradigm shift. The classical Shannon paradigm, centered on reliable bit-level reconstruction for users, is increasingly misaligned with an emerging scenario in which the primary users of the network are interconnected AI agents. This article introduces token communications (TokCom), a novel framework that elevates tokens, i.e., the fundamental processing units of large language models (LLMs), to first-class entities for information exchange in the sixth generation wireless cellular networks (6G). We first examine the architectural transition from conventional communication systems to TokCom and identify the key challenges in implementing this transition, along with potential solution approaches. Thereafter, we present a practical case study to demonstrate the effectiveness of token sharing among heterogeneous language models. Finally, we outline promising future research directions toward realizing an AI-native, token-driven communication paradigm suitable for 6G.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **传统通信范式的局限**：经典Shannon通信以**比特级可靠重建**为目标，优化人类感官体验。然而6G时代的主要用户将不再是人类，而是**AI智能体**（AI agents）——它们需要交换高层知识、意图和上下文，而非原始比特。
- **语义通信的不足**：现有语义通信（SemCom）方法通常**紧耦合于特定任务或模态**，依赖预设计任务相关编码器，导致泛化性差、互操作困难。
- **TokCom的提出动机**：大语言模型（LLM）将各种输入（文本、图像、音频、传感器数据）统一映射到**token嵌入空间**。TokCom将**token提升为一等通信实体**，作为模态无关的通用交换单元，使通信协议直接对接预训练基础模型，实现跨模态融合、任务导向压缩和生成式恢复。
- **整体含义**：TokCom标志着从“传递比特的被动管道”向“分布式神经计算基座”的范式转变，为6G实现AI原生、语义感知通信提供技术路径。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：
  - 将LLM的基本处理单元——**token**——作为信息交换的标准单位。
  - 通信系统不再关注比特级精确性，而是**语义级任务效用**，支持生成式可靠性（通过token inpainting恢复丢失token）和分布式KV缓存（跨边缘节点共享推理上下文）。
- **关键技术细节**：
  - **多模态Token化**：将文本、视觉、音频等信号映射为离散语义token，进入统一嵌入空间。
  - **生成式可靠性层**：当token流部分丢失时，接收端基于生成模型（inpainter）从上下文推断缺失token，而非立即重传。
  - **分布式KV缓存仓库**：网络作为分布式KV缓存，使AI代理能在移动或计算卸载时无缝传递推理状态。
  - **令牌级验证**：通过发送锚定嵌入（anchor embeddings）或使用多个小模型对关键token进行多数投票，防止语义幻觉传播。
  - **令牌感知调度**：根据token语义优先级分配无线资源（如安全关键token优先），并利用轻量SLM预测未来token突发模式。
  - **智能感知资源编排（IARO）**：联合管理推理FLOPS、token带宽和上下文缓存，通过强化学习动态创建不同优先级的token切片。
- **算法流程（文字描述）**：
  1. 异构源层生成多模态数据 → 经多模态token化模块映射为离散token。
  2. token投影到统一嵌入空间，保持跨模态语义一致性。
  3. 多智能体协作调度器选择参与模型（如SLM、LLM），分配角色。
  4. 通信编码器考虑信道条件和任务优先级，传输语义token。
  5. 接收端若检测到token丢失，则调用生成式恢复层（inpainter）进行推理恢复。
  6. 分布式KV缓存支持上下文状态在节点间迁移。
  7. 可选的令牌级验证机制（锚定嵌入或投票）确保语义完整性。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - **MMLU**（大规模多任务语言理解）：随机选取2000道题（从约15900道中抽取）。
  - **HumanEval**（代码生成）：164道手写Python编程题，附带单元测试。
  - **GSM8K**（小学数学推理）：随机选取2000道题，需多步推理得出数值答案。
- **基准方法**：
  1. **SLM Only**：仅用Vicuna v1.5-7B独立推理并直接生成答案。
  2. **LLM Only**：仅用Llama 2-13B从头生成逐步推理并给出答案。
  3. **TokCom（本文方法）**：SLM生成草稿推理token → 直接传至LLM → LLM验证、修正并产生最终答案。
- **评价指标**：
  - **Overall Accuracy (OA)**：正确回答比例。
  - **Collaborative Communication Efficiency (CCE)**：每正确回答问题SLM发送给LLM的平均token数。
  - **System Computational Efficiency (SCE)**：每正确回答问题两模型合计生成token数（越低越好）。

### 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练时长等信息。
- 仅提及模型参数规模：SLM为Vicuna v1.5-7B（约70亿参数），LLM为Llama 2-13B（约130亿参数）。两者共享相同分词器（词汇量32,000）。
- 实验为**推理阶段**，而非训练，因此算力消耗主要为推理计算和通信开销。

### 5. 实验数量与充分性
- **实验数量**：在3个数据集（MMLU、HumanEval、GSM8K）上测试了3种方法（SLM Only、LLM Only、TokCom），每个数据集报告了OA、SCE、CCE三项指标，共3×3=9个主要结果。另有一个具体案例展示（GSM8K第60题）。
- **充分性评价**：
  - **积极方面**：覆盖了语言理解、代码生成、数学推理三种典型任务，具有一定的代表性。指标设计合理，兼顾准确率和效率。
  - **不足**：
    - 未进行**消融实验**（如验证分布式KV缓存、生成式恢复等组件的单独效果）。
    - 未考虑**噪声信道**下的性能（如随机丢包、信噪比变化）。
    - 仅使用**一对模型**（SLM+LLM），未扩展到多agent动态协作场景。
    - 仅对比了单模型基线，未与现有语义通信方案（如SemCom）或传统比特级传输对比。
    - 未在**真实无线网络**或**仿真信道**下测试，缺乏延迟、吞吐量等通信指标。
    - 随机抽取数据子集（MMLU和GSM8K各2000题），但未说明多次随机抽样的统计稳定性（仅提到“平均五次独立运行”）。

### 6. 论文的主要结论与发现
- **准确率接近LLM Only**：TokCom在三个数据集上的准确率仅比最优的LLM Only低5.1%~7.8%，显著优于SLM Only（低16.8%~27.1%）。
- **计算效率显著提升**：TokCom的SCE比LLM Only降低11.8%~16.8%（每正确回答问题生成的token数减少）。
- **通信开销合理**：CCE显示SLM向LLM发送的token数平均为291~514个/正确问题，且这些token是SLM推理中自然产生的，不额外增加成本。
- **验证了草稿-验证范式的有效性**：利用SLM的低成本草案能力和LLM的强验证能力，实现了计算-通信折中。
- **关键洞察**：TokCom为6G实现AI原生通信提供了可行路径，尤其适用于多智能体协作、任务关键型推理等场景。

### 7. 优点
- **创新性强**：首次将token作为通信基本单元，本质上是LLM与通信系统的深度融合，而非简单叠加。
- **架构设计完整**：涵盖了多模态token化、生成式可靠性、分布式KV缓存、令牌级验证、智能调度等关键组件，提出了从物理层到应用层的系统性方案。
- **案例实验具说服力**：通过在通用NLP基准上的对比，直观展示了准确率与效率的平衡，凸显了TokCom在降低计算负载的同时保持高精度的优势。
- **实践导向**：讨论了KV缓存爆炸、非确定性延迟等工程挑战，并给出具体解决方法（如动态管理、预调度）。
- **前瞻性强**：提出了开放研究方向（多agent共识、令牌原生协议、安全隐私、标准化等），为后续研究指明方向。

### 8. 不足与局限
- **实验覆盖有限**：
  - 仅测试了语言模型对语言的交互，未涉及多模态token（如图像、音频）的实际传输。
  - 未在真实无线信道条件（衰落、干扰、延迟等）下评估，缺乏通信性能指标（如端到端延迟、吞吐量、中断概率）。
  - 未与现有语义通信（SemCom）或传统数字通信（如比特级传输）进行性能对比。
  - 仅使用一对模型，未扩展到多个异构agent的动态协作，也未考虑模型间token表示不兼容的情况。
- **方法依赖性**：严重依赖共享tokenizer（本文中两模型tokenizer相同），实际系统中不同模型可能使用不同分词器，需要对齐机制，论文未讨论。
- **安全性分析不足**：虽然提及了token拦截、隐私泄露风险，但未实验验证防御方案的有效性。
- **缺乏消融实验**：未单独评估生成式恢复、令牌级验证、KV缓存管理等模块的贡献，无法确定各部分重要性。
- **计算开销未量化**：分布式KV缓存、令牌验证等机制会引入额外计算与通信开销，论文未给出具体数值或权衡分析。
- **标准化与实用性**：作为6G愿景，缺乏与3GPP现有架构的兼容性讨论，也缺少实际的协议设计草案。

（完）
