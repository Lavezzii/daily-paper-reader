---
title: Deep-Unfolded Accelerated Projected Gradient for Energy-Efficient Cell-Free Massive MIMO
title_zh: 面向高能效无小区大规模MIMO的深度展开加速投影梯度
authors: "Phuong Nam Tran, Nhan Thanh Nguyen, Hien Quoc Ngo, Markku Juntti"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03237v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 面向小区无巨量MIMO功率分配的深度展开神经网络结构
tldr: 针对无蜂窝大规模MIMO下行链路能效最大化问题，在服务质量与接入点功率约束下，推导了目标函数关于功率分配系数的闭式梯度，并提出加速投影梯度（APG）方法。为降低计算复杂度，将APG迭代映射为深度展开网络，步长与惩罚系数从数据中学习。该方法无需线搜索与手动调参，以固定次数梯度更新获得功率分配方案，数值结果表明其能效与迭代APG相当，但浮点运算量降低达30倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03237v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 926, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03237v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1873, \"height\": 555, \"label\": \"Figure\"}]"
motivation: 现有迭代APG算法计算复杂度高、运行时间长，且需手动调参，难以满足实际部署需求。
method: 提出深度展开的APG框架，将迭代更新映射为神经网络层，学习步长与惩罚系数，以固定次数梯度更新求解功率分配。
result: 所提方法能效与迭代APG相当，浮点运算量最多降低30倍，显著降低计算成本。
conclusion: 深度展开APG在保持能效性能的同时大幅降低复杂度，为无蜂窝大规模MIMO能效优化提供高效可行方案。
---

## 摘要
本文研究了在服务质量与每接入点功率约束下，无小区大规模多输入多输出系统下行链路的能效（EE）最大化问题。我们首先推导了目标函数关于功率分配系数的闭式梯度表达式，然后提出一种加速投影梯度（APG）方法来解决该问题。为了降低APG的计算复杂度和运行时间，我们提出了一种深度展开的APG框架，将迭代APG更新映射到有限数量的神经网络层，其中步长和惩罚系数等参数从数据中学习。所提出的方法通过固定次数的基于梯度的更新来产生功率分配解决方案，无需线搜索或手动参数调整。数值结果表明，该方法实现了与迭代APG方法相当的EE性能，同时显著降低了计算成本，在所考虑的系统设置下浮点运算次数减少了多达30倍。

## Abstract
This paper investigates energy efficiency (EE) maximization for the downlink of cell-free massive multiple-input multiple-output systems under quality-of-service and per-access point power constraints. We first derive closed-form gradient expressions of the objective function with respect to the power allocation coefficients, and then propose an accelerated projected gradient (APG) approach to solve this problem. To reduce the computational complexity and runtime of APG, we propose a deep-unfolded APG framework that maps iterative APG updates onto a finite number of neural network layers, where parameters such as step sizes and penalty coefficients are learned from data. The proposed approach produces power allocation solutions through a fixed number of gradient-based updates without the need for line search or manual parameter tuning. Numerical results show that the method achieves EE performance comparable to the iterative APG approach while requiring significantly lower computational cost, with up to a 30-fold reduction in floating-point operations under the considered system settings.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：无小区大规模多输入多输出（CF-mMIMO）系统通过消除小区边界，相比传统蜂窝架构可显著提升频谱效率并提供更均匀的服务质量，被认为是未来无线网络的重要架构。然而，接入点（AP）的密集部署导致网络功耗显著增加，使得能效（EE）成为关键设计目标。在此背景下，功率控制对提升CF-mMIMO系统的能效至关重要。
- **现有方法的不足**：
  - 传统功率分配方法（如SCA）虽能接近最优性能，但每轮迭代需求解凸子问题，计算复杂度高。
  - 加速投影梯度（APG）方法虽降低了单次迭代复杂度，但仍需多轮迭代，且依赖线搜索或人工参数调整，计算成本高且不稳定。
  - 已有深度学习方法多为"黑盒"直接映射，难以显式施加服务质量（QoS）和每AP功率等系统约束。
  - 现有深度展开工作未考虑QoS和每AP功率约束下的能效最大化问题。
  - 已有研究多采用MRT预编码，而性能更优的部分迫零（PZF）预编码尚未被探索。
- **核心问题**：如何在QoS约束和每AP功率约束下，高效实现CF-mMIMO下行链路能效最大化，同时大幅降低计算复杂度？
- **整体含义**：本文通过将APG迭代映射为可训练的深度展开网络，以期在保持能效性能的同时，以固定计算代价替代迭代优化过程，为CF-mMIMO系统的能效优化提供一种高效的模型驱动深度学习方法。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想
将传统的迭代APG优化算法展开为固定层数的深度神经网络：每一层对应一次APG迭代，步长和惩罚系数等关键参数从数据中学习，从而消除线搜索和手动调参需求，实现固定计算复杂度的功率分配推理。

### 2.2 问题建模与重构
- **系统模型**：下行链路CF-mMIMO，L个AP（各配备M根天线）服务K个单天线UE，采用TDD模式，利用MMSE信道估计和本地PZF预编码。
- **功率模型**：总功耗由发射功率、静态电路/前传功耗、业务相关前传功耗三部分组成。
- **优化问题(P1)**：最大化EE，受QoS约束（SEₖ ≥ Sₘᵢₙₖ）、每AP功率约束（Σρₗₖ ≤ ρₘₐₓₗ）和非负约束。
- **变量变换**：引入 θₗₖ = √ρₗₖ ≥ 0，将功率约束转换为 Σθ²ₗₖ ≤ ρₘₐₓₗ，简化SINR表达式中的平方根项。
- **惩罚处理**：将非凸QoS约束 gₖ(θ) ≤ 0 以二次惩罚项形式并入目标函数，构造问题(P2)：max f_ξ(θ) = EE(θ) − ξ·Σ[max(0, gₖ(θ))]²。

### 2.3 APG优化框架
- **APG更新规则**：利用相邻两次迭代的差捕捉最近搜索方向，加速收敛。更新过程包括构造 y⁽ⁿ⁾、计算 z⁽ⁿ⁺¹⁾ 和 v⁽ⁿ⁺¹⁾ 两个候选点、选取目标值较大者作为下一迭代点。
- **投影**：C在AP间可分离，对每AP独立投影到非负象限和功率约束球面，公式为 P_Cₗ(uₗ) = min(1, √ρₘₐₓₗ/‖ũₗ‖)·ũₗ（负项置零后归一化）。
- **闭式梯度（Theorem 1）**：推导了目标函数 f_ξ(θ) 对 θ 的闭式梯度 ∇f_ξ = ∇Ee − ξ∇Ψ，其中：
  - ∇Ee 通过商法则从EE表达式推导，涉及总功率、和SE梯度及发射功率项的梯度；
  - ∇u（和SE梯度）由各UE的SE对θ的梯度求和得到，SE梯度利用链式法则从log₂(1+SINR)推导；
  - ∇Ψ（惩罚梯度）对满足gₖ > 0的项用链式法则计算 2gₖ∇gₖ。
  - 梯度公式中的所有中间量（∇A²ₖ、∇Iₖ、∇gₖ）均给出闭式表达式，避免自动微分。

### 2.4 深度展开网络
- **网络结构**：T层网络，每层对应一次APG迭代的更新操作。层内计算 y⁽ᵗ⁾，然后基于 y⁽ᵗ⁾ 和 θ⁽ᵗ⁾ 分别计算梯度并缩放学习到的步长，投影后得到 z⁽ᵗ⁺¹⁾ 和 v⁽ᵗ⁺¹⁾。
- **关键修改**：原APG中的硬选择（比较f值取较大者）替换为可微的加权组合 θ⁽ᵗ⁺¹⁾ = wₜz⁽ᵗ⁺¹⁾ + (1−wₜ)v⁽ᵗ⁺¹⁾，其中 wₜ ∈ (0,1) 为可学习参数，保证可微性和输出可行性。
- **学习参数**：Φ = {ᾱ_{y,t}, ᾱ_{θ,t}, ξ_t, w_t}ᵀₜ₌₁，其中步长由可学习的缩放因子乘以Barzilai-Borwein（BB）估计得到，结合BB估计的自适应性和学习参数的最优性。
- **训练策略**：损失函数 L⁽ᵗ⁾ = −Ee(θ⁽ᵗ⁾) + ξ_fix·Ψ(θ⁽ᵗ⁾)，采用渐进式学习策略逐层优化（从t=1到T依次训练各层，固定前面层），提高训练稳定性。
- **初始化**：采用启发式信道相关（HCD）分配 ρₗₖ = (γₗₖ/Σᵢγₗᵢ)·ρₘₐₓₗ，即按大尺度衰落系数比例分配功率。
- **算法流程（Algorithm 1）**：预计算{sₜ}→HCD初始化θ⁽⁰⁾→对t=1到T循环：构造y⁽ᵗ⁾、计算梯度、计算自适应步长、更新z和v、加权合并→输出θ⁽ᵀ⁺¹⁾。
- **复杂度分析**：每层复杂度O(LK²)（主导项为梯度计算），总复杂度O(TLK²)，为固定值。相比之下，APG为O(I_P·I_APG·LK²)，SCA为O(I_SCA·√L·K³·LK⁴)，且后两者迭代次数随信道实现变化。

## 3. 实验设计：数据集/场景、基准与对比方法

- **系统场景**：
  - 默认设置：L = 20个AP，每个AP配备M = 4根天线，K = 6个单天线UE。
  - AP和UE随机分布在1000m×1000m的方形区域内，采用wrap-around方案。
  - 大尺度衰落模型：βₗₖ = PLₗₖ · 10^(σ_sh·zₗₖ/10)，σ_sh = 4 dB，路径损耗 PL[dB] = −30.5 − 36.7·log₁₀(dₗₖ/1m)。
  - 系统参数：带宽B = 20 MHz，相干块长度τ_c = 200，导频长度τ_p = 5，QoS目标Sₘᵢₙ = 1 bit/s/Hz，噪声系数9 dB，αₗ = 0.4，P_tc = 0.2 W，P_bt = 0.25 W/(Gbit/s)，P₀ = 0.825 W。
- **数据划分**：数据集包含800个训练、100个验证、100个测试信道实现。
- **训练配置**：网络深度T = 10，Adam优化器，训练1000 epochs，mini-batch size = 32，学习率0.01，λ₀ = 0.3，ξ_fix = 10。
- **Benchmark数据集**：使用的是仿真合成数据（基于随机部署和信道模型生成），没有采用如Rayleigh衰落等开源标准数据集。
- **对比方法**：
  - HCD启发式功率分配方法。
  - 基于回溯线搜索的迭代APG方法（步长由回溯线搜索确定）。
  - 固定步长的APG变体（步长设为回溯APG的平均值）。
  - 所有方法均在PZF和MRT两种预编码方案下评估。

## 4. 资源与算力

- **论文未明确说明**：原文未提及所使用的GPU型号、数量、训练时长等具体算力信息。
- **可推断的线索**：模型规模较小（T = 10层，每层O(LK²)复杂度），数据集规模为1000个样本，训练1000 epochs，推测算力需求不大，消费级GPU即可完成，但具体信息无从考证。

## 5. 实验数量与充分性

### 5.1 实验组数
论文包含三组主要实验结果：
1. **收敛性对比**（Fig. 2a）：对比不同系统规模 (L=20,K=6) 和 (L=40,K=12) 下，所提展开APG与迭代APG方法的目标值随迭代/层数的变化。
2. **EE与最大发射功率的关系**（Fig. 2b）：对比所提方法、迭代APG（固定T=10）和HCD在不同发射功率水平下的EE，均覆盖PZF和MRT两种预编码。
3. **计算复杂度对比**（Fig. 2c）：不同系统规模 (L,K) = (20,6), (30,9), (40,12), (50,15), (60,18) 下的FLOPs比较。对于L = {30,40,50,60}，网络层数分别设置为T = {15,20,25,30}。

### 5.2 充分性与公平性评估
- **优点**：
  - 对比了多种基准（HCD、回溯APG、固定步长APG），并在两种预编码方案下验证，覆盖面较广。
  - 复杂度比较中保证了迭代APG与展开方法达到相当EE性能，使比较更为公平合理。
  - 实验覆盖了不同系统规模，验证了方法的可扩展性。
- **不足**：
  - **缺少消融研究**：未对网络层数T、学习参数Φ（如wₜ的初始值或约束）、损失函数中各部分权重、BB步长估计的贡献等进行消融分析。
  - **缺少与SCA等经典优化方法的直接对比**：仅对比了APG变体和HCD，未与文中所引用的SCA方法直接在性能和复杂度上做数值比较。
  - **缺少对QoS约束满足情况的定量分析**：仅通过损失函数中的惩罚项隐含处理，未报告不同方法下QoS违规率或裕量。
  - **没有真实信道数据集验证**，仅使用仿真合成数据。
  - 实验样本量较小（100个测试样本），统计显著性未报告。

## 6. 论文的主要结论与发现

- **收敛性**：所提深度展开APG的目标值快速上升，在10层内即可达到与迭代APG收敛时相当的EE性能，且优于固定步长APG（需60+次迭代）和回溯APG（约30次）在相同迭代次数下的表现。
- **EE性能**：展开APG在所有发射功率水平下均优于APG（同10次迭代）和HCD；PZF预编码相比MRT显著提升EE（在ρ_max = 25 dBm时增益约198%）；EE随最大发射功率先增后饱和或下降，最佳发射功率约在20–25 dBm，凸显功率控制的重要性。
- **计算复杂度**：展开APG在所有系统规模下均显著降低FLOPs；在(L,K) = (20,6)时迭代APG所需的FLOPs约为展开方法的30倍；随着系统规模增大，复杂度差距进一步扩大，展示出良好的可扩展性。
- **总体结论**：深度展开APG在保持与迭代APG相当能效性能的同时，实现了固定且显著更低的计算代价，为CF-mMIMO能效优化提供了高效方案。

## 7. 优点

- **闭式梯度推导**：给出目标函数梯度的完整闭式表达式（Theorem 1及附录证明），避免自动微分的额外开销，提高计算效率。
- **深度展开范式应用得当**：将APG映射为可训练网络，用学习参数替代手动调参和线搜索，保留了优化算法的结构可解释性，且能显式施加功率约束（通过投影层）。
- **可微化非光滑操作**：将原APG不可微的硬比较选择替换为可学习的加权组合，既保证了可端到端训练，又确保输出落在可行集内。
- **BB步长先行估计 + 可学习缩放因子**：结合经典优化中的Barzilai-Borwein步长和深度学习的自适应能力，既利用问题结构又提供灵活性。
- **渐进式训练策略**：逐层训练提高训练稳定性，改善了深度展开网络常见的训练困难。
- **复杂度分析完整**：给出网络、APG和SCA三者的复杂度对比，理论支撑明确。
- **固定推理复杂度**：网络深度固定，推理时计算代价可预测，适合实际部署。

## 8. 不足与局限

- **仿真验证范围有限**：仅在合成信道仿真下评估，未使用3GPP标准信道模型或实测信道数据；系统规模最大为(L=60, K=18)，未覆盖更大规模网络。
- **缺乏消融实验**：未分析网络深度T、各学习参数（步长缩放因子、加权系数、惩罚系数）的影响，也未验证逐层训练策略相比端到端联合训练的优势。
- **未报告QoS满足情况**：虽然目标中有惩罚项，但没有定量显示所提方法在不同参数设置下是否严格满足QoS约束，以及惩罚系数对约束满足度的影响。
- **未与SCA等方法直接数值比较**：文中提到SCA方法复杂度高，但未在实验中与SCA进行性能和复杂度的量化对比。
- **缺乏统计显著性分析**：100个测试样本的性能均值未报告方差或置信区间，无法判断性能差异的统计可靠性。
- **学习参数的泛化性未知**：训练基于特定范围的系统配置（如L、K、ρ_max），训练好的参数是否能够泛化到不同系统规模或信道分布尚不明确。
- **未提及训练开销**：论文只强调了推理复杂度降低，但未提及训练所需的时间和资源成本；训练开销是否值得在应用场景中权衡需要考虑。
- **方法依赖大尺度信息**：HCD初始化和网络输入依赖大尺度衰落系数和信道估计质量，在实际系统中这些信息的准确性会影响性能，但论文未对此进行鲁棒性分析。

（完）
