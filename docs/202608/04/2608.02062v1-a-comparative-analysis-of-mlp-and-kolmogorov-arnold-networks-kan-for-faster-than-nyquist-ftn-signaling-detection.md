---
title: A Comparative Analysis of MLP and Kolmogorov-Arnold Networks (KAN) for Faster-than-Nyquist (FTN) Signaling Detection
title_zh: MLP与Kolmogorov-Arnold网络（KAN）在超奈奎斯特（FTN）信号检测中的比较分析
authors: "Sude Ertan, Osman Tokluoglu, Enver Cavus"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02062v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 直接对比KAN与MLP用于FTN信号检测，契合KAN在通信中应用的需求
tldr: 针对更快奈奎斯特（FTN）传输中经典序列检测计算复杂度过高的问题，本文在时间压缩因子0.8、信噪比7-10dB的BPSK-AWGN场景下，构建近四百万带标签窗口的数据集，直接比较多层感知机与KAN两类神经网络检测器。通过宽度扫描，最优MLP隐宽32，而KAN隐宽仅4、样条网格5。在10dB时，MLP误码率为1.3e-4，KAN达到7e-6，误码率降低18.6倍，且隐宽仅为MLP的八分之一，表明KAN在FTN检测中更高效、参数效率更优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02062v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 851, \"height\": 108, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02062v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 316, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02062v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 874, \"height\": 617, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02062v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 856, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02062v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 923, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02062v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1040, \"height\": 124, \"label\": \"Table\"}]"
motivation: FTN信号检测中经典BCJR等方法随信道记忆增长计算代价高，需探索数据驱动且高效的神经网络检测方案。
method: 生成大规模蒙特卡洛FTN BPSK数据集，对MLP做宽度扫描，对KAN调整隐宽与样条网格，在7-10dB下比较误码率。
result: 10dB时KAN获得7e-6误码率，比MLP的1.3e-4低18.6倍，且KAN隐宽仅为4，远小于MLP的32。
conclusion: KAN相比MLP在FTN BPSK检测中更有效且参数效率更高，可作为轻量级神经网络检测器。
---

## 摘要
超奈奎斯特信号通过有意引入符号间干扰来提高频谱效率。经典的序列检测器如BCJR可以接近最优性能，但其计算成本随信道记忆长度快速增长。本文通过直接比较多层感知器与Kolmogorov-Arnold网络，研究AWGN下的数据驱动FTN BPSK检测。针对时间压缩因子0.8和信噪比7至10 dB，生成了包含近四百万个标记窗口的大规模蒙特卡洛数据集。通过宽度扫描获得的最佳MLP使用隐藏宽度32，而所选KAN使用隐藏宽度4且样条网格大小为5。在10 dB时，MLP的误码率为1.3×10^-4，而KAN达到7×10^-6。这对应误码率降低了18.6倍，同时仅使用MLP隐藏宽度的八分之一。结果表明，对于FTN BPSK检测，KAN比MLP基线提供了更有效且参数效率更高的神经决策模型。

## Abstract
Faster-than-Nyquist signaling improves spectral ef- ficiency by deliberately introducing inter-symbol interference. Classical sequence detectors such as BCJR can approach optimal performance, but their computational cost grows rapidly with channel memory. This paper investigates data-driven FTN BPSK detection under AWGN through a direct comparison between multilayer perceptrons and Kolmogorov Arnold Networks. A large-scale Monte Carlo dataset containing nearly four million labeled windows is generated for a time-packing factor of zero point eight and signal-to-noise ratio values from seven to ten decibels. The best MLP obtained from width sweeping uses hidden width thirty two, whereas the selected KAN uses hidden width four with spline grid size five. At ten decibels, the MLP produces a bit error rate of one point three times ten to the minus four, while the KAN reaches seven times ten to the minus six. This corresponds to an eighteen point six times lower bit error rate while using only one eighth of the MLP hidden width. The results show that KAN provides a more effective and more parameter-efficient neural decision model than the MLP baseline for FTN BPSK detection.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与整体含义

- **研究背景**：超奈奎斯特（Faster-than-Nyquist, FTN）信令通过有意压缩符号间隔（使符号速率超过奈奎斯特速率）来提升频谱效率，但代价是人为引入确定性符号间干扰（ISI），将逐符号检测转化为结构化序列估计问题。
- **核心动机**：经典最优/近优序列检测器（如 BCJR）在信道记忆长度增加时计算复杂度快速攀升，难以满足实时接收机与硬件受限平台的需求。因此，需要探索数据驱动、低复杂度的神经网络检测方案。
- **研究问题**：在相同数据集、相同输入表示和相同评估条件下，**直接且系统地比较** MLP 与 Kolmogorov-Arnold 网络（KAN）孰能更高效地建模 FTN 检测中的非线性决策边界，并评估参数效率。
- **整体含义**：本文不仅仅提出一个新的神经网络接收机，而是试图回答“哪一种网络结构更能匹配 FTN 信道的固有结构”这一架构层面的问题，为轻量级神经接收机设计提供依据。

## 2. 方法论：核心思想、关键技术细节

- **系统模型**：
  - 发送符号：BPSK 调制，\( x_k \in \{-1, +1\} \)；
  - FTN 符号间隔：\( T_{FTN} = \tau T \)，其中 \( 0 < \tau < 1 \)，本文取 \( \tau = 0.8 \)，对应 25% 的符号速率提升；
  - 发送波形：\( s(t) = \sum_k x_k g(t - kT_{FTN}) \)；
  - 接收信号：\( r(t) = s(t) + n(t) \)，\( n(t) \) 为零均值高斯噪声；
  - 离散观测模型：\( r_k = \sum_i h_{k-i} x_i + n_k \)，其中 \( h \) 为有效离散 FTN 脉冲响应。

- **检测窗口设计**：每个检测器基于长度 65 的接收向量
  \[
  \mathbf{r}_k = [r_{k-32}, \ldots, r_k, \ldots, r_{k+32}]^T
  \]
  估计中心符号 \( \hat{x}_k = \text{sign}(f_\theta(\mathbf{r}_k)) \)。

- **MLP 检测器**：
  - 结构：稠密输入层→ReLU 激活→标量输出层；
  - 宽度扫描集合 \( \{8, 16, 32, 64, 128, 256\} \)，以验证 BER 为标准选出最佳隐宽 32；
  - 参数量：\( P_{MLP} = 65 \times 32 + 32 + 32 \times 1 + 1 = 2145 \)。

- **KAN 检测器**：
  - KAN 的核心思想是将非线性置于**边**（edge）上而非节点上，每个连接配备可学习的 B 样条函数：
    \[
    y_j = \sum_i \phi_{ij}(x_i), \quad \phi_{ij}(x) = \sum_m c_{ijm} B_m(x)
    \]
  - 考察两种配置：w4_g5（隐宽 4，样条网格 G=5）和 w8_g3（隐宽 8，网格 3）；
  - 最终选择兼顾验证 BER 与参数紧凑性的 w4_g5，参数量约 3920。

- **参数效率指标**：定义
  \[
  \eta = \frac{\text{BER 改善倍数}}{\text{参数增加倍数}}
  \]
  在 10 dB 下，\( \eta = 18.6 / 1.83 \approx 10.2 \)，即每单位参数增加可获得约 10 倍的 BER 改善。

- **训练设置**：两种网络均使用二元交叉熵损失与 Adam 优化器，在同一数据划分、同一 SNR 值、同一输入表示下训练与评估。

## 3. 实验设计

- **数据集**：通过蒙特卡洛方法在 AWGN 信道下生成大规模 FTN-BPSK 数据集，总窗口数 **3,999,936**；
  - 训练集：2,799,955 个窗口；
  - 验证集：599,990 个窗口；
  - 测试集：599,991 个窗口。
- **场景参数**：
  - 调制：BPSK；信道：AWGN；
  - FTN 因子 \( \tau = 0.8 \)，速率提升 \( 1/\tau = 1.25 \)；
  - 窗口长度：65 个样本；
  - SNR 值：7、8、9、10 dB。
- **对比方法**：
  - FTN 硬判决基线（无 ISI 处理）；
  - 最佳 MLP（宽度 32，w32）；
  - 所选 KAN（w4_g5）；
  - BCJR 参考检测器（近最优序列检测）。

- **Benchmark 设定**：在同一数据划分和数据表示下直接比较所有模型，BCJR 作为性能上界参考。

## 4. 资源与算力

- 论文中**未明确说明**所用的 GPU 型号、数量、训练时长或具体硬件平台。
- 作者仅提到使用 Adam 优化器训练 MLP 与 KAN，以及为 MLP 进行了 6 种宽度的扫描、为 KAN 测试了 2 种配置，但未报告单次训练时间或总计算开销。
- 可以推断：由于数据规模约 400 万样本且模型较小（参数量为数千级），整体训练成本在合理范围内，但论文未提供可复现训练成本的量化信息。

## 5. 实验数量与充分性

- **实验规模**：在大规模数据集（约 400 万窗口）上进行，覆盖 4 个 SNR 点，对比 4 种检测方案（基线、MLP、KAN、BCJR），保证了统计可靠性。
- **模型选择实验**：
  - MLP 进行了 6 档宽度扫描（8~256）；
  - KAN 仅测试了 2 种配置（w4_g5 与 w8_g3），没有对样条网格大小或层数做更系统的扫描。
- **公平性**：两者在相同数据划分、相同窗口表示、相同 SNR 和相同损失/优化器下评估，比较条件受控，公平性较好。
- **充分性评价**：
  - **优点**：数据量大、条件统一、BCJR 参考明确；
  - **不足**：KAN 的超参数搜索较浅；仅 AWGN 信道和单一 \( \tau=0.8 \)；仅 BPSK 调制，未覆盖更高阶调制；未做针对深度、激活函数、正则化等的更广消融实验。

## 6. 主要结论与发现

1. **KAN 全面优于 MLP**：在所有 SNR 值（7~10 dB）下，所选 KAN（w4_g5）的 BER 均显著低于最佳 MLP（w32）。
2. **KAN 相对 MLP 的 BER 改善倍数**：约 2.3×（7 dB）、2.7×（8 dB）、2.7×（9 dB）、**18.6×（10 dB）**。
3. **达到 BCJR 参考性能**：在 10 dB 时，KAN 的 BER 为 \( 7.0 \times 10^{-6} \)，与 BCJR 参考（\( 7.0 \times 10^{-6} \)）完全一致，说明 KAN 可以不依赖序列估计算法直接逼近最优检测性能。
4. **参数效率突出**：KAN 仅使用 MLP 隐藏宽度的 1/8（4 vs. 32），参数总量仅约为 MLP 的 1.83 倍，却带来 18.6 倍的 BER 改善；每单位参数增加的 BER 改善约 10.2 倍。
5. **高 SNR 区域差异更明显**：MLP 随 SNR 升高趋于饱和，而 KAN 的 BER 持续快速下降，说明 KAN 的样条边缘函数能更精细地建模 ISI 决策边界。
6. **结构解释**：KAN 的边基参数化与 FTN 信道中相邻符号的局部、结构化干扰模式更匹配，是其效率优势的结构性来源。

## 7. 优点

- **直接且受控的比较**：在同一数据集、同一窗口表示、同一 SNR、同一损失/优化器下进行 MLP 与 KAN 的直接对比，结论可信度高。
- **大规模数据集**：近 400 万个标记样本，统计稳定性和泛化可靠性较好。
- **引入明确参数效率度量**：通过 \( \eta = \text{BER改善}/\text{参数增加} \) 定量刻画“性能增益 vs. 参数代价”，比仅看 BER 更具说服力。
- **以 BCJR 为参考上界**：将神经网络检测器性能与经典近最优检测器对齐，使“KAN 达到 BCJR”这一结论具有很强的实践意义。
- **文章结构清晰**：从数学建模、架构分析到实验验证，逻辑链条完整；表格与图（BER-SNR 曲线）数据支撑充分。

## 8. 不足与局限

- **信道模型单一**：仅考虑 AWGN 信道，未涉及多径衰落、时变信道等更贴近实际通信场景的条件；作者也将其列为未来工作。
- **调制方式有限**：仅 BPSK，未扩展到 QPSK、QAM 等高阶调制，结论的外推性有限。
- **FTN 因子单一**：仅测试 \( \tau = 0.8 \)，不同 \( \tau \) 对应不同的 ISI 强度和决策边界复杂度，尚不清楚 KAN 的优势是否在所有 \( \tau \) 下保持。
- **KAN 超参数搜索不充分**：仅测试了两种 KAN 配置（w4_g5 与 w8_g3），未系统扫描隐宽、网格大小、层数、样条阶数等，存在“KAN 配置未調优即与最优 MLP 比较”的潜在不公平风险。
- **未报告训练成本与推理成本**：缺少训练时间、推理延迟、内存占用、FLOPs 等实际部署关心的指标；“参数效率”只衡量参数量，未覆盖计算量/延迟。
- **无统计误差信息**：未报告多次重复实验的标准差或置信区间，单次大样本评估不足以完全排除随机性。
- **依赖单一数据集配置**：所有实验基于同一 \( \tau \) 与 SNR 区间的蒙特卡洛数据，缺少跨配置泛化验证。

（完）
