---
title: "STCC: A Unified Source-Channel Semantic Token Coding Framework for Semantic Communications"
title_zh: STCC：一种用于语义通信的统一信源信道语义令牌编码框架
authors: "Zhicheng Bao, Chen Dong, Sen Wang, Long Liu, Nan Ma, Hao Chen, Xiaodong Xu, Yinqiu Liu, Ping Zhang"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11819v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 提出用于语义通信的统一源信道语义令牌编码框架，是深度学习在通信中的应用
tldr: 深度学习联合源信道编码（JSCC）面临瓶颈：直接处理原始模拟数据，无法与基础模型的离散语义token兼容；传统固定星座图平等对待所有token，信道噪声导致随机错误。本文提出STCC框架，核心是语义token编解码器STC，利用残差MLP编码器学习几何结构的星座图，并通过三重损失函数优化，使信道拓扑与语义嵌入空间对齐。实验表明，STCC在低信噪比下显著优于传统方案，将信道噪声转化为语义相似的拓扑错误（如语义漂移或结构扭曲），无需修改接收端即可有效抗噪。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11819v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1858, \"height\": 1118, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11819v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1783, \"height\": 1321, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11819v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 851, \"height\": 1622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11819v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1781, \"height\": 1658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11819v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1855, \"height\": 1887, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11819v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 889, \"height\": 1064, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11819v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1866, \"height\": 553, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11819v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11819v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 671, \"height\": 250, \"label\": \"Table\"}]"
motivation: 现有Deep JSCC处理原始模拟数据而非离散语义token，传统星座图平等对待所有token，导致信道噪声下随机错误。
method: 提出STCC框架，核心为STC，采用残差MLP编码器学习几何结构星座图，通过三重损失函数优化使信道拓扑对齐语义嵌入空间。
result: 在低信噪比下显著优于传统系统，信道噪声转化为拓扑错误（语义漂移或结构扭曲），而非随机破坏。
conclusion: STCC实现统一的源信道语义token编码，无需修改接收端即可有效对抗信道噪声。
---

## 摘要
深度联合信源信道编码（Deep JSCC）已成为克服无线通信中“悬崖效应”的一种有前景的范式。然而，现有的Deep JSCC框架直接对原始模拟数据（如图像像素）进行操作，而不是基础模型所需的离散语义令牌。此外，传统系统采用固定的、手工设计的星座图，平等对待所有令牌，导致在信道噪声下出现灾难性的随机错误。本文提出语义令牌码本通信（STCC），作为一种统一的信源信道语义令牌编码框架，旨在通过噪声信道传输基础模型的离散语义令牌。STCC的核心是语义令牌编解码器（STC）。它接受离散令牌作为输入，保持与基础模型的兼容性，同时采用基于残差多层感知器（即MLP）的编码器，学习几何结构化的星座图，并通过三重损失目标进行优化。这种学习映射迫使信道拓扑与语义嵌入空间对齐，确保信道噪声导致拓扑错误而非随机破坏。本文从理论和实验上表征了这一现象，识别出符号模态中的“语义漂移”和感知模态中的“结构扭曲”，其中错误将预测转移到语义或结构相似的令牌。大量实验表明，STCC在低信噪比区域显著优于传统系统，有效将信道噪声转化为语义变化，且无需修改接收端。

## Abstract
Deep Joint Source-Channel Coding (JSCC) has emerged as a promising paradigm for overcoming the ``cliff effect" in wireless communications. However, existing Deep JSCC frameworks operate directly on raw analog data such as image pixels rather than the discrete semantic tokens that foundation models require. Moreover, traditional systems employ fixed, hand-designed constellations that treat all tokens equally, leading to catastrophic random errors under channel noise. In this paper, the Semantic Token Codebook Communication (STCC) is proposed as a unified source-channel semantic token coding framework designed to transmit the discrete semantic tokens of foundation models over noisy channels. The core of STCC is the Semantic Token Codec (STC). It accepts discrete tokens as input, which maintains compatibility with foundation models while employing a residual multiple layer perceptron, i.e., MLP-based encoder that learns geometrically structured constellations optimized with a triple-loss objective. This learned mapping forces the channel topology to align with the semantic embedding space, ensuring that channel noise results in topological errors rather than random corruption. This phenomenon is theoretically and empirically characterized, identifying ``Semantic Drift" in symbolic modalities and ``Structural Distortion" in perceptual modalities, where errors shift predictions to semantically or structurally similar tokens. Extensive experiments demonstrate that STCC significantly outperforms traditional systems in low-SNR regimes, effectively converting channel noise into semantic variations without requiring receiver-side modification.

---

## 论文详细总结（自动生成）

# 论文总结：STCC: A Unified Source-Channel Semantic Token Coding Framework for Semantic Communications

## 1. 核心问题与整体含义
- **研究动机**：现有深度联合信源信道编码（Deep JSCC）直接处理原始模拟数据（如图像像素），无法与当前主流基础模型（如BERT、VQ-GAN）的离散语义令牌接口兼容；传统分离式通信系统使用固定星座图（如QPSK），平等对待所有令牌，导致信道噪声下出现灾难性随机错误（“悬崖效应”）。
- **整体含义**：本文提出语义令牌码本通信（STCC）框架，核心是语义令牌编解码器（STC），旨在构建一个统一的源信道编码方案：既能接收离散令牌（兼容基础模型），又能通过学习几何结构化星座图将信道拓扑与语义嵌入空间对齐，从而将随机信道噪声转化为有界的语义相似变化（如近义词替换或视觉相似纹理替换），而非随机乱码。

## 2. 方法论
- **核心思想**：将离散语义令牌通过可学习的神经网络映射为连续信道符号，并使符号间角度距离反映语义相似度，从而确保噪声导致的解码错误落在语义/结构近邻内，而非随机索引点。
- **关键技术细节**：
  - **语义令牌编码器**：采用残差MLP（5个残差块，隐藏维512），将高维令牌嵌入映射为2L维实数向量（对应L个复数信道符号），最后通过LayerNorm强制满足平均功率约束。
  - **语义令牌解码器**：4层Transformer编码器（模型维512，8注意力头），对噪声序列进行上下文感知处理，输出恢复的语义嵌入；再利用余弦相似度最近邻搜索回离散令牌。
  - **三重损失函数**：
    - 交叉熵损失（L_CE）：以低温度参数（τ<1）迫使输出分布尖锐，防止“语义坍缩”，使解码器指向精确令牌。
    - 均方误差损失（L_MSE）：约束全局能量，防止编码器人为放大功率以对抗噪声。
    - 余弦嵌入损失（L_Cos）：最小化收发嵌入间的角度偏差，使语义相似性体现在方向而非幅度上。
    - 总损失：L_STC = L_CE + λ1 L_MSE + λ2 L_Cos（文中λ1=10.0, λ2=2.0）。
- **信道模型**：AWGN和Rayleigh衰落信道，符号满足平均功率约束。训练时SNR从U(5,15) dB随机采样。
- **信息流程**：离散令牌 → 嵌入层（查表） → 编码器（残差MLP）→ 信道 → 解码器（Transformer）→ 最近邻检索 → 离散令牌。

## 3. 实验设计
- **数据集**：
  - 文本：Wikitext-103，序列长度64。
  - 图像：ImageNet，图像resize至256×256，使用MaskGIT编码为离散网格（码本1024×256）。
- **基础模型**：
  - 文本：BERT-base（WordPiece tokenizer，码本30522×768）。
  - 图像：MaskGIT（VQ-GAN）。
- **对比方法**：
  - 传统分离系统：1/2 LDPC + BPSK（高带宽L=30）和1/2 LDPC + QPSK（低带宽L=15）。
  - Deep JSCC基线：标准JSCC（直接映射连续符号，仅用MSE损失训练）。
- **评估指标**：
  - 文本：token准确率、BLEU-4分数、语义相似度（余弦相似度）。
  - 图像：token准确率、PSNR、LPIPS（感知相似度）。
  - 下游任务：SST-2情感分类准确率（解码文本上）、ImageNet Top-1分类准确率（重建图像上）。
- **信道条件**：AWGN和Rayleigh衰落，SNR范围0–20 dB。
- **带宽配置**：高带宽L=30（对比BPSK），低带宽L=15（对比QPSK）。另做L=16/20/24/28/32的消融。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量及具体训练时长。
- 仅提供了模型复杂度：5个残差块+4层Transformer，总参数量2.85M，MACs 0.73G（约是ResNet-18的18%计算量和25%参数量）。
- 训练超参数：batch size=32，AdamW优化器，学习率5e-5。
- **需指出**：缺乏详尽资源报告，影响可重复性。

## 5. 实验数量与充分性
- **实验组数**：覆盖两大类模态（文本、图像），两种信道（AWGN、Rayleigh），多个SNR点（通常0–20 dB），两个带宽配置，以及下游任务性能、带宽消融、复杂度分析。还包括定性分析（UMAP投影、相似度矩阵、误码模式可视化）。
- **充分性与公平性**：对比方法在相同功率约束和带宽下进行。实验结果图表清晰，趋势一致。不足的是未进行组件消融（如移除三重损失中某一项）的量化实验，但理论分析部分论证了各损失的作用。整体实验设计较全面，结论可靠。

## 6. 主要结论与发现
- STC在低SNR（如0 dB）下显著优于传统LDPC+BPSK/QPSK和JSCC基线，消除了“悬崖效应”；如文本在高带宽下0 dB准确率86.91%，传统方案接近0%。
- STC错误模式为拓扑错误：文本中表现为“语义漂移”（误码为同义词如“chickens”→“birds”），图像中表现为“结构扭曲”（误码为视觉相似的patch）。
- 下游任务（SST-2情感分析、ImageNet分类）在低SNR下仍保持有效性能（0 dB SST-2准确率65.55% vs 传统9.08%）。
- 增大带宽L能有效提升抗噪声能力，特别是在极低SNR（-5 dB）下增益显著。
- STC模型轻量（2.85M参数），适合边缘部署。

## 7. 优点
- **方法创新**：首次提出将离散语义令牌通过拓扑保持的星座图学习直接映射到连续信道，实现与基础模型原生兼容且具有JSCC鲁棒性。
- **理论贡献**：严格区分了“语义漂移”和“结构扭曲”两种错误类型，并从角度拓扑角度解释了其成因。
- **实验全面**：涵盖文本和图像两种异质模态，验证了通用性；同时进行了下游任务评估，证明实用性。
- **高效轻量**：模型参数量和计算量远低于传统CNN骨干，适合实时或资源受限系统。

## 8. 不足与局限
- **实验局限**：未与更多当前的离散语义通信方案（如sDAC、SemPA-GBeam等）进行直接对比，仅对比了基础LDPC+调制和简单JSCC。
- **泛化性**：仅在BERT和MaskGIT上进行验证，对其他基础模型（如GPT、DALL-E）的适用性未讨论。
- **超参数依赖性**：三重损失权重λ1、λ2需手动设定，文中未提供自动调节或敏感度分析。
- **资源公开不足**：缺少详细的硬件配置和训练时长，影响可重复性和实际部署参考。
- **信道模型简化**：仅考虑AWGN和简单Rayleigh衰落，未涉及多用户干扰、时变性、多径等更复杂场景。
- **缺乏消融实验**：未量化删除各损失项的影响，对三重损失的必要性证据依赖理论论述而非实证对比。

（完）
