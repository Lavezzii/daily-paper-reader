---
title: Double-Directional Wireless Channel Modeling Using Statistics-Aided Machine Learning
title_zh: 基于统计辅助机器学习的双向无线信道建模
authors: "Richmond Boamah, Ferdous Pervej"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05993v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 统计辅助机器学习用于双方向无线信道建模
tldr: 双方向信道模型对系统设计至关重要，但现有机器学习方法因要求固定输入输出形状且局限于短时间跨度而难以捕捉完整信道统计特性。本文提出统计数据辅助的机器学习方法，通过选择固定数量的top-M多径分量并构建可学习图，训练混合TimesNet-TimeFilter模型。利用信道统计数据辅助训练生成未来信道实现，使生成实现的统计量接近真实信道统计。在合成随机信道模型和确定性射线追踪数据集上的仿真表明，该方法优于现有基线，为长时统计显著的信道建模提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05993v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 918, \"height\": 1511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05993v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1866, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05993v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1877, \"height\": 628, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05993v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1872, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05993v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1880, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05993v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 804, \"height\": 336, \"label\": \"Table\"}]"
motivation: 现有机器学习信道建模方法受限于短时间跨度和固定输入输出形状，难以处理多径分量数量时空变化的问题，无法生成统计显著的信道实现。
method: 提出统计数据辅助的机器学习方法：选择top-M个多径分量构建可学习图，训练混合TimesNet-TimeFilter模型，并利用信道统计量辅助训练生成未来信道实现。
result: 在合成随机信道模型和确定性射线追踪数据集上，该方法生成的信道实现统计量与真实信道统计量吻合，性能优于现有基线方法。
conclusion: 该统计数据辅助机器学习方法有效解决了双方向信道建模中多径分量数量变化和长时统计显著性问题，提升了信道生成的统计准确性。
---

## 摘要
双向（DD）无线信道模型对于实际系统设计非常重要，因为它提供了完整的传播信息。虽然随机性和确定性信道模型被广泛采用，并且现有的机器学习（ML）解决方案大多旨在对齐未来的信道实现，但这些解决方案通常限于统计上可能不显著的时间跨度。此外，由于多径分量（MPC）的数量随接收器（RX）和/或交互对象（IO）的空间和时间变化而变化，典型的需要固定、预定义输入和输出形状的ML解决方案存在不足。为了克服这些限制，我们提出了一种基于固定MPC子集选择的统计辅助ML解决方案。更具体地说，我们首先选择前M个MPC，其中M∈Z^+远小于MPC的总数，并构建可学习图来训练我们提出的混合TimesNet-TimeFilter（TNTF）模型。然后，我们使用信道统计辅助训练方法生成未来的前M个DD信道实现，使得从这些实现计算出的统计量与来自完整时变DD信道实现的实际统计量紧密匹配。我们通过对基于合成随机信道模型（SCM）和确定性射线追踪的数据集进行大量仿真来验证所提出的解决方案，并证明其相对于最先进基线的有效性。

## Abstract
The double-directional (DD) wireless channel model is important for realistic system design since it provides complete propagation information. While stochastic and deterministic channel models are widely adopted, and existing machine learning (ML) solutions mostly aim to align future channel realizations, these solutions are often limited to short time spans that may not be statistically significant. Moreover, because the number of multi-path components (MPCs) varies with spatial and temporal variation of the receiver (RX) and/or interacting objects (IOs), typical ML solutions that require fixed, predefined input and output shapes fall short. To curb these limitations, we propose a statistics-aided ML solution that relies on a fixed subset of MPCs selection. More specifically, we first select top-$M$ MPCs, where $M\in\mathbb{Z}^+$ is much smaller than the total number of MPCs, and construct learnable graphs to train our proposed hybrid TimesNet-TimeFilter (TNTF) model. We then use a channel statistics-aided training method to generate future top-M DD channel realizations such that the statistics calculated from these realizations matches closely with those of the actual statistics from the complete time-varying DD channel realizations. We validate the proposed solution using extensive simulations on both synthetic stochastic channel model (SCM)-based and deterministic ray-tracing-based datasets, and demonstrate its effectiveness relative to state-of-the-art baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：双向（DD）无线信道模型对系统设计至关重要，但传统随机性和确定性信道模型各有局限（随机模型忽略环境几何，确定性模型计算量大）。现有机器学习（ML）方法多用于短时预测或生成，难以捕捉长期信道统计特性；且多径分量（MPC）数量随接收端/交互对象时空变化，导致ML模型因固定输入/输出形状而无法处理。
- **整体含义**：提出一种统计辅助的ML解决方案，通过选择固定数量的top-M MPCs，结合图神经网络和混合时序模型，生成长期（统计显著）的未来DD信道实现，使其统计量接近真实信道统计量，从而在实用性和准确性之间取得平衡。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 由于完整DD信道实现中MPC数量不固定，先提取top-M MPCs（M远小于总MPC数），构造近似信道张量；利用传播知识进行参数特定的分块（patching），构建可学习图（基于曼哈顿距离和KNN）。
  - 设计混合TimesNet-TimeFilter（TNTF）模型，结合TimesNet（二维时频变换）和TimeFilter（图滤波器）的优势，学习MPC特征的时域、空域及时空相关性。
  - 采用统计辅助训练方法：损失函数不仅包含预测误差，还直接匹配由完整信道计算出的统计量（时延扩展、角度扩展、增益等），使生成的top-M实现与实际统计量一致。

- **关键技术细节**：
  - **数据准备**：将历史序列中的每个时间点（轨迹点）的MPC参数按类型分7个patch（位置/总增益、增益、时延、DoD方位角、DoD仰角、DoA方位角、DoA仰角），计算曼哈顿距离矩阵，经GeLU和KNN得到邻接矩阵。
  - **模型架构**：补丁嵌入 → 多头注意力计算可训练邻接矩阵 → 与固定邻接矩阵融合 → 频域分析（FFT将1D序列转为2D，参数inception块学习2D模式，再聚合回1D） → 构建ego-graph并分割为时、空、时空子图，用混合专家（MoEs）分配滤波器 → 重构稀疏图，经GNN消息传递得到图级预测 → 与原始嵌入及Ha残差相加，层归一化后经前馈网络输出。
  - **损失函数**：$\phi(s,\hat{s},g,\hat{g}) = \phi(s,\hat{s}) + \phi(g,\hat{g}) + \lambda_1 L_{dyn} + \lambda_2 L_{imp}$，其中$\phi(s,\hat{s})$为各统计量（时延扩展、角度扩展）的加权均方误差，$\phi(g,\hat{g})$为top-M增益的均方误差。后两项$L_{dyn}$和$L_{imp}$来自TimeFilter文献。
  - **训练**：使用AdamW优化器，小批量SGD，训练250 epoch。

## 3. 实验设计：数据集、场景、基准与对比方法

- **数据集**：
  - **合成SCM数据集**：基于随机信道模型生成1M样本，接收器轨迹为螺旋线，MPC由泊松点过程生成，强度0.0004，载频2.4 GHz，0 dBm功率，偶极子天线。
  - **射线追踪数据集**：基于OpenStreetMap、Blender和Sionna构建USU附近真实环境，生成两种类型轨迹（螺旋形和正弦形），同样1M样本。
  - 划分：75%训练、5%验证、25%测试。

- **对比方法（baselines）**：
  - Transformer (T)：T(P)（预测损失）和T(S)（统计损失）
  - Hybrid BiLSTM-Transformer (BT)：BT(P)和BT(S)
  - TimesNet (TNet)：TNet(P)和TNet(S)
  - TimeFilter (TF)：TF(P)和TF(S)
  - 提出的TNTF：TNTF(P)和TNTF(S)

- **实验场景**：
  - 不同M（5,10,15）对性能影响（表I）
  - 不同预测长度P（100,200,300）的影响（图4、5）
  - 固定L=100，对比CDF分布（图2、3）

## 4. 资源与算力

- 论文未明确说明所用GPU型号、数量、训练时长。仅提到超参数设置（如batch size 32/128/256、学习率0.0003等），未提及训练时间和硬件平台。未提供算力资源信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个数据集（合成SCM和射线追踪），每个数据集上测试了多个M、多个P，并对比了8种方法（各两种损失）的CDF和NMSE指标。
  - 表I：SCM数据集上M=5/10/15的NMSE。
  - 图2-3：CDF对比（L=100, P=300, M分别为5和2）。
  - 图4-5：不同P下的NMSE（L=100, M分别为5和2）。
  - 合成数据集结果为3次独立试验平均，射线追踪为单次运行。
- **充分性与公平性**：
  - 实验覆盖了模型关键变量（M、P），并对比了多种主流架构（Transformer、BiLSTM-Transformer、TimesNet、TimeFilter），在两种数据集上验证了统计辅助损失的有效性。
  - 超参数通过消融研究获得，各组基线使用了近似合理的配置。
  - 但缺乏更多M值（如M=1或M=20+）的测试，且仅评估了DS和AS，未涉及二阶统计量（多普勒扩展等）。射线追踪数据集只有单次结果，统计稳定性不足。另外，未与纯生成模型（如GAN）对比。

## 6. 论文的主要结论与发现

- **结论**：统计辅助训练方法显著优于传统预测方法，尤其当top-M MPCs不占主导地位时；提出的TNTF模型在CDF对齐和NMSE上均优于所有基线，对长期预测（P=300）保持稳定。
- **关键发现**：
  - 增大M可提升性能，但增益递减（表I）。
  - 统计辅助方法在不同数据集和不同P下均更鲁棒，而预测方法在长预测时性能下降明显。
  - 射线追踪数据集M=2的极端情况下，TNTF(S)仍能较好对齐真实CDF。

## 7. 优点

- **方法创新**：结合传播知识的分块策略、可学习图构建、混合TimesNet-TimeFilter架构，以及统计辅助损失函数，有效解决了变长MPC和长期统计匹配问题。
- **实验设计**：在两个异构数据集（合成+真实场景）上验证，考虑了不同M和P，对比多种模型架构，并使用统一评价指标（NMSE、CDF），体现方法通用性。
- **实用性**：仅需top-M（通常M很小）即可准确生成长期信道统计，降低计算和存储开销。

## 8. 不足与局限

- **统计量覆盖不全**：只评估了时延扩展和角度扩展（方位/仰角），未包括多普勒扩展、空间相关性等二阶统计量。
- **算力资源未说明**：无法评估方法的训练成本与可复现性。
- **射线追踪数据集实验不充分**：仅单次运行，缺乏统计可靠性；且M=2极端情况外，未测试更大M对实际场景的增益。
- **对比基线有限**：未与生成对抗网络（GAN）、扩散模型等最新生成方法对比。
- **应用限制**：假设TX固定、RX移动，未考虑多用户或MIMO场景；天线模型为全向，未扩展至定向天线。
- **缺乏消融实验**：未单独验证各模块（如分块策略、TNTF子模块、损失函数项）的贡献。

（完）
