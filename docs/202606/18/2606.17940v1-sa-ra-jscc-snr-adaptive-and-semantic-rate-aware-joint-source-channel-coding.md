---
title: "SA-RA-JSCC: SNR-Adaptive and Semantic-Rate-Aware Joint Source-Channel Coding"
title_zh: SA-RA-JSCC：信噪比自适应与语义率感知的联合信源信道编码
authors: "Shitong Zhang, Yaping Sun, Hao Chen, Xiaoyi Li, Bo Gu, Xiaodong Xu, Nan Ma"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17940v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 面向SNR自适应的深度学习联合源信道编码
tldr: 现有JSCC语义通信中信道适配模块独立逐层注入SNR，缺乏全局协调，导致难以学习一致的噪声鲁棒表示。为此提出SA-RA-JSCC，将SNR映射为特征空间的统一语义向量，并一次性全局重加权编码特征，实现全局可学习的信道适配；同时引入语义率感知模块，使自适应策略同时响应信道质量和语义率变化。在多个信道和数据集上，该方法在PSNR和MS-SSIM等重建指标上显著优于现有模型，且在不同SNR范围内表现出更强鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17940v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1786, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17940v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1823, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17940v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 901, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17940v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1814, \"height\": 1004, \"label\": \"Figure\"}]"
motivation: 现有信道适配逐层独立注入SNR，缺乏全局协调，模型难以学到一致噪声鲁棒表示。
method: 提出SA-RA-JSCC，将SNR映射为统一语义向量进行全局重加权，并引入语义率感知模块协同优化。
result: 在多种信道和数据集上，PSNR和MS-SSIM显著优于现有语义通信模型，鲁棒性更强。
conclusion: 全局信道适配与语义率感知结合，有效提升了语义通信的抗信道能力和重建质量。
---

## 摘要
在基于联合信源信道编码（JSCC）的语义通信系统中，在信道约束下实现稳定可靠的图像语义传输仍是关键挑战。在大多数信道自适应模块中，信噪比（SNR）通常以独立逐层的方式注入信道自适应模型的每一层，这破坏了层间的全局协调。因此，整个模型可能无法学习到一致的噪声鲁棒表示。为解决这一问题，我们提出SA-RA-JSCC，一种新颖的信道自适应JSCC模型。SA-RA-JSCC将SNR映射到特征空间中的统一语义向量，然后对编码特征进行一次性全局重加权，从而实现全局一致且可学习的信道自适应。此外，为进一步增强语义信息的抗信道能力，引入了语义率感知模块，使自适应策略能够同时响应信道质量波动和语义率约束变化，从而增强全局网络协调性和信道自适应性。在多个信道和数据集上的大量实验结果表明，SA-RA-JSCC在PSNR和MS-SSIM等重构指标上显著优于现有语义通信模型，在广泛的SNR范围内表现出更强的鲁棒性。

## Abstract
In joint source-channel coding (JSCC)-based semantic communication systems, achieving stable and reliable image semantic transmission under channel constraints remains a key challenge. In most channel adaptation modules, the signal-to-noise ratio (SNR) is often injected into each layer of a channel-adaptation model in an independent and layer-wise manner, which undermines global coordination across layers. Therefore, consistent noise-robust representations may fail to be learned throughout the model. To address this problem, we propose SA-RA-JSCC, a novel channel-adaptive JSCC model. SA-RA-JSCC maps SNR into a unified semantic vector in the feature space and then applies a one-shot global reweighting to the encoded features, thereby enabling globally consistent and learnable channel adaptation. Moreover, in order to further enhance the anti-channel capability of semantic information, a semantic-rate-aware module is introduced, enabling the adaptive policy to respond simultaneously to fluctuations in channel quality and changes in semantic-rate constraints, thereby enhancing global network coordination and channel adaptivity. Extensive experiment results across multiple channels and datasets demonstrate that SA-RA-JSCC significantly outperforms existing semantic communication models in terms of reconstruction metrics such as PSNR and MS-SSIM, exhibiting stronger robustness across a broad range of SNR regimes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有基于联合信源信道编码（JSCC）的语义通信系统中，信道自适应（CA）模块通常采用**逐层独立注入**信噪比（SNR）的方式，使得各层之间缺乏全局协调，导致模型难以学习到一致的噪声鲁棒表示，限制了重构质量。
- **核心问题**：如何设计一种全局一致、可学习的信道自适应机制，同时融合语义率（semantic rate）信息，以提升模型在时变信道下的鲁棒性和重构保真度。
- **整体含义**：论文提出了一种新的信道自适应JSCC模型——**SA-RA-JSCC**，通过将SNR映射为统一语义向量并一次性全局重加权编码特征，再结合语义率感知模块，实现更协调的自适应策略，旨在超越现有逐层注入方案的性能上限。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  - 将SNR和语义率分别通过独立的预处理模块映射为特征空间中的权重向量，然后对编码特征进行**一次性全局重加权**，而非逐层注入。
  - 引入**语义率感知模块**，通过码掩码（code mask）机制选择与语义率最相关的通道，使自适应策略能同时响应信道质量和语义率变化。

- **关键技术细节**：
  1. **SNR自适应模块**：
     - 预处理模块：`S_mi = Sigmoid( W(3)·ReLU(W(2)·ReLU(W(1)·γ_I + b(1)) + b(2)) + b(3) )`，其中γ_I为SNR。
     - Anti-SNR编码特征：`˜S_{I,1} = ˜S_I + Sigmoid( MLP( S_mi · ˜S_I ) )`。
  2. **语义率感知模块**：
     - 结构与SNR模块类似，输入为实际语义率R_I，生成权重S_mj。
     - Anti-语义率特征：`ˆS_{I,2} = ˜S_{I,1} + Sigmoid( MLP( S_mj · ˜S_{I,1} ) )`。
     - 引入码掩码：对特征通道按重要性排序，选取前C个通道构造二值掩码p，最终特征为`˜S_{I,2} = ⟨ ˆS_{I,2} · p⟩`。
  3. **损失函数**：联合使用MS-SSIM（感知损失）和MSE（像素损失），训练目标为最小化`1 - MS-SSIM`和MSE的组合。

### 3. 实验设计

- **数据集**：
  - **DIV2K**：1000张2K分辨率RGB图像，800训练、100验证、100测试；训练时随机裁剪为256×256。
  - **CLIC2020**：用于跨数据集泛化测试。
- **评估指标**：PSNR（像素级）和MS-SSIM（感知级）。
- **对比方法**：
  - **SNR-EQ-JSCC**：将SNR嵌入注意力块。
  - **SwinJSCC w/o CA**：无SNR和语义率模块。
  - **SwinJSCC w/ CA**：有逐层注入的SNR和语义率模块。
  - **ADJSCC**：CNN骨干+通道注意力。
  - **BPG+LDPC**：传统分离编码方案（BPG压缩+5G LDPC信道编码）。
- **实验场景**：
  - AWGN信道，不同SNR（-10dB ~ 20dB）。
  - 不同压缩率：r=1/32（严格）和r=1/8（宽松）。
  - 不同信道带宽比cbr：在CLIC2020上测试。
- **可视化对比**：提供了图4的定性重构结果。

### 4. 资源与算力

- **论文提及**：所有实验使用 **NVIDIA GeForce RTX 5060 Ti** 进行训练和测试。
- **未明确说明**：具体训练时长、GPU数量、batch size（仅提及8）等详细信息。

### 5. 实验数量与充分性

- **实验组数**：
  - 图2：两种压缩率（r=1/32, 1/8）下的PSNR和MS-SSIM曲线，共4个子图。
  - 图3：在CLIC2020数据集上不同cbr下的性能比较。
  - 图4：可视化对比示例（4个图像样本）。
- **充分性评估**：
  - **优点**：覆盖了两种压缩率、多个SNR点、两个数据集、定性+定量评估；对比了5种代表性方法（包括传统分离编码和不同JSCC变体）。
  - **不足**：未进行消融实验（如单独去除语义率模块的影响）；未在更复杂的信道（如瑞利衰落）或真实信道上测试；缺少与最新SOTA方法的全面对比；仅使用单一GPU，未分析模型参数和计算复杂度。

### 6. 论文的主要结论与发现

- **核心结论**：SA-RA-JSCC在**所有SNR范围和压缩率下**，PSNR和MS-SSIM均**显著优于**所有对比方法。
- **关键发现**：
  - 在`r=1/32`的严格压缩下，优势最大，表明全局协调的自适应机制尤其有利于保持关键语义信息。
  - 与传统BPG+LDPC相比，在低SNR下具有明显更平稳的性能退化，无“悬崖式”下降。
  - 在`r=1/8`时，模型能更好利用高SNR增益，性能差距随SNR增加而扩大。
  - 在CLIC2020上跨数据集泛化良好，验证了方法的鲁棒性。

### 7. 优点

- **方法设计**：
  - **全局一致性**：通过将SNR映射为统一语义向量并一次性重加权，避免了逐层注入带来的表示冲突。
  - **语义率感知**：同时考虑信道质量和压缩率约束，增强了自适应策略的协调性。
  - **架构简洁**：无需修改骨干网络（SwinJSCC），仅添加轻量级预处理模块和重加权操作。
- **实验结果**：
  - **全面超越**：在多个指标、数据集、SNR点、压缩率下均优于所有baseline。
  - **视觉质量提升**：定性图显示能更好保留纹理和结构信息。
  - **实用性**：避免了传统数字方案在高压缩率下的性能崩溃。

### 8. 不足与局限

- **实验覆盖**：
  - 仅测试了AWGN信道，未涉及衰落信道、多用户场景或实际硬件平台。
  - 缺乏与近期其他Transformer JSCC模型（如WITT、CSWIN）的直接对比。
  - 未报告模型参数量、计算复杂度或推理时间。
- **消融与泛化**：
  - 未进行消融实验，无法量化SNR模块和语义率模块各自的贡献。
  - 仅使用DIV2K和CLIC2020，其他领域（如医学图像、视频）未验证。
- **潜在偏差**：
  - 训练细节（如数据增强、学习率调度）未充分说明，可能影响可复现性。
  - 对比baseline的原始设置是否经过公平调优不明确（仅说“剩余参数遵循SwinJSCC设置”）。
- **应用限制**：
  - 仅面向图像传输，未扩展到视频或语义任务（如分类、检测）。
  - 依赖SwinJSCC骨干，对于轻量化场景可能计算开销仍较大。

（完）
