---
title: Physics Equivariance for Robust Generalization in Wireless Foundation Model
title_zh: 物理等变性用于无线基础模型的鲁棒泛化
authors: "Haoyu Wang, Xigang Gao, Zhi Sun, Zhaocheng Wang"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28847v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 物理等变性作为无线基础模型的归纳偏置
tldr: 无线基础模型面临数据规模有限、分布覆盖窄和仿真到真实差距问题，简单增加参数和样本难以保证鲁棒泛化。本文提出物理内在WFM，利用电磁波传播的波等变性作为归纳偏置，显式对齐模型行为与可解释的波传播结构。实验表明，phys-WFM有效捕获波等变性，并在分布偏移下显著提升对未知环境的鲁棒性和泛化能力。该工作为构建可解释的无线基础模型提供了物理驱动的可靠路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28847v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28847v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1898, \"height\": 981, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28847v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1904, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28847v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1891, \"height\": 716, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28847v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1482, \"height\": 338, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28847v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1900, \"height\": 443, \"label\": \"Table\"}]"
motivation: 现有无线基础模型缺乏物理先验，在分布偏移下泛化能力差，简单缩放模型和数据无法解决。
method: 设计phys-WFM，将波等变性（CSI调制时输出响应对应变换）作为归纳偏置嵌入模型结构。
result: phys-WFM有效捕获波等变性，在分布偏移下鲁棒性和泛化性显著优于vanilla-WFM。
conclusion: 物理等变性作为可解释归纳偏置可增强无线基础模型的鲁棒性和泛化，提供物理驱动方法。
---

## 摘要
无线基础模型（WFM）近期作为学习多通道状态信息（CSI）获取任务的一种有前景范式出现。然而，与由统计共现支配的自然语言标记不同，无线信道由电磁传播规律生成，当前WFM训练受限于有限的数据规模、以仿真为主的窄分布覆盖以及显著的仿真到真实差距。因此，简单地扩展模型参数和CSI样本并不一定能产生鲁棒且可泛化的模型。本文中，我们倡导将物理等变性作为WFM的一种原则性和可解释性的归纳偏置。具体而言，我们关注电磁波的一个通用传播特性，称为波等变性：当输入CSI沿时-频-空维度调制时，输出信道响应应呈现相应的变换。实证研究表明，即使使用大量模型参数和训练样本，普通WFM也无法可靠地获取这种等变性。为解决此问题，我们设计了具有波等变性的物理内在WFM（phys-WFM），该模型明确地将模型行为与可解释的波传播结构对齐。结果表明，所提出的设计有效捕获了波等变性，并显著提升了在分布偏移下对未见环境的鲁棒性和泛化能力，为可解释的无线基础模型提供了一条基于物理且可测试的路径。

## Abstract
Wireless foundation models (WFMs) have recently emerged as a promising paradigm for learning multiple channel state information (CSI) acquisition tasks. However, unlike natural language tokens governed by statistical co-occurrence, wireless channels are generated by electromagnetic propagation laws, and current WFM training is constrained by limited data scale, narrow distribution coverage dominated by simulations, and a pronounced sim-to-real gap. As a result, simply scaling model parameters and CSI samples does not necessarily yield robust and generalizable models. In this paper, we advocate enabling physics equivariance as a principled and explainable inductive bias for WFMs. Specifically, we focus on a universal propagation property for electromagnetic waves, termed wave equivariance: when the input CSI is modulated along time-frequency-space dimensions, the output channel response should exhibit the corresponding transformation. Empirical studies show that the vanilla-WFM fails to reliably acquire such equivariance even with a large number of model parameters and training samples. To address this, we design the physics-intrinsic WFM (phys-WFM) with wave equivariance, which explicitly aligns model behaviors with an interpretable wave propagation structure. Results demonstrate that the proposed design effectively captures wave equivariance and substantially improves robustness and generalization to unseen environments under distribution shift, offering a physics-grounded and testable route toward explainable wireless foundation models.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：无线基础模型（WFM）旨在通过大规模预训练实现多任务、多环境下的信道状态信息（CSI）获取。但不同于自然语言处理中依赖统计共现，无线信道由电磁波传播规律生成，且面临三大瓶颈：
  - 实测数据昂贵，难以达到LLM级别的万亿token规模；
  - 训练数据以仿真为主（如3GPP 38.901），分布覆盖窄；
  - 仿真与真实信道之间存在显著差距（sim-to-real gap）。
- **核心挑战**：单纯增加模型参数和CSI样本数（“缩放优先”范式）无法保证模型的鲁棒泛化能力，因为模型可能过拟合于统计相关性而非物理规律。因此需要引入可解释的物理归纳偏置，以提升WFM在分布偏移下的泛化性。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用电磁波传播的**波等变性**（wave equivariance）作为环境无关的归纳偏置。波等变性指：当输入CSI沿时间、频率或空间维度施加相位斜坡变换（phase-ramp）时，模型的输出信道响应应发生对应的可预测变换。若模型满足该性质，则其行为与物理规律一致。
- **模型结构（phys-WFM）**：
  - **输入CSI** → **掩码操作**（随机/结构化掩码） → **过采样峰值扫描与对齐模块**：通过过采样（oversampling）精确估计主导相位斜坡参数，将CSI对齐到标准化坐标框架。
  - **Transformer骨干网络**（使用MAE结构）对对齐后的CSI进行重建/预测。
  - **信道恢复模块**：利用旁路传递的参数施加逆变换，恢复原始信道，实现端到端波等变性。
- **理论保证**：当过采样因子趋于无穷时，模块能渐近实现精确的波等变性。该等变性缩小了模型假设空间到物理一致函数，防止过拟合，并通过将单个样本沿其等变轨道扩展，有效扩大训练数据的物理覆盖范围，从而提升泛化性。

### 3. 实验设计

- **预训练数据**：基于3GPP TR 38.901信道模型合成40个异构配置的CSI数据集，包含不同场景、系统配置、用户速度等。
- **测试集（零样本泛化）**：
  - 3GPP未见场景/参数配置（unseen 3GPP）
  - WAIR-D（射线追踪，100个环境）
  - DeepMIMO-cities（20个环境）
  - RENEW实测信道数据集
- **评估任务**：三种CSI获取任务——随机掩码重建、时间域预测、频率域外推，均使用NMSE（归一化均方误差）指标。
- **对比方法**：
  - 传统深度学习：GRU、LSTM、Informer（需为每个任务单独训练）
  - 现有无线基础模型：WiFo、HeterCSI（使用相同预训练数据）
- **等变性的量化实验**：在时间、频率、空间三个域中，通过扫描相位斜坡频率ω，计算模型输出与理论等变输出之间的NMSE（NMSE-WE），并与HeterCSI对比。

### 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长等算力资源。仅指出phys-WFM总参数量为69.9M，但未提及训练配置。

### 5. 实验数量与充分性

- **实验数量**：包含两组主要实验：
  - 波等变性定量评估（3个域 × 多频率ω × 2种过采样率，与HeterCSI对比）
  - 零样本泛化评估（4个数据集 × 3个任务，对比5/6个基线方法）
- **充分性**：
  - 覆盖了不同数据来源（仿真、射线追踪、实测）、不同任务类型，结果全面。
  - 等变性实验直接验证了物理一致性，并分析了过采样率的影响，设计合理。
  - 但**缺少消融实验**（如移除等变模块仅保留骨干网络的效果对比），不过文中已与普通WFM（HeterCSI）进行了同等训练条件下的对比，可视为间接消融。
  - 所有方法使用相同预训练数据，比较公平。总体实验充分、客观。

### 6. 主要结论与发现

- phys-WFM在所有测试域上均实现了近理想的波等变性（NMSE-WE < 0.05），且随过采样率增大而进一步降低；而HeterCSI在频率ω接近π时等变性显著退化（NMSE-WE > 0.2），说明单纯缩放无法习得物理一致性。
- 在零样本泛化中，phys-WFM在所有4个测试集、所有3个任务上取得最佳NMSE性能，尤其在实际RENEW数据集上增益最大，有效缩小了sim-to-real差距。
- 波等变性通过扩大有效样本覆盖范围，极大提升了数据效率，使模型在未见环境中仍能保持高精度。

### 7. 优点

- **新颖性**：首次将物理等变性作为可测试的归纳偏置引入无线基础模型，明确了“变换入/变换出”的可审计准则。
- **理论基础扎实**：从电磁波传播定律出发，形式化波等变性，并给出渐近实现的保证，同时理论解释了泛化增益（有效样本覆盖扩展）。
- **实践兼容性**：所提等变模块即插即用，与现有Transformer骨干和多元预训练范式兼容。
- **解释性**：模型行为可通过与已知物理变换的一致性进行验证，提升了可解释性和可信度。
- **实验设计全面**：涵盖仿真、射线追踪、实测数据集，以及多种任务，验证了鲁棒泛化能力。

### 8. 不足与局限

- **硬件与协议约束未考虑**：论文指出等变性的实际效果取决于物理变换在硬件非理想性和协议限制下的保真度，但未对此进行深入分析或实验。
- **缺乏消融实验**：没有定量展示去掉等变模块后模型性能下降的幅度，尽管与基线对比提供了部分证据。
- **物理多样性不足**：仅关注波等变性，未探索其他潜在物理结构（如因果时间演化、极化等），未来可扩展。
- **算力资源未报告**：无法评估训练成本，也不利于复现。
- **潜在偏差**：预训练数据仅基于3GPP 38.901一种仿真模型，可能隐含了特定仿真假设，对不同信道模型（如几何随机模型）的泛化性有待验证。

（完）
