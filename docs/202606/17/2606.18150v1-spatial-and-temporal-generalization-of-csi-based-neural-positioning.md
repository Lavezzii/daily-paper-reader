---
title: Spatial and Temporal Generalization of CSI-based Neural Positioning
title_zh: 基于CSI的神经定位的空间和时间泛化
authors: "Till-Yannic Müller, Frederik Zumegen, Reinhard Wiesmayr, Christoph Studer"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18150v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 利用CSI进行通信信道建模的神经网络
tldr: 现有CSI定位研究多采用随机数据划分，导致泛化性能评估过于乐观。本文使用Wi-Fi和5G NR室内外真实数据集，评估MLP和Transformer架构在空间（未见过区域、轨迹）和时间（一周后）上的泛化能力。实验表明两种架构均能良好泛化，Transformer以更少参数取得更高精度。该工作揭示了实际部署中时空泛化的重要性，并提供了标准合规系统的基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18150v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1814, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18150v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1588, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18150v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 872, \"height\": 753, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18150v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1887, \"height\": 1479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18150v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1862, \"height\": 561, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18150v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 478, \"height\": 326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18150v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 512, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18150v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 936, \"height\": 504, \"label\": \"Table\"}]"
motivation: 现有CSI神经定位评估使用随机划分数据集，未考虑真实部署中空间和时间泛化需求，结果偏乐观。
method: 采用MLP和Transformer架构，在Wi-Fi和5G NR室内外数据集上评估对未见过空间区域、轨迹及一周后数据的泛化。
result: 两种架构均实现良好时空泛化，Transformer定位精度优于MLP且模型参数更少。
conclusion: CSI神经定位可有效泛化至新的时空场景，Transformer是高效解决方案。
---

## 摘要
基于信道状态信息（CSI）的神经定位利用神经网络学习从CSI测量到用户设备（UE）位置的映射。然而，现有的大多数性能评估采用随机划分的训练/测试CSI数据集，这未能反映实际部署中的泛化需求，并呈现了过于乐观的结果。本文中，我们研究了符合标准的Wi-Fi和5G NR系统在三个室内外环境获取的真实CSI数据集上的神经定位的空间和时间泛化。我们使用两种不同的架构——传统的多层感知机（MLP）和一种新颖的Transformer架构——评估对未见过的空间区域、未见过的UE轨迹以及相隔一周的CSI测量活动的泛化能力。我们的实验表明，两种架构在空间和时间上都具有良好的泛化能力，所提出的Transformer在定位精度上始终优于MLP，同时所需的模型参数更少。

## Abstract
Channel state information (CSI)-based neural positioning learns a mapping from CSI measurements to user equipment (UE) positions using neural networks. However, most existing performance evaluations utilize randomly partitioned train/test CSI-dataset splits, which fail to reflect the generalization requirements of practical deployments and present optimistic results. In this paper, we study the spatial and temporal generalization of neural positioning with standard-compliant Wi-Fi and 5G NR systems for three real-world CSI datasets acquired in indoor and outdoor environments. We assess generalization with two different architectures, a conventional multilayer perceptron (MLP) and a novel transformer architecture, to unseen spatial regions, unseen UE trajectories, and CSI measurement campaigns separated by one week. Our experiments show that both architectures generalize well in space and time, and the proposed transformer consistently outperforms the MLP in positioning accuracy while requiring fewer model parameters.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：基于信道状态信息（CSI）的神经定位通过学习从CSI到用户设备（UE）位置的映射，近年来受到广泛关注。然而，现有工作普遍采用随机划分的训练/测试数据集来评估性能，这无法反映实际部署中系统需要对**未见过空间区域**、**未见过轨迹**以及**时间变化**进行泛化的需求，导致评估结果过于乐观。
- **背景**：已有研究（如[4]–[11]）几乎都使用随机划分，仅有[16]研究了空间泛化但局限于单一小尺度室内数据集，未考虑现代定位架构、多种无线标准或时间分隔的测量活动。
- **核心问题**：神经定位是否能够在空间和时间上有效泛化？不同架构的泛化能力如何？

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：系统性地评估神经定位在空间（未见过区域、未见过轨迹）和时间（相隔一周的测量活动）上的泛化能力，对比两种神经网络架构：传统的**全连接多层感知机（MLP）** 和一种新颖的**Transformer**架构。
- **关键技术细节**：
  - **预处理**：从每个CSI张量 \(\mathbf{H}_n \in \mathbb{C}^{B \times A \times W}\) 提取特征向量 \(\mathbf{f}_n \in \mathbb{R}^{F}\)，取元素级绝对值并归一化（\(\|\mathbf{f}_n\| = 1\)）。
  - **MLP架构**：采用概率图方法（来自[9]）：将空间离散化为 \(K = 484\) 个网格点，输出概率图 \(\hat{\mathbf{p}}_n \in [0,1]^K\)，通过二值交叉熵（BCE）损失训练，最终位置估计为 \(\hat{\mathbf{x}}_n = \mathbf{A} \hat{\mathbf{p}}_n\)。MLP有4个隐藏层（维度 512），输出层 \(K\)。
  - **Transformer架构**：将每个AP视为一个token，通过两层MLP嵌入得到 \(\mathbf{t}_{n,b} \in \mathbb{R}^d\)，加上可学习的位置编码 \(\mathbf{e}_b\)，经过2层Transformer编码器（4头自注意力，FFN维度512），将所有token输出拼接后通过4层回归头（含sigmoid激活）输出归一化位置 \(\hat{\mathbf{x}}_n \in [0,1]^2\)，使用MSE损失训练。
- **损失函数**：
  - MLP：BCE损失（概率图）
  - Transformer：MSE损失（直接回归坐标）
- **训练细节**：MLP使用Adam优化器，30个epoch，批量大小10；Transformer使用AdamW，1000个epoch，批量大小32，学习率余弦退火至\(10^{-6}\)。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集**：三个真实世界CSI数据集，涵盖不同无线标准、环境和规模：
  - **Wi-Fi Office**（新采集）：IEEE 802.11a，5.23 GHz，40 MHz带宽，4个AP×4天线×104子载波，测量区域13.7 m×10.6 m，两个相隔一周的测量活动（各约1小时）。
  - **5G Hallway**（新采集）：5G NR，3.45 GHz，100 MHz带宽，4个O-RU×4天线（273个下采样子载波），室内大面积52.8 m×29.4 m。
  - **5G Outdoor**（已有数据集）：5G NR，室外庭院9.9 m×10.1 m。
- **测试场景**（如图2所示）：
  - **基线场景**：随机80%训练/20%测试划分。
  - **空间泛化**：保留若干半径为0.44 m的圆形区域（全部用于测试，训练中从未出现）。
  - **时间泛化（未见轨迹）**：保留一段连续轨迹（约总数据1%，时长约1分钟）用于测试。
  - **跨周一致性**（仅Wi-Fi Office）：训练于Week 1测试于Week 2，训练于Week 2测试于Week 1，以及两个周合并训练测试。
- **基准（baseline）**：单最佳猜测（single-best-guess），即使用训练集位置的几何中位数作为固定预测。
- **对比方法**：MLP vs. Transformer（本文提出）。

## 4. 资源与算力

- **文中明确说明**：所有模型在**单张NVIDIA RTX 4070 GPU**上使用PyTorch训练。MLP训练30个epoch，Transformer训练1000个epoch。未提及GPU数量或总训练耗时具体数值，但硬件资源较低（单卡消费级显卡），表明方法对算力要求不高。

## 5. 实验数量与充分性

- **实验组数**：
  - 3个数据集 × 2种架构 × 3种测试场景（随机划分、未见过区域、未见过轨迹）→ 共18个主要实验（图4）。
  - 额外跨周实验（Wi-Fi Office）：Transformer训练于Week1/Week2/两者联合，测试于不同周，共3×2=6组（图5）。
- **充分性评估**：
  - 覆盖了室内/室外、Wi-Fi/5G NR、大小尺度（4m²~1500m²）等多种场景，具有代表性。
  - 使用三个误差指标（MAE、中位数误差、95%分位误差），全面反映性能。
  - 与单最佳猜测基线对比，明确显示神经定位远优于固定预测。
  - **但有局限**：仅两种架构，缺乏与其他SOTA（如CNN、KNN等）的直接对比；未见过区域测试半径固定为0.44m，未扫描不同半径的影响；跨周实验仅在一个数据集上（Wi-Fi Office），室外数据集缺乏时间泛化评估。

## 6. 论文的主要结论与发现

- **泛化能力**：两种神经定位架构在空间和时间上均表现出**有意义的泛化**，误差始终远低于单最佳猜测基线。
- **随机划分过于乐观**：随机划分下的误差远低于泛化场景，说明此前文献结果高估了实际性能。
- **Transformer优于MLP**：在所有数据集和所有泛化场景中，Transformer的定位精度（MAE、中位数、95%分位）均优于MLP，且参数数量更少（约1/3 ~ 1/2）。
- **跨周泛化**：单周训练后在另一周测试性能下降显著，但联合两周训练可大幅提升并优于单周训练，表明长期测量数据可改善时间泛化。
- **数据集影响**：5G系统（3.45 GHz，100 MHz）比Wi-Fi（5.23 GHz，40 MHz）实现更低定位误差，可能源于带宽和信号特性差异。

## 7. 优点：方法或实验设计上的亮点

- **实验设计系统性强**：首次系统性地评估神经定位在空间和时间上的泛化，包括三种泛化场景（未见过区域、未见过轨迹、跨周），纠正了随机划分的乐观偏差。
- **真实世界数据集**：新采集两个标准兼容的Wi-Fi和5G NR数据集（含多天测量），公开可重现代码和数据集（接受后发布），促进可复现性。
- **公平比较**：对两种架构均进行了超参数优化和训练策略调整，且Transformer参数更少却性能更优，增加了结论的说服力。
- **多指标评估**：使用均值、中位数、95%分位误差，全面反映分布。
- **分析深入**：不仅报告结果，还探讨了跨周泛化中联合训练的好处，以及归一化对Transformer训练稳定性的重要性。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **架构覆盖不全**：仅对比MLP和Transformer，未与CNN、RNN、GNN等其他流行架构或传统方法（如KNN、指纹库）对比，无法判断Transformer是否是当前最优选择。
- **泛化场景有限**：
  - 未见过区域半径固定（0.44m），未研究不同半径的影响。
  - 未见轨迹仅1%数据，且取自中心，可能不具有代表性。
  - 跨周实验仅在一个Wi-Fi数据集上执行，未在5G室外/室内数据集验证时间泛化（可能因缺乏多周数据）。
- **评估偏差风险**：
  - 所有数据集均在同一实验平台采集，可能隐含设备特性偏差。
  - 环境中存在多个移动人体（非受控），但未量化人体动态对泛化的影响。
- **应用限制**：
  - 需要大量标注位置的真值数据（训练阶段），难以大规模部署。
  - 仅考虑2D定位，未涉及3D或朝向估计。
  - 未测试更长时间跨度（如数月）或环境变化（如家具移动、AP更换）下的泛化。
- **其他**：MLP使用概率图损失（BCE），Transformer使用MSE损失，两者损失不同可能导致比较不完全公平；文中提到Transformer用BCE优于MSE，但未给出具体数据。

（完）
