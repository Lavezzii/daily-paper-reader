---
title: "Z-Plane Neural Networks: Bounded Geometric Activation Replaces ReLU and LayerNorm"
title_zh: Z平面神经网络：有界几何激活函数替代ReLU和LayerNorm
authors: "Sungwoo Goo, Hwi-yeol Yun, Sangkeun Jung"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15669v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 具有几何激活函数的新型神经网络架构
tldr: "传统ReLU激活和LayerNorm归一化导致死神经元、方向信息丢失及正交性破坏。受生物轴突频率调制启发，提出Z-Plane神经网络，采用Radial Bounding几何激活函数将隐藏状态映射到超球面，在限制能量幅度的同时保留相位方向。实验表明，100层无ReLU和LayerNorm的Z-Plane MLP在MNIST上达到98.34%准确率且数值绝对稳定。该工作证明了有界几何激活可单独替代归一化实现深度网络的稳定训练。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15669v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 1809, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15669v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1017, \"height\": 364, \"label\": \"Table\"}]"
motivation: 解决ReLU导致死神经元、丢弃方向信息及归一化破坏特征正交性的问题。
method: "提出Z-Plane网络，用Radial Bounding(x/max(1,||x||_2))将隐藏状态映射到超球面，保持相位并限制能量。"
result: "100层无ReLU和LayerNorm的Z-Plane MLP在MNIST上达98.34%准确率，训练绝对数值稳定。"
conclusion: 有界几何激活足以单独确保深度网络稳定收敛，无需传统归一化层。
---

## 摘要
现代深度神经网络依赖欧几里得标量激活函数（如ReLU）和全局归一化技术（如LayerNorm）来防止深层架构中的梯度不稳定性。然而，这些机制固有地导致神经元死亡、丢弃关键的方向信息，并破坏特征表示的正交性。受生物轴突频率调制传输的启发，我们提出了Z平面神经网络，将隐藏状态映射到超球面上的二维相量束。我们引入了一种新颖的几何激活函数——径向有界函数（Radial Bounding, $\mathbf{x} / \max(1, \|\mathbf{x}\|_2)$），它在保持相位（方向）的同时限制能量幅度。我们从数学上证明了这种各向同性激活函数保持了1-Lipschitz连续性，并通过保留切向梯度防止了梯度消失。实验上，一个完全不含ReLU和LayerNorm的100层Z平面多层感知机（MLP）在MNIST数据集上成功收敛，准确率达到98.34%，且具有绝对的数值稳定性，证明了仅使用有界几何激活函数就足以实现稳定的深度学习。

## Abstract
Modern deep neural networks rely on Euclidean scalar activations (e.g., ReLU) and global normalization techniques (e.g., LayerNorm) to prevent gradient instability in deep architectures. However, these mechanisms inherently cause dead neurons, discard critical directional information, and destroy the orthogonality of feature representations. Inspired by the frequency-modulation transmission of biological axons, we propose the Z-Plane Neural Network, which maps hidden states into 2D phasor bundles on a hypersphere. We introduce a novel geometric activation function, Radial Bounding($\mathbf{x} / \max(1, \|\mathbf{x}\|_2)$), which limits the energy magnitude while preserving the phase (direction). We demonstrate mathematically that this isotropic activation maintains 1-Lipschitz continuity and prevents gradient vanishing by preserving tangential gradients. Empirically, a 100-layer Z-Plane Multi-Layer Perceptron (MLP)-entirely devoid of ReLU and LayerNorm-successfully converges on the MNIST dataset with 98.34% accuracy and absolute numerical stability, proving that bounded geometric activation alone is sufficient for stable deep learning.

---

## 论文详细总结（自动生成）

# 论文总结：Z平面神经网络：有界几何激活函数替代ReLU和LayerNorm

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现代深度神经网络依赖ReLU等标量激活函数和LayerNorm等归一化层来防止梯度爆炸/消失，但这些方法存在固有缺陷：ReLU导致“死神经元”并丢弃负值信息；LayerNorm强制全局缩放，破坏特征间的正交性和方向信息。
- **生物学启发**：生物神经元的轴突传输依赖频率和相位调制（而非幅度），幅度仅作为触发阈值，信号传输本质上是纯频率。作者将这一原理数字化，提出用**相位方向**作为信息载体，幅度仅用于非线性交互。
- **核心含义**：探索一种新的神经网络范式——使用**有界几何激活函数**同时提供非线性、能量有界和梯度稳定，从而彻底替代ReLU和LayerNorm。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将隐藏状态表示为2D相量束（phasor bundles），即把特征向量分组为二维复数对，每个对代表一个相量。通过线性投影（类似树突整合）产生幅度波动（构造性/相消性干涉），然后使用**径向有界激活函数**将幅度限制在单位球面上，同时保留相位（方向）。
- **关键技术**：
  - **径向有界函数（Radial Bounding）**：  
    \[
    f(\mathbf{v}) = \frac{\mathbf{v}}{\max(1, \|\mathbf{v}\|_2)}
    \]
    当 $\|\mathbf{v}\|_2 \le 1$ 时为恒等映射，保留残差路径；当 $\|\mathbf{v}\|_2 > 1$ 时严格将幅度压缩到1，将向量折射到单位超球面，产生非线性。
  - **梯度特性**：在 $\|\mathbf{v}\| > 1$ 时，Jacobian矩阵为 $\frac{1}{R}(I - \frac{\mathbf{v}\mathbf{v}^T}{R^2})$，径向梯度被置零防止爆炸，切向梯度（相位旋转）被保留并缩放 $1/R$ 安全后向传播。网络通过旋转相位而非膨胀幅度来学习。
  - **理论保证**：该激活函数保持1-Lipschitz连续性，无需LayerNorm即可防止梯度爆炸。
- **算法流程**（文字描述）：
    1. 输入图像展平为2D束（每个束2个分量）。
    2. 通过线性层（无偏置）进行投影（等效于树突整合）。
    3. 对每个2D束计算L2范数，应用径向有界函数：若范数≤1则不变；若>1则除以范数（即投影到单位圆）。
    4. 残差连接：每层输出加上输入。
    5. 最后通过一个线性分类器输出logits。

## 3. 实验设计

- **数据集**：仅使用MNIST手写数字识别（28×28灰度图像，10类）。
- **场景**：验证极深网络（100层MLP）在没有ReLU和LayerNorm下的稳定性与性能。
- **Benchmark**：未提供标准基准对比（如ResNet、Transformer等），仅与“标准欧几里得MLP在相同深度下会崩溃（梯度爆炸导致NaN loss）”进行定性对比。
- **对比方法**：无直接对比其他激活函数或归一化方法（如与ModReLU、Squash函数等对比仅在相关工作部分提及，但无实验对比）。
- **模型架构**：100个ZPlaneLayer堆叠，隐藏束数256（即每层512维特征，分256个2D对），输入层将784像素映射为392个2D束，输出层为线性分类器。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量、训练时长。仅提到训练20 epoch，batch size 128，优化器AdamW，学习率5e-4。由于是100层小规模MLP（约几千万参数？），推测只需单卡即可在几小时内完成。无分布式或多GPU信息。

## 5. 实验数量与充分性

- **实验数量**：仅做一个实验——在MNIST上训练一个100层Z-Plane MLP，报告20个epoch的训练和测试准确率（表1）。
- **充分性评价**：
  - **不足**：仅单数据集、单任务，未在更大规模（如CIFAR-10/100、ImageNet）或不同领域（NLP、语音）验证；未做消融实验（如替换不同激活函数、不同深度、有无残差连接等）；未与现有归一化/激活方法公平对比（如ReLU+LayerNorm、ModReLU等）；未分析不同初始化、学习率敏感度。
  - **客观性**：作者仅给出初步实证结果，声称“标准MLP在此深度会失败”，但未提供具体失败数据。实验设置简单，结论的泛化性存疑。

## 6. 主要结论与发现

- 100层无ReLU和LayerNorm的Z-Plane MLP在MNIST上成功收敛，训练准确率98.34%，测试准确率96.89%，数值绝对稳定（无NaN）。
- 证明**有界几何激活函数**（径向有界）可单独替代传统归一化和标量激活，实现深度网络稳定训练。
- 该方法保留了切向梯度（相位信息），避免死神经元，且天然具有1-Lipschitz性。

## 7. 优点

- **理论创新**：将生物轴突频率调制映射为神经网络中的相位/幅度分离机制，提供了新的激活设计视角。
- **简洁性**：单一激活函数同时实现非线性、能量有界和梯度稳定，无需额外归一化，简化了网络结构。
- **数学优雅**：径向有界函数在范数≤1时为恒等映射，确保了残差路径的线性传递；在范数>1时严格有界，且切向梯度安全传播。
- **实证稳定性**：在极深（100层）条件下展示了绝对数值稳定性，这是传统MLP难以做到的。

## 8. 不足与局限

- **实验覆盖严重不足**：仅在一个简单数据集（MNIST）上验证，未在更复杂任务（如图像分类CIFAR-100、ImageNet，或序列建模、Transformer）中测试。MNIST上传统MLP加ReLU+BN/LN也能轻松达到97%以上，该结果的竞争力需要更严谨对比。
- **缺乏公平对比**：未与标准基线（如相同深度的ReLU+LayerNorm MLP，或CapsNet的squash函数）进行直接实验对比。
- **潜在偏差风险**：
  - 100层MLP虽深但每层宽度仅256束（512维），参数总量可能不大，泛化能力可能有限。
  - 没有分析不同深度（如10层、50层）、不同学习率、不同初始化对稳定性的影响。
  - 没有提供梯度范数的监控或可视化来证明“相位梯度被保留”这一主张。
- **应用限制**：
  - 2D束的分组方式可能破坏特征间的高维相关性（如将原本非正交的维度强行配对）。
  - 残差连接（每层输出+输入）是必要的，若去掉残差是否仍稳定？论文未讨论。
  - 线性层无偏置，可能限制表示能力；无偏置的设定是否会影响分类边界？
- **理论分析不完整**：虽然给出了Jacobian公式，但未讨论切向梯度随层数传播是否会衰减（1/R因子可能累积）。1-Lipschitz性是否在复合后仍然保持？
- **计算效率**：需要对每个2D向量求范数，相比ReLU等标量激活有一定计算开销，但现代GPU可向量化，影响不大。

**（完）**
