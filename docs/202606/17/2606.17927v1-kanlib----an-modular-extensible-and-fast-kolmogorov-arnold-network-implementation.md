---
title: "KANLib -- An Modular, Extensible and Fast Kolmogorov-Arnold Network Implementation"
title_zh: KANLib -- 一个模块化、可扩展且快速的Kolmogorov-Arnold网络实现
authors: "Julian Hoever, Gregor Schiele"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17927v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: KANLib：模块化、快速的Kolmogorov-Arnold网络实现
tldr: Kolmogorov-Arnold Networks (KANs) 通过可学习单变量函数替代线性权重，具有可解释性和表达力，但实际研究受限于高计算成本和框架功能不一致。KANLib 是一个模块化、可扩展、高效的 KAN 框架，统一 PyKAN、EfficientKAN 等现有实现，支持两种基函数、自适应网格缩放和网格扩展。在 California Housing 基准上，KANLib 复现了参考 KAN 的预测性能，同时达到了有竞争力的计算效率。该框架为未来可扩展 KAN 架构研究提供了稳健基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 453, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1215, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17927v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1418, \"height\": 322, \"label\": \"Table\"}]"
motivation: 解决现有 KAN 实现计算成本高、功能不一致的问题，提供统一高效的研究平台。
method: 提出 KANLib，一个模块化、可扩展、高性能的 KAN 框架，集成多类基函数、自适应网格缩放与扩展，兼容 PyTorch 工作流。
result: 在 California Housing 上复现参考 KAN 预测行为，计算效率有竞争力，并支持架构变体探索。
conclusion: KANLib 为可扩展 KAN 研究提供了稳健基础，促进未来架构创新与高效实现。
---

## 摘要
Kolmogorov-Arnold网络（KANs）最近作为传统多层感知机的一种有前景的替代方案出现，它用可学习的单变量函数替换了线性权重。尽管在可解释性和表达能力方面具有理论优势，但由于高计算成本和现有框架中不一致的特征支持，KANs的实际研究仍然困难。本文介绍了KANLib，一个用于开发和评估KAN架构的模块化、可扩展且计算高效的框架。KANLib在一致的软件架构中统一了现有实现（包括PyKAN、EfficientKAN和FastKAN）的核心概念，强调灵活性、特征对等性和高性能。该框架支持两种基函数类型、自适应网格重缩放、网格扩展以及细粒度的架构定制，同时保持与标准PyTorch工作流的兼容性。在加利福尼亚房价基准上的实验评估表明，KANLib在复现已建立参考KAN实现的预测行为的同时，达到了有竞争力的计算效率。此外，该框架使得探索超出标准KAN公式的架构变体成为可能，且对预测性能的影响微乎其微。总体而言，KANLib为未来关于可扩展和可扩展KAN架构的研究提供了坚实基础。

## Abstract
Kolmogorov-Arnold Networks (KANs) have recently emerged as a promising alternative to traditional multilayer perceptrons by replacing linear weights with learnable univariate functions. Despite their theoretical advantages in interpretability and expressiveness, practical research of KANs remains difficult due to high computational costs and inconsistent feature support across existing frameworks. This paper introduces KANLib, a modular, extensible, and computationally efficient framework for developing and evaluating KAN architectures. KANLib unifies core concepts from existing implementations, including PyKAN, EfficientKAN, and FastKAN, within a consistent software architecture that emphasizes flexibility, feature parity, and high performance. The framework supports two basis function types, adaptive grid rescaling, grid extension, and fine-grained architectural customization while maintaining compatibility with standard PyTorch workflows. Experimental evaluation on the California Housing benchmark demonstrates that KANLib reproduces the predictive behavior of established reference KAN implementations while achieving competitive computational efficiency. Furthermore, the framework enables the exploration of architectural variations beyond standard KAN formulations with only minor impacts on predictive performance. Overall, KANLib provides a robust foundation for future research on scalable and extensible KAN architectures.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：Kolmogorov-Arnold 网络（KAN）是一种用可学习单变量函数替代线性权重的新型网络结构，理论上比多层感知机更具可解释性和表达能力。但现有 KAN 实现（如 PyKAN、EfficientKAN、FastKAN）面临计算开销大、功能支持不一致、缺乏统一平台等问题，阻碍了实际研究。
- **核心目标**：设计一个模块化、可扩展、计算高效的 KAN 框架 **KANLib**，统一既有实现的核心特性，提供一致的软件架构，便于研究者快速开发、比较和扩展 KAN 架构。

### 2. 论文提出的方法论

- **核心思想**：将 KAN 层拆分为可组合的模块，支持两种基函数（B‑样条和 Gauss 径向基函数），并保留关键功能（自适应网格缩放、网格扩展、残差分支、可选的 Layer Normalization 等）。
- **关键技术细节**：
  - 每个可学习函数定义为：`ϕ(x) = w_b·b(x) + w_s·spline(x)`，其中 `b(x)` 为 SiLU 残差分支，`spline(x)` 为基函数线性组合。
  - 基函数：B‑样条（三阶）或 Gauss RBF。
  - 自适应网格缩放：根据输入统计分布动态调整网格边界和分布。
  - 网格扩展：训练中逐步增加网格精细度，无需重新学习。
  - 提供细粒度控制：可独立开启/关闭残差分支、额外样条权重，以及 Layer Normalization。
  - 兼容 PyTorch 标准训练流程，支持构建纯 MLP 型 KAN 或混合模型。

### 3. 实验设计

- **数据集**：仅使用 California Housing 数据集（21k 样本，8 个数值特征，回归任务）。
- **基准/对比方法**：
  - 参考实现：PyKAN、EfficientKAN（B‑样条）、FastKAN（Gauss RBF）。
  - 评估 KANLib 的两个变体：KANLib (B‑spline) 和 KANLib (GRBF)。
- **网络与训练配置**：
  - 2 层 KAN，隐藏维度 30，网格大小 10，网格范围 [-1,1]，样条阶数 3。
  - 禁用自适应缩放和 Layer Normalization（公平对比）。
  - 优化器 Adam，lr=1e-3，批次大小 1024，epoch=300，MSE 损失。
  - 输入特征缩放至 [-1,1]。
  - 训练/验证 80%/20% 随机划分。
  - 重复 20 次独立初始化，报告均值和标准差。

### 4. 资源与算力

- 论文未明确说明使用的 GPU 型号或数量。
- **推理性能测量**在 CPU（AMD Ryzen 9 9950X）上完成，训练未指定硬件，推测可能在 CPU 或普通 GPU 上进行（未报告训练耗时）。
- 由于仅使用单一小数据集（California Housing），算力需求低，未涉及大规模资源消耗。

### 5. 实验数量与充分性

- **实验组数**：
  - **预测性能对比**：5 种实现（PyKAN, EfficientKAN, KANLib B-spline, FastKAN, KANLib GRBF），各 20 次重复 → 共 100 次训练。
  - **架构变体探索**：4 种配置（Default, No Residual, No Spline Weight, Plain）× 2 种基函数 → 8 组，各 20 次重复 → 共 160 次训练。
- **充分性评价**：
  - 重复次数充足（20 次），统计结果可信。
  - **局限性**：仅使用一个数据集（California Housing），缺乏多数据集、多任务验证；未与其他非 KAN 基线（如 MLP）对比；未提供显著性检验。
  - 实验设计整体公平（禁用额外优化技巧，统一超参），但覆盖范围偏窄。

### 6. 论文的主要结论与发现

- KANLib 在预测精度（RMSE, R²）上与各参考实现持平甚至略优，证明其复现正确性。
- 计算效率：KANLib (B‑spline) 与 EfficientKAN 相当，比 PyKAN 快约 33%；KANLib (GRBF) 比 FastKAN 慢（约 2 倍），原因在于为支持自适应缩放增加了开销。
- 架构变体实验显示：移除残差分支或样条权重对精度影响很小（<2% RMSE 变化），可换取 6%–17% 的推理加速；Plain 配置（仅样条）精度略有下降但速度提升最明显。
- KANLib 的模块化设计允许轻松探索非标准 KAN 结构，为未来研究提供坚实基础。

### 7. 优点

- **统一框架**：首次将三种主流 KAN 实现（PyKAN、EfficientKAN、FastKAN）整合到同一软件架构，支持多种基函数和可切换特性。
- **模块化与可扩展**：允许灵活增减组件（残差分支、样条权重、网格缩放等），便于研究者快速原型化新想法。
- **计算效率**：B‑样条版本达到与当前最快实现（EfficientKAN）相当的推理速度。
- **代码开源**（GitHub），便于复现和社区贡献。

### 8. 不足与局限

- **实验覆盖不足**：仅基于单一数据集（California Housing），结论普适性有限；未在图像、时序、自然语言等常见任务上验证。
- **缺乏与经典 MLP 的对比**：未证明 KAN 在实际任务上相比 MLP 的优势（仅与 KAN 内部实现对比）。
- **GRBF 性能退化**：自适应缩放功能导致 KANLib (GRBF) 比 FastKAN 慢约 2 倍，效率优化不足。
- **缺失大规模训练统计**：未报告训练耗时、GPU 型号等算力信息。
- **架构变体实验仅涉及消融**，未探索更广的深度、宽度、基函数组合等。
- **未涉及可解释性分析**（原文 PyKAN 强调符号回归和可视化，但 KANLib 仅提供绘图，未展示解释性结果）。

（完）
