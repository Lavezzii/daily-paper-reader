---
title: Physics-Informed Neural Optimization Based Antenna Coding Design for Pixel Antenna Systems
title_zh: 基于物理信息神经优化的像素天线系统天线编码设计
authors: "Taoning Zhan, Shanpu Shen, Danny H. K. Tsang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21235v1"
tags: ["query:channel-nn"]
score: 9.0
evidence: 用于无线系统天线编码设计的物理信息神经优化器
tldr: 像素天线通过开关状态优化实现方向图可重构，但其编码设计是NP-hard问题。传统启发式搜索算法计算复杂度高。本文提出物理信息神经优化器，利用深度卷积神经网络先验和Gumbel-Sigmoid连续松弛将二进制优化转化为连续可微问题，通过梯度下降高效求解。仿真表明，该算法相比启发式算法显著降低计算时间，同时获得更高平均信道增益。该工作为像素天线编码设计提供了高效数据无关的优化方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21235v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21235v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 885, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21235v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1657, \"height\": 569, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21235v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 784, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21235v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 741, \"height\": 414, \"label\": \"Figure\"}]"
motivation: 当前像素天线编码设计依赖启发式搜索，计算复杂度过高，亟需高效优化方法。
method: 结合深度卷积神经网络先验和Gumbel-Sigmoid连续松弛，将二进制优化转化为连续可微问题，利用梯度下降求解。
result: 所提算法在减少计算时间的同时，实现比启发式算法更高的平均信道增益。
conclusion: 提出了一种数据无关的高效像素天线编码优化方法，通过物理信息神经优化器提升性能并降低复杂度。
---

## 摘要
像素天线能够实现高度方向图可重构性以增强无线系统，但其天线编码设计——即优化嵌入像素天线中开关的状态——仍然是一个NP难问题。传统的天线编码设计方法通常依赖于启发式搜索算法，计算复杂度高。为克服这一问题，我们提出了一种新颖高效的无数据优化算法，称为物理信息神经优化器（PINO），用于天线编码设计。通过将深度卷积神经网络先验和Gumbel-Sigmoid连续松弛方法集成到可微物理引擎中，所提算法将二元优化问题转化为连续可微问题，使得天线编码优化问题能够通过梯度下降高效求解。仿真结果表明，所提算法优于基于启发式搜索的算法，在实现更高平均信道增益的同时减少了计算时间。

## Abstract
Pixel antennas enable highly radiation pattern reconfigurability to enhance wireless systems, but its antenna coding design, that is optimizing the states of switches embedded in pixel antennas, remains an NP-hard challenge. Conventional approaches for antenna coding design typically rely on heuristic search algorithms, which suffer from high computational complexity. To overcome this issue, we propose a novel efficient data-free optimization algorithm called physics-informed neural optimizer (PINO) for antenna coding design. By integrating a deep convolutional neural network prior and a Gumbel-Sigmoid continuous relaxation into a differentiable physics engine, the proposed algorithm transforms the binary optimization problem into a continuous differentiable problem, which enables the antenna coding optimization problem to be efficiently solved via gradient descent. Simulation results demonstrate that the proposed algorithm outperforms the heuristic search based algorithms, reducing computational time while achieving higher average channel gain.

---

## 论文详细总结（自动生成）

# 基于物理信息神经优化的像素天线系统天线编码设计——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：像素天线通过控制RF开关的二进制状态（“天线编码”）可灵活重构辐射方向图，从而适应动态无线环境、提升信道增益。然而，天线编码设计是一个NP难的二元优化问题。现有方法主要依赖启发式搜索算法（如SEBO），计算复杂度高；而监督深度学习方法虽能加速，但仍需依赖SEBO生成大量标注数据进行离线训练，缺乏高效的无数据优化方案。
- **研究动机**：设计一种无需预收集数据、能高效求解二进制天线编码问题的数据无关优化算法，以降低计算复杂度并提升性能。
- **整体含义**：本文提出物理信息神经优化器（PINO），将物理模型（像素天线阻抗矩阵与辐射方向图）与深度学习（卷积神经网络）及连续松弛技巧（Gumbel-Sigmoid）结合，将NP难问题转化为连续可微问题，通过梯度下降并行搜索最优编码，为像素天线在6G等场景中的实用化提供高效解决方案。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程（文字说明）

- **核心思想**：利用深度神经网络作为结构先验，结合可微物理引擎和Gumbel-Sigmoid连续松弛，将离散的二元优化转化为连续优化，从而支持梯度下降反向传播，实现高效的无数据在线优化。
- **关键技术细节**：
  - **张量初始化模块**：将复值虚拟信道矩阵 \( \mathbf{H}_V \) 分解为实部和虚部两个通道，形成2×2K×2K的实值特征张量，并复制M份构成批次输入，实现多探针并行搜索。
  - **深度先验模块**：采用修改后的ResNet-50（首层卷积适配2通道输入）结合MLP头，从信道空间特征中提取与像素端口相关的logits（未归一化概率）。网络参数ρ可训练。
  - **Gumbel-Sigmoid连续松弛模块**：对每个logit注入Gumbel噪声（通过均匀采样构造），经Sigmoid函数得到连续软编码 \(\hat{b}_q^{(m)} \in (0,1)\)。温度τ按指数衰减 \(\tau(t) = \max(\tau_{\min}, \tau_0 \gamma^t)\)，迫使软编码在训练后期逼近{0,1}。
  - **硬决策模块**：训练过程中，软编码通过阈值0.5二值化得到硬编码 \(\mathbf{b}^{(m)}\)，用于评估真实物理性能。
  - **可微物理引擎**：基于像素天线多端口网络模型（阻抗矩阵Z、开路辐射方向图矩阵E_oc）计算软/硬增益。软增益定义为 \(G_{\text{soft}}^{(m)} = |\mathbf{e}_R^T(\hat{\mathbf{b}}^{(m)}) \mathbf{H}_V \mathbf{e}_T|^2\)，其中 \(\hat{\mathbf{b}}^{(m)}\) 通过对数插值映射为连续负载阻抗以保证梯度。硬增益 \(G_{\text{hard}}^{(m)}\) 类似，但使用硬编码。损失函数为平均软增益的负值：\(\mathcal{L}(\rho) = -\frac{1}{M}\sum_{m=1}^M F_{\text{phy}}(\hat{\mathbf{b}}^{(m)}, \mathbf{H}_V)\)。
- **算法流程**（文字说明）：
  1. 从信道矩阵构造批次张量，随机初始化网络权重ρ，设定最大训练轮数T_max、初始温度τ_0、收敛耐心窗口P。
  2. 每个训练轮次t，更新温度τ(t)，网络前向传播输出logits，结合Gumbel噪声生成软编码，物理引擎计算软增益和损失。
  3. 通过Autograd计算损失关于ρ的梯度，使用AdamW优化器更新网络参数。
  4. 计算当前平均硬增益，检查两个收敛准则：损失变化小于ϵ_L，且软硬增益差距小于ϵ_gap。若同时满足，则训练终止，选择最大硬增益对应的硬编码作为最终天线编码。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集/场景**：
  - 富散射瑞利衰落环境，波束空间信道矩阵元素独立同分布服从 \( \mathcal{CN}(0,1) \)。
  - 空间角度数K=72；发射天线为各向同性天线，接收端为像素天线（来自文献[6]的设计）：工作频率2.4 GHz，物理孔径0.5λ×0.5λ，Q=39个像素端口，λ=125 mm。
  - 使用CST Studio Suite仿真获得像素天线的阻抗矩阵Z和开路辐射方向图矩阵E_oc。
- **benchmark**：
  - 固定天线系统（Conv）
  - 离线码本设计（CDB，码本大小1024）[6]
  - 监督学习方法（HMSM）[12]
  - 启发式搜索SEBO（块大小J=10和J=20）[11]
- **对比方法**：PINO（探针数M=8,16,32,64,128,256），主要与SEBO和上述方法对比平均信道增益和计算时间。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及学习率5e-4，最大训练轮数500，温度衰减率0.998，收敛耐心窗口P=10，容忍阈值1e-3。
- **推断**：由于模型为ResNet-50（轻量级）配合MLP，且为在线训练（单信道样本迭代数百轮），推理成本较低，但具体硬件未提及，无法评估绝对算力需求。

## 5. 实验数量与充分性

- **实验组数**：
  - 收敛行为展示（M=64，固定信道样本）：图4(a)展示软/硬增益随训练轮次变化。
  - 探针数M的影响（M=8~256）：图4(b)展示平均信道增益和收敛所需轮次。
  - 与其他算法对比：图5(a)比较7种方法的平均信道增益（Conv, CDB, HMSM, SEBO10, PINO256等）。
  - 计算时间对比：图5(b)比较SEBO10、SEBO20、PINO256、PINO64的计算时间。
- **充分性分析**：
  - 消融实验仅涉及M这一超参数，未对网络架构（如ResNet-50深度、MLP大小）、温度衰减策略、损失函数形式等进行消融。
  - 仅使用单一信道环境（瑞利衰落），未在不同信道模型（如毫米波、非平稳信道）或不同像素天线设计参数下验证。
  - 对比方法中CDB和HMSM未参与计算时间对比，SEBO仅对比两个J值，未与SEBO在不同搜索块大小下的表现进行系统比较。
  - 总体实验覆盖了性能与复杂度核心指标，但广度有限，客观性尚可（因为对比方法均为公开文献方法，使用相同物理模型），公平性方面需注意PINO的探针数M=256对应约256×500轮计算，而SEBO的复杂度随J指数增长，对比合理但未考虑优化超参数搜索。

## 6. 论文的主要结论与发现

- **PINO能够有效求解天线编码优化**：收敛曲线表明软/硬增益在训练后期一致，且满足双收敛准则时停止。
- **增加探针数M可提升性能**：M从8增至256，平均信道增益显著提高，但M≥64后提升趋于饱和；同时更多探针提供更稳定梯度，显著减少收敛所需轮次。
- **PINO超越现有启发式方法**：PINO（M=256）的平均信道增益高于SEBO10、CDB、HMSM和固定天线系统。
- **计算复杂度显著降低**：PINO256的计算时间仅约4.17秒，远低于SEBO10的31744秒和SEBO20的指数级爆炸时间（未列具体值但趋势明显）。PINO64仅需约3.16秒。
- **结论**：PINO是高效且实用的数据无关天线编码优化方法，可平衡性能与复杂度。

## 7. 优点

- **创新性**：提出将物理模型（可微物理引擎）与深度神经网络先验、Gumbel-Sigmoid松弛结合的优化框架，将NP难问题转化为可微连续优化，突破传统启发式搜索的效率瓶颈。
- **无数据性**：无需预收集标注数据，完全在线优化，适用于实时或动态环境。
- **并行搜索**：通过批次探针实现多起点并发搜索，利用共享权重网络隐式平均梯度，提高全局搜索能力。
- **收敛机制**：设计双收敛准则（损失稳定、软硬增益对齐），自动终止训练，避免不必要的计算浪费。
- **实验充分性亮点**：展示了收敛过程、M的影响、与多种代表性方法对比，并量化计算时间，结果清晰有说服力。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅测试单一瑞利衰落信道模型，未评估在毫米波、非高斯噪声、时变信道等复杂场景下的表现。
  - 未对网络架构（ResNet-50深度、MLP结构）、温度衰减参数、损失函数形式等进行消融分析，无法评估各模块贡献。
  - 仅使用一个像素天线设计（Q=39），未验证Q较大或较小时PINO的扩展性。
  - 计算时间对比中，未提供SEBO20的具体耗时，且仅比较了SEBO两个J值，代表性有限。
  - 未与更先进的启发式优化（如遗传算法、模拟退火）或基于强化学习的方法对比。
- **潜在偏差风险**：
  - 物理模型基于CST仿真，实际硬件可能存在制造误差、阻抗不匹配等，未考虑模型失配的影响。
  - 假设发射天线各向同性、信道为富散射瑞利模型，可能高估真实场景下PINO的性能。
- **应用限制**：
  - PINO需要在线训练（每个信道样本需几百轮迭代），实时性可能受限，尤其低时延场景（如URLLC）。
  - 温度衰减策略和收敛阈值需手工设定，可能依赖经验调参。
  - 若大规模天线阵列（Q很大），ResNet-50输入尺寸2K×2K可能过大（本文K=72），实际中需降采样或改用轻量网络。
- **可复现性**：未提供开源代码或详细超参数设置（如学习率调度、批处理大小、网络层数等），不利于复现。

（完）
