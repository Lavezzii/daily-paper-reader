---
title: Learning to Compute on Dirty Paper
title_zh: 学习在脏纸上进行计算
authors: "Shreesal Shrestha, Kuranage Roche Rayan Ranasinghe, Giuseppe Thadeu Freitas de Abreu, Elza Erkip"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23252v1"
tags: ["query:channel-nn"]
score: 8.0
evidence: 用于脏纸编码和空中计算的神经网络编码器/解码器
tldr: 在无线通信中，脏纸编码（DPC）可非因果预消除已知干扰但计算复杂，而空中计算（AirComp）能高效估计函数但受干扰影响。本文提出首个完全基于学习的统一框架：每个用户利用带正弦激活的神经编码器学习预消除自身计算符号，恢复模类周期结构；联合神经解码器从接收信号恢复所有用户消息，另设神经AirComp估计器利用多时隙块结构在编码解码网络收敛后估计目标函数。实验表明该方法有效实现干扰预消除与函数估计，为集成通信与计算提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23252v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 905, \"height\": 350, \"label\": \"Figure\"}]"
motivation: 现有DPC和AirComp分离，缺乏统一学习框架来处理干扰预消除与函数估计的联合优化。
method: 各用户用正弦激活神经编码器非因果预消除自身计算符号；联合神经解码器恢复消息；神经AirComp估计器利用多时隙块结构估计目标函数。
result: 方法在干扰预消除和函数估计上均有效，恢复模类周期结构，实现端到端学习。
conclusion: 首个完全基于学习的DPC-AirComp统一框架，为集成通信与计算提供了可学习的新途径。
---

## 摘要
我们提出了一种基于学习的通信与计算一体化方法，该方法将脏纸编码与空中计算相结合。每个用户采用具有正弦激活函数的神经编码器，学习将其自身的计算符号作为非因果已知干扰进行预消除，从而恢复出与基于格点的脏纸编码方案一致的类模周期结构。一个联合神经解码器从接收信号中恢复所有用户的消息，而另一个独立的神经AirComp估计器则利用多时隙块结构，在编解码网络收敛后估计计算符号的目标函数。据我们所知，这是首个完全基于学习的方法，在统一框架中同时解决了基于脏纸编码的干扰预消除和空中计算问题。

## Abstract
We propose a fully learning-based approach to integrated communication and computing (ICC) that combines dirty paper coding (DPC) with over-the-air computation. Each user employs a neural encoder with sinusoidal activations that learns to pre-cancel its own computing symbol as non-causally known interference, recovering modulo-like periodic structures consistent with lattice-based DPC schemes. A joint neural decoder recovers all users' messages from the received signal, while a separate neural AirComp estimator exploits a multi-slot block structure to estimate a target function of the computing symbols after the encoder-decoder network converges. To our knowledge, this is the first fully learning-based approach to jointly address DPC-based interference pre-cancellation and over-the-air computation in a unified framework.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在无线通信中，脏纸编码（DPC）能够非因果地预消除已知干扰，但传统实现复杂；空中计算（AirComp）能高效聚合函数值，但易受干扰影响。现有工作将两者分离，缺乏一个统一的学习框架来联合优化干扰预消除与函数估计。
- **整体含义**：本文首次提出一个完全基于学习的统一框架，将DPC与AirComp结合，实现集成通信与计算（ICC）。每个用户利用神经网络学习预消除自身的计算符号（作为已知干扰），同时接收端通过另一网络估计计算符号的目标函数（如求和）。该框架无需显式信道状态信息（CSI），通过端到端训练自动学习周期性的类模结构，为未来无线通信与计算的深度融合提供了新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用神经网络的可学习性，将DPC的干扰预消除和AirComp的函数估计统一在一个端到端训练框架中。每个用户的神经编码器（正弦激活）学习将通信符号和计算符号映射为预消除信号，使得叠加后传输的信号在接收端既能恢复通信消息，又能支持函数聚合。
- **系统模型**：
  - 上行SIMO：K个单天线用户，接收端有N根天线。
  - 每个用户k发送QPSK通信符号d_k和计算符号s_k。
  - 用户k使用神经编码器e_θk输出预消除信号x_k = e_θk(d_k, s_k)，发送信号为v_k = x_k + s_k（将s_k视为已知干扰）。
  - 接收信号：y = Σ h_k v_k + n。
  - 计算任务：s_k在T个时隙内固定，通信符号独立变化；接收端需要估计目标函数f(s) = Σ s_k（算术和）。
- **神经网络架构**：
  - **编码器**：每个用户独立的2层隐藏层（128神经元）前馈网络，正弦激活（周期函数特性），输入为[d_k; s_k] 4维实向量，输出2维x_k，受功率约束E[||x_k||^2] ≤ P_x。
  - **联合解码器**：接收端单个神经网络，输入y ∈ R^(2N)，输出K个用户的QPSK软概率（logits），通过softmax和硬判决恢复消息。
  - **AirComp估计器**：有两种实现：
    - 扁平估计器：将T个时隙的观测y(t)和译码估计d̂(t)拼接成输入 z ∈ R^(T(2N+2K))，经过3层256神经元ReLU网络，输出f̂ ∈ R^2。
    - 循环估计器（GRU）：按时间顺序处理每个时隙的 [y(t); d̂(t)]，通过GRU更新隐藏状态，最后用两个全连接层输出估计值。
- **训练流程**：
  - 第一阶段：联合训练编码器和解码器，最小化通信损失 L_comm = 交叉熵 + λ·功率惩罚。
  - 第二阶段：冻结编解码器，训练AirComp估计器，最小化MSE损失 L_comp = E[||g_ω(z) - Σ s_k||^2]。
- **关键技术细节**：所有复数用实数等价表示；编码器无需CSI，解码器在训练中隐式学习信道。

## 3. 实验设计：数据集/场景、基准、对比方法
- **数据集/场景**：论文未提供任何实验验证或仿真结果。文中仅提出系统模型和网络架构，未说明使用何种信道模型、信噪比范围、用户数等具体场景参数。
- **基准方法**：未提及对比方法。仅引用了相关参考文献（如基于格点的DPC、传统AirComp），但未做数值比较。
- **对比方法**：无。

## 4. 资源与算力
- **未说明**：论文未提及使用的GPU型号、数量、训练时长等硬件资源信息。仅提到使用Adam优化器进行训练。

## 5. 实验数量与充分性
- **无实验**：全文只有理论框架描述，没有进行任何仿真或实际实验。因此无法评估实验的充分性、客观性或公平性。论文仅停留在概念提出阶段，缺乏数值结果验证其有效性。

## 6. 论文的主要结论与发现
- **主要结论**：
  - 提出首个完全基于学习的、统一DPC和AirComp的框架。
  - 通过正弦激活的神经编码器，能够学习到周期性的类模结构，与基于格点的DPC方案一致。
  - 联合解码器可恢复所有用户的通信消息。
  - 利用多时隙块结构，神经AirComp估计器能够有效估计计算符号的目标函数（如求和）。
  - 该框架无需显式CSI，通过端到端训练自动实现干扰预消除与函数估计。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次将DPC的干扰预消除与AirComp的聚合功能统一在一个完全学习的框架中，摆脱了对传统格点编码或手动设计的依赖。
- **无需CSI**：编码器不依赖信道信息，解码器在训练中隐式学习，降低了系统开销。
- **神经网络设计合理**：正弦激活符合DPC所需的周期性，GRU结构适合处理多时隙序列，为后续研究提供了可借鉴的设计思路。
- **模块化训练**：通信和计算任务分阶段训练，简化了优化难度。

## 8. 不足与局限
- **缺乏实验验证**：作为一篇提出方法的论文，未提供任何仿真结果（如误码率、NMSE、收敛曲线等），无法评估方法的实际性能，说服力不足。
- **实验覆盖不足**：没有考虑不同用户数、天线数、信噪比、块长度T等参数的影响，也未与现有DPC或AirComp方案对比，无法说明优势。
- **偏差风险**：仅基于理想假设（块衰落、固定计算符号、QPSK、算术和），未讨论更实际的场景（如频率选择性衰落、非正交波形、复杂函数等），应用限制较大。
- **计算复杂度未分析**：神经网络训练和推理的复杂度未给出，难以判断实际部署可行性。
- **资源与可重复性**：缺乏代码或详细超参数设置，他人难以复现。

（完）
